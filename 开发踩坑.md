# 开发踩坑记录

> 个人工作台 · 开发过程中遇到的问题与解决方法，持续更新。
> 每条格式：【问题】→【原因】→【解决】→【后续注意】。

---

## 环境与第三方资源

### #001 Windows 下 `/tmp` 写入失败（curl exit 23）
- **问题**：Git Bash 里 `curl -o /tmp/xxx` 报 exit 23（写入错误）。
- **原因**：Windows 下 Git Bash 的 `/tmp` 映射与实际权限不符，根目录不可写。
- **解决**：统一用项目内 `.tmp/` 目录做临时文件。
- **后续注意**：脚本中永远不要写 `/tmp`、`/xxx` 这类绝对 POSIX 路径。

### #002 PATH 上的 python 与 pip 版本不一致
- **问题**：`pip install fonttools` 成功但 `import fontTools` 失败。
- **原因**：PATH 上 `python` 是 3.11（hermes-agent venv，无 pip），`pip` 却是 miniconda 3.13 的，包装进了另一套解释器。
- **解决**：显式用 `C:\Users\echo\miniconda3\python.exe -m pip install` 安装并运行。
- **后续注意**：本机所有 Python 依赖操作统一走 miniconda 全路径。

### #003 GitHub Release 资产直连超时
- **问题**：下载霞鹜文楷 TTF（13MB）直连 github.com 300s 超时。
- **原因**：release 资产走 objects.githubusercontent.com，该网络下极慢。
- **解决**：`curl -C -` 断点续传 + 镜像 `https://gh-proxy.com/https://github.com/...`，实测 ~160KB/s 可用；jsdelivr/raw.githubusercontent 直连正常。
- **后续注意**：大文件优先 jsdelivr（npm 包）；release 资产用 gh-proxy 镜像 + `-C -` 续传。

### #004 UMD 库在 Node 里测试 global 名不对
- **问题**：`new Function(code + '; return solarlunar')()` 报 not defined。
- **原因**：node -e 环境下 `module`/`exports` 对 new Function 可见，UMD 走了 CommonJS 分支，没有挂到 window。
- **解决**：直接 `require('./vendor/solarlunar.min.js')` 测试；浏览器 `<script>` 引入后全局名 `solarlunar` 可用。
- **后续注意**：UMD 库优先用 require 验证逻辑，浏览器端名以 UMD 包裹器为准。

### #005 字体"精简字库"无现成单文件分发
- **问题**：需求要"霞鹜文楷精简字库（常用字子集 woff2）"，npm 上的 `lxgw-wenkai-lite-webfont` 是 582 个 unicode-range 分片（24.5MB/三字重），没有单文件常用字版。
- **解决**：下载完整 TTF（v1.522 Regular 13.9MB）后用 fontTools/pyftsubset 自裁：字符集 = 字频表 Top4500 常用汉字 ∪ 项目源码全部字符 ∪ ASCII/标点/符号，产出 **2.2MB woff2**（`vendor/font/lxgw-wenkai-subset.woff2`）。脚本 `make-font-subset.py` 可随时重跑（收尾时会再跑一次，覆盖最终全部文案）。
- **后续注意**：新增模块后如出现"字体回退到系统黑体"，重跑一次裁剪脚本即可。

## 架构与运行时（file:// 双击场景）

### #006 file:// 下不能用 ES Module
- **问题**：本地版要求"双击 index.html 即开"，而 `<script type="module" src>` 在 file:// 下被 Chrome 的 CORS 拦截。
- **解决**：全部 JS 采用经典 `<script>` 顺序加载 + 全局命名空间（`window.WB`），不做 import/export。模块加载顺序见 开发交接文档.md。
- **后续注意**：任何想引入打包器/ESM 的想法都会破坏"双击即开"，需连构建脚本一起改。

### #007 file:// 下不能 fetch 本地 JSON
- **解决**：内置数据（节假日/词库/书单/播客/食谱/每日一问）全部写成 `.js` 文件挂全局对象，不用 `fetch()`/`import()`。

### #008 Chrome 的 Web Speech 语音识别并非离线
- **问题**：需求规格假设"Web Speech 识别免费离线"，实测 Chrome/Edge 的 SpeechRecognition 走云端识别服务。
- **解决**：保留该功能（免费、无需密钥），但按"联网能力"对待：不可用时按钮置灰 + 提示，不报错；已记入交接文档「与需求的偏差」。
- **后续注意**：不要把任何依赖云端服务的功能宣传为"100% 离线"。

### #009 白噪音"真实录制音频内嵌"无素材来源
- **问题**：需求要求雨声/白噪/篝火真实录音转 base64 内嵌，开发环境无授权音频素材可下载。
- **解决**：三路白噪音全部改用 WebAudio 实时合成（过滤噪声模拟雨声/粉红噪/篝火噼啪），效果接近、零文件、无限时长，且反而减小了单文件构建体积。已记入「与需求的偏差」。
- **后续注意**：若日后拿到授权音频，替换 `js/modules/sound.js` 中三个合成器为 `<audio>` 循环播放即可。

### #010 el() 组件函数不接受数字/布尔子节点
- **问题**：`el("div", {}, 42, el("span"))` 报 `appendChild: parameter 1 is not of type 'Node'`。
- **原因**：子节点只处理了 string 和 Node，number 被直接透传。
- **解决**：`el()` 收尾统一 `typeof c === "object" ? c : createTextNode(String(c))`。

### #011 深层嵌套 el(...) 括号失配（两次 SyntaxError）
- **问题**：`el(a, attrs, el(b, attrs, el(c, attrs, map(...))))` 五层嵌套时少一个右括号，浏览器只报"missing ) after argument list"不给上下文。
- **解决**：超过 3 层嵌套就拆中间变量（如先建 `stepsOl` 再 append）；写了个 `for f in js; do node --check` 的循环做全量语法门禁。
- **后续注意**：语法检查脚本里 `node --check "$f" | head` 会吞退出码，必须用 `node --check "$f" && echo OK || echo FAIL` 的结构（曾因此误报全过，见 #013）。

### #012 跨午夜时 bizDate 与 todayStr 基准不一致
- **问题**：凌晨 0-4 点打开应用，「假期还有 X 天」比「倒数日」少算一天。
- **原因**：`nextHoliday()` 用 `todayStr()`（真实日期）做基准，其余模块用 `bizDate()`（凌晨 4 点前算昨天）。测试时恰好过了午夜，两者相差一天，暴露此问题。
- **解决**：`relDayLabel` / `nextHoliday` / `nextLunarBirthday` 全部统一用 `bizDate()` 做基准。
- **后续注意**：所有"相对今天"的计算禁止直接用 `todayStr()`，一律走 `bizDate()`。

### #013 bash 管道吞退出码导致语法门禁假阳性
- **问题**：`node --check "$f" 2>&1 | head -3 && echo OK` 永远打印 OK（head 的退出码是 0）。
- **解决**：`node --check "$f" > /dev/null 2>&1 && echo OK || echo FAIL`。
- **后续注意**：CI/门禁脚本里绝不让管道中间命令决定成败。

### #014 挂后台的浏览器里 rAF 停摆、GSAP 时间线冻结
- **问题**：自动化测试时欢迎雾层迟迟不消失。
- **原因**：后台标签页 rAF 暂停，GSAP timeline 停在第一帧；setTimeout 也被节流。
- **解决**：入场动画加 4 秒 `setTimeout` 兜底强制移除雾层；功能逻辑（番茄钟等）全部基于真实时间戳而非累计 rAF。
- **后续注意**：任何"必须播完才能用"的入场动画都要有计时器兜底。

### #015 UMD 库浏览器全局名与 npm 包名不同
- **问题**：`solarlunar` 在 Node 里 require 正常，浏览器里想当然写 `window.solarLunar` 报 undefined。
- **解决**：UMD 暴露名以源码包裹器为准（本例是全小写 `solarlunar`）；`qrcode-generator` 暴露为 `qrcode`。
- **后续注意**：vendor 一个库后先在控制台 `typeof 全局名` 验证一次。

### #016 CloudBase MCP 未登录无法自动部署
- **问题**：云端部署需要用户扫码/授权登录（auth REQUIRED），AI 无法代办。
- **解决**：全部云端物料已备好（云函数源码/cloudbaserc/部署脚本 deploy.sh/单文件构建产物 cloud/hosting/），用户登录后执行 `ENV_ID=你的环境ID bash deploy.sh` 一键完成。步骤见 开发交接文档.md §6。

### #017 .bat 里写中文 + chcp 65001 会"错位执行"
- **问题**：启动脚本内容含中文（`title 个人工作台…`）且文件为 UTF-8，运行报 `'浣滃彴' 不是内部或外部命令`（UTF-8 字节被按 GBK 断句后当命令执行）。
- **原因**：cmd 解析批处理按字节偏移逐行读，`chcp 65001` 切换码页后偏移口径变化，多字节字符导致行边界错位。
- **解决**：**.bat 内容一律纯 ASCII**（注释/提示都用英文），文件名可以用中文（资源管理器双击不受影响）。
- **后续注意**：给中文 Windows 用户的脚本，凡是 `.bat/.cmd` 就别在内容里写中文；要中文提示就改用 `.ps1`（UTF-8 BOM）。

### #018 mp3 截断后元数据时长仍显示全长，素材处理前必须校验完整性
- **问题**：并行 curl 下载 freesound 试听 mp3 时 `--max-time` 把传输掐断，文件少了近一半；ffprobe 仍按头部 Xing 帧报全长，后续 atrim/响度处理出的音频莫名短几十秒，且与 loudnorm 无关（换静态增益同样短）。
- **原因**：mp3 时长写在头部元数据里，截断文件元数据不变，解码到断点即止；元数据不可信。
- **解决**：下载后比对 `curl -I` 的 Content-Length 与实际字节数；`ffmpeg -v error -i f.mp3 -f null -` 应零输出；各处理步骤的时长以解码产物（wav/ogg）的 ffprobe 为准。
- **后续注意**：素材管线按"下载→校验字节数→处理→校验时长"走，任何一步对不上就停下重下。

### #019 npm init 在中文目录名下直接失败
- **问题**：`npm init -y` 报 `Invalid name: "个人工作台"`，npm 包名不允许中文。
- **解决**：手写 `package.json`（name 用 ASCII，如 personal-workbench）。
- **后续注意**：中文目录的项目要装 npm 依赖，别指望 npm init。

### #020 esbuild CLI 的 `--define` 在 Git Bash 里引号被吞
- **问题**：`npx esbuild --define:process.env.NODE_ENV='"production"'` 报错退出（bash 把引号层层剥掉后 esbuild 解析失败），`--version` 却正常，极具迷惑性。
- **解决**：改用 esbuild JS API（写 `.mjs` 构建脚本调用 `esbuild.build()`），参数类型化且报错完整；本项目落地在 `tools/build-islands.mjs`。
- **后续注意**：Git Bash 下给 CLI 传含引号的参数，优先脚本化调用而不是跟转义搏斗。

### #021 React/Three 裸导入的解析：工作区不装 React 也能打包
- **问题**：islands 入口 `import "react"` 报 Could not resolve——工作区没装 React。
- **解决**：esbuild `nodePaths` 指向 ThreeUI 素材仓库的 `node_modules`（含 react/react-dom/three165 别名包），bundle 时把 React 一并打进 IIFE；islands 产物提交仓库，运行时零依赖。ThreeUI 仓库缺失时 build-islands 跳过、用已提交产物。
- **后续注意**："从外部仓库借源码打包"用 nodePaths，而不是把对方整个 vendor 进来。

### #022 ThreeUI 组件脱离站点使用：布局 CSS 与 props 名的坑
- **问题一**：React 组件只带结构不带样式，esbuild 打包不会带 `styles.css` → 画布 0 高/不铺满，看起来像"渲染黑屏没内容"。
- **问题二**：EnergyOrb 有**两块 canvas**（星场 2D + 星球 WebGL），不做 `position:absolute` 会纵向堆叠，星球画布被 `overflow:hidden` 圆形容器裁掉——表现为"只有星场没有星球"；实际 WebGL buffer 里像素是对的，用 readPixels 才定位到是裁剪问题。
- **问题三**：组件 props 名与站点控件 label 不同：站点写 Pointer/Tint，代码是 `mouseAmount`/`tintAmount`；EnergyOrb 的 `hue` 单位是度且基座是紫（约 260°），要金色传 ~145。
- **解决**：自补四条布局规则（`.threeui-background{position:absolute;inset:0}`、canvas `position:absolute;inset:0;width/height:100%`、双 canvas 各自 absolute）；参数以源码 props 类型为准。
- **后续注意**：借第三方 React 视觉组件，先建独立 spike HTML 补齐布局验证，再集成进主应用。

### #023 自定义 `display:flex` 会覆盖 `[hidden]` 的 UA 样式
- **问题**：`#scene-viewer{display:flex}` 写进 CSS 后，HTML 的 `hidden` 属性失效，浮层"默认隐藏"却一直显示空框架。
- **原因**：UA 样式 `[hidden]{display:none}` 特异性低于 `#id{display:flex}`。
- **解决**：紧跟补 `#scene-viewer[hidden]{display:none;}`。
- **后续注意**：给可能带 hidden 的元素写 display 时，永远同时写 `[hidden]` 覆盖规则。

### #024 `WB.registerModule` 不产生全局导出——books/podcasts/notes 三连坑
- **问题**：今天页"一书/播客"每日推荐永不出现；快捷键 N"新笔记"没反应。三处同款：books.js / podcasts.js / notes.js 把 `dailyPick`/`quickAdd` 写进了 `registerModule({...})` 对象，但从未挂 `WB.<name>`，跨文件的守卫 `if(WB.books && ...)` 恒为 false，**静默跳过、零报错**。
- **原因**：`registerModule` 只做路由注册（`routes[mod.id] = mod`），不创建 `WB.<id>` 全局引用；误以为"注册即可跨模块调用"。
- **解决**：模块文件尾部显式导出：`WB.books = {dailyPick}`、`WB.podcasts = {dailyPick}`、`WB.notes = {quickAdd(){...}}`。
- **后续注意**：模块能力要被别的文件调用，必须显式挂 WB 命名空间；写 `WB.<name>.` 之前先 grep `WB.<name> =` 确认导出真的存在。

### #025 快捷键 N/T 抛 ReferenceError——router 函数困在 IIFE 里被裸调
- **问题**：按 N/T 完全无反应，控制台 `Uncaught ReferenceError: go is not defined`；表象是"快捷键只有 Ctrl+K 能用"。
- **原因**：`09-router.js` 的 `go` 定义在 IIFE 内部，仅经 `WB.router` 导出；main.js 快捷键分支直接裸调 `go("notes")`。Ctrl+K 分支在守卫检查之前就 return，所以唯独它能用。
- **解决**：改为 `WB.router.go(...)`。
- **后续注意**：IIFE 内未导出的函数对其他文件不可见；跨文件调用统一走 `WB.router.*` / `WB.<name>.*`。

### #026 空格键在非番茄页报 innerHTML null——toggleAll 未守卫未挂载面板
- **问题**：在番茄钟以外的任何页面按空格，声音实际切换了，但控制台报 `Cannot set properties of null (setting 'innerHTML')`（sound.js renderPanelInto）。
- **原因**：声音面板只在番茄页 `renderPanel` 挂载（`panelRoot` 非空）；`toggleAll()` 结尾直接 `renderPanelInto(panelRoot)`，`panelRoot` 为 null 时对 null 写属性。同文件的 `muteAll` 早已用带守卫的 `refreshPanel()`，`toggleAll` 漏了。
- **解决**：`toggleAll` 两处 `renderPanelInto(panelRoot)` 改调 `refreshPanel()`。
- **后续注意**：全局快捷键触发的 UI 刷新，必须容忍目标组件未挂载（守卫后再刷）。

### #027 无头测试快捷键的两个小坑
- **坑一**：想清理弹窗时手动 `scrim.remove()` 只删了 DOM，`WB.modalOpen()` 的栈还在——守卫"弹窗内只留 Esc"把后续按键全拦了，测试结果全错。要用 `WB.closeTopModal()` 正常关闭。
- **坑二**：断言星标高亮用 `style.color.includes("e8cf8f")` 永远 false——CSSOM 把十六进制序列化成 `rgb(232, 207, 143)`。断言颜色要么比较 rgb 值，要么 getComputedStyle。
- **后续注意**：测 UI 状态一律走应用自身的关闭/状态接口，别绕过它手动改 DOM。

### #028 × 关闭弹窗后快捷键全失灵——modal 栈只包装了返回对象的 close
- **问题**：按 N/T 弹出新建弹窗，点 × 关掉（不保存），再按同一快捷键毫无反应；Esc 按一次能救回来。
- **原因**：main.js 的 modal 栈包装（供 Esc/`modalOpen()` 用）只替换**返回对象**上的 `m.close`；而 06-ui.js 里 × 按钮和遮罩 mousedown 直调内部 `close()`，绕过了包装——弹窗从 DOM 移除了，栈里的记录永远不弹，`modalOpen()` 恒真，快捷键守卫"弹窗内只留 Esc"把其余键全拦住。
- **解决**：06-ui.js modal() 内部建 `const m = {el, body, close: null}`，内部所有关闭点（× 按钮、actions 按钮、遮罩点击）统一改走 `m.close()`，返回 `m`。包装器替换 `m.close` 后，运行时点击走到的是包装后的版本，栈正常弹出。
- **后续注意**：包装/装饰一个对象的方法时，对象内部的自引用必须走同一入口（运行时经 `m.xxx` 查找），不能在闭包里提前捕获直调。

### #029 找免费音频素材：freesound CDN 被墙，OpenGameArt 是可达出路
- **问题**：给声音面板补四路真实录音，freesound.org 主站能开（搜索页 curl 正常），但 `cdn.freesound.org` 预览直链和 `/data/previews/` 路径全部 TCP 复位/SSL reset，0 字节；`-C -` 续传、伪装 UA、公共 CORS 代理（allorigins/corsproxy，返回的是报错 JSON）都救不回。同批不可达：upload.wikimedia.org、web.archive.org、itch.io、soundbible.com；pixabay.com 页面能开但音频直链要登录+防盗链（403）。
- **解决**：走 **OpenGameArt.org**——站点和 `sites/default/files/` 文件直链都直连可用，CC0 素材库够大（搜 `art-search-advanced?keys=xxx&field_art_licenses_tid[]=4`，4=CC0）。钢琴/氛围/Lo-Fi/白噪四路全部从 OGA 取得，授权页逐个核对 CC0 图标。构建侧注意：`build.js --cloud` 会用自己硬编码的清单**覆盖** sw.js 的 SHELL，预缓存要改 build.js 那份。
- **另记两个小坑**：① Write 工具在 Windows 写出的 bash 脚本带 CRLF，ffmpeg 滤镜串里的 `\r` 导致静默失败——`tr -d '\r' < x.sh | bash` 管道跑（钩子会拦 Bash 直接落盘脚本文件，管道不落盘可行）；② `loudnorm=print_format=json` 的键值格式是 `"input_i" : "-25.05"`（冒号两侧有空格、值可能带负号），正则要写 `\"input_i\" : \"(-?[0-9.]+)\"`。
- **后续注意**：以后找 CC0 音频直接从 OpenGameArt 开始；下载完必须 ffprobe 时长 + `ffmpeg -f null -` 解码校验（踩坑 #018）；上 sw 预缓存的资源同步改 build.js。

## 动效 / 交互手感（2026-09-18，待优化清单 §1–§4）

### #030 `$$` 在 06-ui.js 里从未定义 → 整套拖拽重排静默失效
- **问题**：做 2.8「拖拽 FLIP」时用无头浏览器验收，抛 `Uncaught ReferenceError: $$ is not defined`；`onReorder` 从不触发、位移动画也看不到。
- **原因**：`js/06-ui.js` 顶部只解构了 `const { el, esc, icon } = WB;`，而 `draggable()` 的 `dragover / dragend / drop` 三处都直接调用了裸 `$$`。`WB.$$` 本身是存在的（`01-utils.js:8`），只是没解构进来。**这是从第一版就带着的 bug**——报错发生在事件回调里，页面其余部分照常工作，且 `test-smoke.mjs` 只遍历路由、不触发拖拽，所以一直没被发现。
- **解决**：解构改为 `const { el, esc, icon, $$ } = WB;`，并加注释说明为什么必须一起解构。
- **后续注意**：跨模块工具（`$ / $$ / el / esc / uid / clamp / debounce`）必须显式解构或用 `WB.` 前缀。`node --check` 只能查语法，查不出这种「跑到才炸」的自由变量——新增事件回调后要用无头浏览器真跑一遍触发路径。

### #031 冷却闸门不能卡在 `render()` 入口
- **问题**：2.1 要求给路由加冷却闸门，直觉写法是 `render()` 第一行 `if(!WB.ui.lock("route", 400)) return;`。
- **原因**：`go()` 是先改 `location.hash`、再由 `hashchange` 触发 `render()`。冷却期被丢掉的 render 不会补发 → hash 已变、视图还是旧的，状态与视觉不一致；同理 `WB.router.render()` 也是各模块改完数据后的主动刷新入口（设置页分段控件、命令面板快速捕捉），被丢掉就是「点了没反应」。
- **解决**：闸门只锁**入场动画**（GSAP 补间 + `.view-inner.fresh` 的 CSS 动画），内容渲染永远执行；冷却期内直接以终态呈现——既不丢内容也不叠帧。
- **后续注意**：加互斥闸门先问一句「被丢掉的那次操作，有没有人负责补发？」没有补发机制就只能锁表现层，不能锁状态层。

### #032 入场编排会双写 opacity/transform
- **问题**：一度在 `09-router.js` 的 `render()` 里同时调了 `WB.ui.staggerIn(inner)`，结果卡片被 `main.js` 的 GSAP `animateCards` 与 CSS 错峰动画同时驱动，opacity/transform 互相打架。
- **原因**：`animateCards` 有 60ms 延迟，两条动画链落在同一批节点上。
- **解决**：立规矩「一份节点只由一条链驱动」——`animateCards` 里有 `.card` 就走 GSAP，没有卡片才走 CSS 错峰（二选一 return）；`render()` 里不再做错峰。
- **后续注意**：新增入场编排前，先确认目标节点是否已被现有动画链覆盖。

### #033 弹窗遮罩的 `body.scrim-open` 摘不掉（退场定时器与状态同步错位）
- **问题**：4.5 用 `body.scrim-open` 让侧栏在弹窗打开时摘掉 `backdrop-filter`。首版在 `close()` 里同步调 `syncScrim()`，但遮罩要 200ms 后才真正 `remove()` → 查询仍然命中，类名永远摘不掉，侧栏白丢一层模糊。
- **解决**：`syncScrim()` 必须跟在**真正移除**之后——立即移除的分支里紧接 remove 调用；带动画的分支放进 `setTimeout(() => { scrim.remove(); syncScrim(); }, 200)`。
- **后续注意**：「DOM 里还在」与「逻辑上已关闭」是两件事。基于 DOM 查询判断状态的，要挂在移除之后；基于 phase 变量判断的（`WB.modalOpen` / `commands.isOpen` / `router.sheetOpen`），要在退场一开始就切换。

### #034 命令面板输入防抖的回调悬空（原实现漏了守卫）
- **问题**：`commands.js` 的 `input` 事件走 `WB.debounce(..., 120)`；若在 120ms 内回车关闭面板，`close()` 已把 `input / listEl` 置空，防抖回调随后触发 → `input.value` 抛 TypeError。
- **解决**：`paint()` 开头加 `if(!input || !listEl) return;`。
- **后续注意**：凡是「延时回调 + 提前销毁引用」的组合，回调入口都要加空值守卫。`close()` 里把引用置空本身是对的（便于 GC），错的是不设防的回调。

### #035 退场兜底定时器会误伤「期间重新打开」的面板
- **问题**：`scenes.js` 加了 `setTimeout(done, 500)` 兜底（后台标签页 rAF 停摆时 GSAP 补间冻结，见 #014）。但用户若在这 500ms 内重新开窗，旧兜底照样执行 → 新窗被清空 `iframe.src`，白屏。
- **解决**：加自增代次令牌 `gen`，开/关都 `++gen`，`done()` 里 `if(settled || g !== gen) return;`。命令面板与移动端抽屉用同样的 `token` 对比。
- **后续注意**：任何「延迟执行 + 可能被新操作覆盖」的收尾逻辑都要配代次令牌。这和 2.1 的冷却闸门是同一族问题的两种解法：**闸门防重叠，令牌防误伤**。

### #036 `#topbar` 抬不到 `--z-sheet` 之上
- **问题**：4.1 建议把 `#topbar` 抬到抽屉之上，让移动端抽屉打开时顶栏仍可点击。
- **原因**：`#app` 有 `position:relative; z-index:var(--z-content)` → 已形成**层叠上下文**，`#topbar` 给多大 z-index 都被封在 `--z-content`(=1) 这一层里，永远低于 `#sheet-root`(=110)。而把 `#app` 整体抬到 110 以上，内容层又会反过来压住 `.modal-scrim`(120) 之下的一切浮层，弹窗直接失效。
- **解决**：走文档给的备选方案——**抽屉内提供显式关闭入口**（`.sheet-head` 把手 + 右上角叉号，汉堡图标同时交叉变形为叉号），Esc 也能关。遮罩覆盖内容层是模态行为的正确语义，不该为了「顶栏可点」破坏它。
- **后续注意**：调层级前先确认祖先链上有没有已形成的层叠上下文（`position` + `z-index != auto`、`transform`、`filter`、`backdrop-filter` 都会造），否则 z-index 调了不生效、白排查。

### #037 动态元素的「CSS 里查不到的魔法值」
- **问题**：`pomodoro.js` 的全屏提示把 `z-index:190`、`backdropFilter:blur(10px)`、`transition:opacity .6s` 全写在 `el()` 的 style 对象里；`15-gestures.js` 的 `#pull-tip` 是一整串内联 `cssText`。CSS 里 grep 不到这些层级/模糊值；更麻烦的是内联 `animation` 会**绕过** `html.no-motion *{animation:none !important}`（层叠中 inline 非 important 低于样式表 important）。
- **原因**：`el()` 的 `style` 走 `Object.assign(node.style, v)`，因此 `zIndex:"var(--z-alert)"` 这类写法也不保险（CSSOM 对 `var()` 的接受度按属性而异）。
- **解决**：动态创建的元素一律给 `class`，样式进 `css/main.css`（`.pomo-flash` / `#pull-tip` / `.card.skeleton` / `.pet-art`），层级读 `--z-*`、模糊读 `--blur-*`；JS 只保留真正需要逐帧改的属性（`transform` / `opacity`）。
- **后续注意**：新增动态元素先写 CSS 类再写 JS。`grep -n "z-index\|backdrop-filter\|animation:" js/**/*.js` 应当只剩受控的少数几处。

### #038 缓动 token 统一时，长距离旋转不能硬塞进四档
- **问题**：把 `cubic-bezier` 全部收进 `--ease-*` 时，「吃什么转盘」用的是 `cubic-bezier(.15,.9,.25,1)`（要长时间滑行后缓缓停下），四档里没有等价的：`--ease-pop` 回弹过头，`--ease-flow` 前段太快。
- **解决**：单开第五档 `--ease-spin`（长距离旋转档）。验收要求是「grep 结果只剩 `:root`」，但「只剩 `:root`」不等于「硬压成四档」。
- **后续注意**：token 化的目标是「同一族效果取同一个值」，不是「个数越少越好」。遇到表达不了的效果应该补档，并在注释里写清适用场景。

### #039 `swapIcon` 与 `iconHydrate` 抢同一个按钮
- **问题**：`#btn-theme` 原本带 `data-icon="moon"`。`WB.iconHydrate(document)` 在 boot 里会给 `[data-icon]` 元素 `insertAdjacentHTML("afterbegin", …)` 再插一个 `<svg>` → 交叉变形的按钮里成了 3 个图标，`.icon-swap>*:last-child` 指向注入的那个，切换时两个图标同时可见。
- **原因**：`iconHydrate` 靠 `n.__iconed` 去重，而 `swapIcon` 是直接写 `innerHTML`，没参与这套标记。
- **解决**：① `swapIcon` 里补 `btn.__iconed = true;`；② `#btn-theme` 去掉 `data-icon`（图标本就该由 theme 模块按当前主题决定）；③ 补 `.icon-btn.icon-swap{display:inline-grid}` 压过 `.icon-btn` 的 `inline-flex`（否则两个 `<span>` 会并排而非叠放）。
- **后续注意**：凡「由 JS 注入完整 innerHTML」的按钮都不要同时挂 `data-icon`；`icon-swap` 用到既有 flex 按钮上时记得覆盖 `display`。

### #040 只挂在 `toggleTheme()` 里的图标同步，被「设置页改主题」绕过了
- **问题**：2.2 的太阳/月亮交叉变形只在 `toggleTheme()` 里调了 `swapIcon`。但设置页「深浅色」走的是 `theme.set("theme", v)`，`theme: "auto"` 还会跟随系统变化——这两条路径都不经过 `toggleTheme`。结果暗色主题下顶栏仍显示月亮，图标与实际状态脱节。
- **发现方式**：`node --check`、冒烟（只看 console 错误）、行为断言（只断言了「连点 D 只切一次」）全都没抓到。是桌面视口截图人工复核时看到「暗色下是个月亮」才发现的。
- **解决**：把 `swapIcon` 挪进 `theme.apply()`——它是所有变更路径的唯一汇聚点（`set` / `merge` / `auto` 的系统监听 / 首帧 都从这里走），`toggleTheme` 与 `init` 里的重复调用删掉。
- **后续注意**：**派生状态（图标、`meta`、类名）要挂在唯一的汇聚函数上，不要挂在某个具体入口上**——本项目的三个汇聚点是 `theme.apply()`、`router.render()`、各模块的 `render()`。另外冒烟测试只保证「不报错」，视觉正确性得靠截图人工复核，`.tmp/smoke/` 的截图别只看第一张。

### #041 给 `.icon-swap` 加 `display:inline-grid !important` 会顺带把 `.mobile-only` 的显隐压掉
- **问题**：`.icon-swap` 最初用 `display:inline-grid; place-items:center` 让两个图标叠放。但 `#btn-theme` 是 `.icon-btn`（`inline-flex`）会并排显示，`#btn-menu` 更是 `.mobile-only{display:flex !important}`。为了压过它们，改成了 `.icon-btn.icon-swap{display:inline-grid !important}`——结果特异性 `(0,2,0) + !important` 连 `.mobile-only{display:none !important}` 也一起赢了，**汉堡按钮在桌面端冒了出来**。
- **发现方式**：复验脚本里顺手打印了 `getComputedStyle(btn).display`，桌面宽度下期望 `none` 却拿到 `grid`。
- **解决**：不去改宿主的 `display`。`.icon-swap` 只加 `position:relative`，两个子元素改成 `position:absolute; inset:0` 自行叠放 —— 宿主是 `flex` / `inline-flex` / `grid` 都无所谓，也就完全不用碰 `!important`。
- **后续注意**：`!important` 是**全局**的，它不区分「同一个元素上的不同声明」和「不同元素上的同族规则」。要靠 `!important` + 提特异性去赢一条规则时，先列出这个选择器还可能命中哪些元素、原规则里还有哪些属性会一起被翻掉。能用「换个实现方式绕开冲突」就别加 `!important`。
- **顺带**：验收时看到 `getBoundingClientRect()` 宽高 27px（36 × 0.75）不要慌——`transform:scale(.75)` 会让包围盒一起缩，`left/top` 偏移 4.5px = `(36-27)/2`，说明它**是居中的**，两个图标确实完全叠放。

---

## 沉浸专注层（2026-09-21，参考 Lumora 的沉浸式番茄钟）

### #042 `confirmBox` 点完按钮从不关闭弹窗 → 全屏遮罩永久挡住整页
- **问题**：做沉浸专注层的「放弃」确认时，无头截图里发现点完「放弃 / 再想想」后，弹窗还挂在屏幕上；再确认：`.modal-scrim` 仍在 DOM 里，它是 `position:fixed; inset:0; z-index:120`，把整页点击全吃掉，连 Esc 都关不掉。
- **原因**：`06-ui.js` 的 `modal()` 里动作按钮的守卫写反了 ——
  `const keep = a.onClick ? a.onClick(...) : false; if(!keep && a.onClick) return; if(!a.onClick || !keep) close();`
  只要动作带 `onClick`，**无论返回什么都不会自动关**（`todos.js:168` 的注释「return true; // 不自动关」正是这条约定）。全体调用点因此都靠「自己 `m.close()` + `return true`」收尾，只有 `confirmBox` 的按钮既不关也不返回 true。另外 `confirmBox` 用内部 `modal()`，绕过了 `main.js` 包过栈的 `WB.ui.modal`，所以 `WB.modalOpen()` 也不认得它、`WB.closeTopModal()` 关不掉它。
- **发现方式**：断言全过（业务逻辑确实跑完了、promise 也 resolve 了），是**截图人工复核**时看到拖着的弹窗才追出来的。同类问题 `commands.js:157`「已复制，关闭」、`todos.js:228/309`「完成」、`today.js:637` 也一样（都不带 `m.close()`），属于同一个根因。
- **解决**：只修被本次功能踩到的这一条路径（不动 100+ 调用点的既有约定）——`confirmBox` 的两个按钮改成 `resolve(...); m.close(); return true;`，并且改走 `WB.ui.modal`（带栈版本），这样 Esc 会先关确认框、`modalOpen()` 也会算上它。其余调用点保持原样，待用户决定是否统一改成「falsy 即自动关」。
- **后续注意**：本项目 `modal()` 的语义是**「带 onClick 的动作由它自己负责关闭」**。新增弹窗动作时，要么在成功路径里显式 `m.close()` 并 `return true`，要么不带 `onClick`（那样框架会自动关）。写完弹窗务必用无头浏览器点一次按钮，再数一遍 `document.querySelectorAll(".modal-scrim").length` 是否为 0。

### #043 headless 不出帧时，CSS 过渡的"当前值"会骗人
- **问题**：探针里进入沉浸层 900ms 后读 `getComputedStyle(#app).opacity`，拿到 `0.999923`；读沉浸层是 `7.68702e-05` —— 看起来像「CSS 没生效」。
- **原因**：CSS 过渡的起始时刻是在**样式被真正计算/出帧**时敲定的。无头环境没有持续出帧，第一次 `getComputedStyle` 才触发样式计算 → 过渡的 0ms 就是那一刻。于是读到的是刚起跑的值，而不是真实时间轴上的值。
- **解决**：读过渡结果前先强制出一帧（`page.screenshot({clip:{x:0,y:0,width:8,height:8}})`），再等够时长、再出一帧、再读。
- **后续注意**：headless 断言只对「DOM 类名 / 内联样式 / JS 状态 / 不需要插值的计算样式（z-index、opacity 的终值）」可靠；凡是依赖过渡/动画中间态的，要么强制出帧，要么改断言目标。另：**全新 profile 启动会自动弹「晨间仪式」**（`lastOpenDay` 未记录），它会盖住被断言元素并吃掉键盘事件 —— 探针开头必须先 `WB.closeTopModal()` + 写 `lastOpenDay`。

### #044 沉浸层的两个"少写一行就静默失效"的地方
- **问题一**：`WB.immersive.finishPhase()` 调 `WB.pomodoro.finish()` 永远无效（不报错）。原因：`pomodoro.js` 结尾的 `WB.pomodoro = {...}` 里**没有导出 `finish` / `stopAndClear`**，`WB.pomodoro.finish` 是 `undefined`，`else if(WB.pomodoro.finish)` 直接跳过。已补导出。
- **问题二**：空格键在「待确认」状态下走了 `resume()` —— 那会把**已经结算过的这一段重新跑起来**（`resume()` 会清掉 `needConfirm`），并且到点再结算一次，重复记一条成绩。改为：待确认时空格 = 推进到下一段（`confirmPhase`），与主按钮语义一致。
- **后续注意**：跨文件调用的能力必须在文件尾部显式挂到 `WB.<name>`（踩坑 #024 的老规律，这次又栽了一次）；`resume()` 只对「暂停中」的会话成立，凡是「已跑完/待确认」的状态都要先判断 `needConfirm`。

### #045 全屏 `requestFullscreen()` 的两种"假成功"
- **现象**：无头环境里 `requestFullscreen()` 被拒（Promise reject，`fullscreenElement` 仍为 null），但沉浸层其余部分（隐藏 UI、计时、环、数字）全部照常 —— 这是**设计行为**：请求被 `.catch(() => {})` 静默吞掉，只是不占满屏。真实浏览器里按 F 才会真正全屏，**需人工确认**。
- **另一种**：用户按 Esc / F11 退出全屏时会触发 `fullscreenchange`。沉浸层监听它并同步退出（否则会出现「全屏没了但 UI 还藏着」的错位）。注意判断条件是 `active && fsAsked && !document.fullscreenElement` —— 用户在没请求全屏时按 F11 **不该**被当成退出。
- **后续注意**：浏览器全屏无法在无头环境验证，验收口径写清楚「被拒也能用」，真机/真浏览器要人工过一遍 F → Esc → Esc 的三连。

### #046 想做出"水光"，就别用能看出重复周期的图案（B 轮动态观感）
- **问题**：给沉浸层的「深海」加动态，第一版用两条细硬边 `repeating-linear-gradient` 交叉 → 截图里变成一张 **方格纸**；改成软边（三段式渐变做进/出缓坡）后变成一片 **竖条纹帘子**。两次都不像水。
- **原因**：`repeating-*` 图案的周期是可被视觉解析的——尤其叠两层（不同角度）会立刻出现摩尔纹与格子。水光的成因是**不规则的、各自缓慢游动的大光块**，而任何周期性重复都在跟这个物理事实打架。
- **解决**：「深海」改成 4 团不规则大柔光斑（`radial-gradient` 38%/30% 大小），分两组、周期 44s / 67s（`-22s` 错相）反向漂移 —— 没有周期、边缘天然软。同样思路下：星野用 **5 层互质平铺尺寸**（150/210/262/318/386px）铺星点，把平铺周期互质化，人眼就看不出规律。
- **顺带**：星点半径从 1.6px 提到 2.2–3px 才在 1440 宽屏上看得见；1.6px 只在 4K 截图上勉强可见。凡是"极细的装饰元素"，验收一定要在**目标屏幕尺寸**上看截图，别看放大图。
- **性能约定**：这些层全部只动 `opacity` / `transform`（合成层内完成，不触发 layout、不重绘），并且都是 CSS keyframes —— `html.no-motion` 一开就整体消失，不用写额外守卫。

### #047 "参考项目里的动态画面"其实是远程视频，本地没有素材
- **问题**：用户希望把参考项目（`vibecoding/111`）那种沉浸式动态画面搬过来。查完发现：那个项目的 `public/` 里**一个视频文件都没有**，`scenes.ts` 的四段 `videoUrl` 全部指向 `d8j0ntlcm91z4.cloudfront.net/user_38xzZboKViGWJOttwIXH07lWA1P/hf_*.mp4`（第三方 AI 生成素材，README 只标注了 CC0 音频，**没有标注视频来源与授权**）。
- **结论**：**"搬过来"这件事没有可以复制的本地文件**。只能三选一：① 直接外链同一批 URL（要联网、离线就没画面）；② 把 mp4 下载进仓库（先要确认授权，仓库 +6~8MB，单文件版还得降级）；③ 另找 CC0 循环视频（像本项目音频那样走 OpenGameArt/CC0 台账）。
- **教训**：遇到"参考项目效果很好"时，**先确认那个效果是靠什么实现的**（视频？WebGL？远程资源？），再决定要不要跟。它那边真正可搬的是**方法论**（细线环、逐字符定宽、时间戳计时、键位隐藏 UI），画面本身是素材问题，不是代码问题。

### #048 视频背景的三条坑：防盗链 / 循环缝 / 明暗极性
- **① 防盗链**：`assets.mixkit.co` 裸 curl 直连会连接失败（返回 000，不是 404/403，很容易误判成素材没了）。**下载必须带 `Referer: https://mixkit.co/` + 浏览器 UA**；探活也别用 HEAD（会被拒），用带 Referer 的 Range GET。
- **② 循环缝**：截取中间几秒直接 `loop` 会在接缝处硬切。用**波折循环（boomerang）**——正放 + 倒放拼接（`split` → `reverse` → `concat`），接缝两端是同一帧，天然无缝；对水下光纹 / 雾 / 星空这类无方向性运动完全看不出来，火苗也只对极敏的人有违和感。
- **③ 明暗极性（最容易漏）**：CSS 场景的文字色是我们定的，实拍视频的明暗是素材定的，两者会打架。**篝火那支：CSS 场景是奶油底 + 深棕字，而视频是暗木柴 + 亮火焰** → 深色数字直接糊进木柴里，控件也看不清。解法两条一起上：
  1. `.fs-halo`：有视频时在「数字区 + 上下控件区」各叠一层雾，颜色取各场景的 `--fs-halo`（与文字色**反极性**，所以亮/暗主题、常亮/常暗场景都对）；
  2. 对极性严重不一致的场景，**在视频态整体翻转配色**：`#focus-stage.has-video[data-scene="ember"]{--fs-ink:#f8ece1; ...}`。
- **顺带**：720p 足够（背景常年压着 26%~46% 的雾面），CRF 31 时四支才 2.65MB，提到 27 是 4.53MB —— **体积这么便宜就别省画质**。
- **不预缓存**：视频没写进 `sw.js` / `build.js` 的 shell 清单（安装包不该为此变重），首次进沉浸时由 SW 的 fetch 处理器顺手缓存，之后离线可用。

### #049 "参考项目里的动态画面"要先确认是本地文件还是远程资源
- **现象**：用户希望把参考项目的动态画面搬过来，查完发现那个项目 `public/` 里**一个视频都没有**，`scenes.ts` 的四段 `videoUrl` 全指向第三方 CloudFront 桶，README 只标了 CC0 音频、没标视频来源。
- **解法**：没有可复制的本地文件，只能选来源策略（外链 / 下载 / 另找 CC0）。本次选了"另找免署名素材自带"：4 支 Mixkit Free-License 短片转码后入库，台账写清来源页 + 授权元数据 + 处理链路。
- **教训**：看到"参考项目的效果好"时，**先确认它靠什么实现**（视频？WebGL？远程资源？本地文件？）再决定跟不跟。它那边真正可搬的是**方法论**（细线环、逐字符定宽、时间戳计时、键位隐藏 UI、环境音与计时状态绑定），画面本身是**素材问题**而不是代码问题。

### #050 视频背景 v2：「不真实且不高清」的三个来源与解法
- **现象**：v1 的四支视频（720p、720p+波折循环）被用户评价"不真实且不高清"。
- **拆解**（三个原因各占一分）：
  1. **分辨率**：720p 源在 1440×900 桌面被放大 2 倍、在 2K/4K 屏更糟。→ 改用 **1080p**（`{id}-1080.mp4` 原片直链，CRF 24~26 / `-tune film`）。
  2. **倒带感**：波折循环（正放+倒放拼接）在雾、火、水这类有方向性的运动上会**明显倒放**，人眼对"火苗倒着烧、雾往回飘"极其敏感——这是"不真实"的最大来源。→ 改用 **`xfade` 正向无缝循环**：把成片的结尾 F 秒与开头 F 秒交叉溶解（`[main][head]xfade=offset=T-F`），循环点两侧画面连续且无倒放。
  3. **选材**：v1 的"深海"其实是**游泳池底的灯光**（一眼假），"篝火"是壁炉木柴特写（像照片），"晨雾"是快速流动的 CGI 感白雾。→ 重选为日出林海雾 / 水下鱼群 / 野外篝火 / 极光，标准是：实拍自然、构图没有与中央数字打架的主体、暗部占比高、动作慢。
- **顺带的坑**：
  - **夜空素材的 ISO 噪点会吃掉码率**：极光那支 CRF 23 编 5 秒要 10.6MB（全是噪点），`hqdn3d=4:4:8:8` 强时域降噪后 4.15MB 且画面更干净——固定机位夜景，时域降噪零副作用。
  - **带雾面的视频会显灰**：`.fs-veil` 全强度盖在 1080p 上会把细节洗掉 → 视频态把 veil 降到 68%，可读性交给 `.fs-halo`；再给视频加 `saturate(1.06) contrast(1.03)` 的轻微级调色抵消发灰。
  - 换素材时**每支都要重新核授权档位**（素材页元数据 `copyrightNotice:"Free"` + `license:#videoFree`，且无 `Restricted` 字样），不同素材可能落在不同档。
- **教训**：用户说"不真实"时，先按「分辨率 / 运动方向是否倒放 / 素材内容本身像不像真的」拆开定位——三条腿，缺一条都会被读成"假"。

### #051 Mixkit 的「好素材」大多是 Restricted 档：AccessDenied 不是限流
- **现象**：为沉浸层换篝火/星野素材，在 Mixkit 篝火分类里挑中的高质量夜景素材（47818 树林营火），下载 1080 原片时 CDN 返回 **111 字节的 XML**：`<Error><Code>AccessDenied</Code><Message>Access Denied</Message></Error>`；抓素材页源码才发现其元数据是 `"copyrightNotice":"Mixkit Restricted License"`（免费版**仅限个人使用**、商业用途需订阅）。
- **原因**：Mixkit 把优质实拍素材划入 Restricted 档引流 Envato 订阅——分类页同样展示、同样有「免费下载」按钮，但只给 720p；**Restricted 素材的 1080 原片直链一律被 CDN 拒绝**。此前的调素材过程里曾把这种拒绝误判成「限流」，又等了几分钟重试——其实等多久都没用。
- **区分方法**（看响应形态，不用猜）：**连接被重置**（curl 35 / 返回 000）＝ IP 突发限流，等几分钟自动恢复；**AccessDenied XML**（有响应体）＝ 授权拒绝，换素材或换档位才有用。另外：Free 素材页 HTML 也只列 720/360 直链，但 `-1080.mp4` 实测可下（28339 / 4148 均成功）——「页面没列」≠「不存在」，以带 Referer 实测为准。
- **解决**：定稿为 Free 档的 2707（室内壁炉）与 4148（银河+山脊）；把「先抓素材页核实 `copyrightNotice`、再下载 1080」写进 `vendor/video/README.md` 的选材流程，并登记已核实不可用的 Restricted 素材清单（47818/47311/31352/31466/21855/30253）。
- **后续注意**：**#050 里已经写过「换素材时要核授权档位」，这次仍然踩中**——因为核查动作放在了「下载之后」而不是「挑选之前」。凡引用第三方素材站的素材，把授权核查做成选材的**第一步**：拿到候选先批量抓素材页、正则提取 `copyrightNotice`（30 行脚本一次核 8 支）。

### #052 Mixkit 的 CDN 有两套直链格式：老库 `videos/{id}/` vs 新库 `active_storage/`
- **现象**：给沉浸层加「室内组」四个场景时，两支素材页元数据明明写着 `copyrightNotice:"Free"`、按 #051 的流程核实通过，但照惯例拼 `assets.mixkit.co/videos/99839/99839-720.mp4` 下载，拿回来的是 **111 字节 XML `AccessDenied`** —— 与 Restricted 素材的表现**一模一样**，差点误判成「这两支其实是 Restricted 档」而白白换素材。
- **原因**：Mixkit 换过素材库。2024 年后上传的素材（如 99839、100252）走 Rails ActiveStorage，直链是
  `https://assets.mixkit.co/active_storage/video_items/{id}/{timestamp}/{id}-video-1080.mp4`；
  老素材才是 `https://assets.mixkit.co/videos/{id}/{id}-1080.mp4`。拿老格式去拼新 ID，S3 层直接拒绝。
- **区分办法**（别猜，抓页面）：素材页 HTML 里就写着真实路径 —— 正则 `https?:\/\/[^"']*\.mp4` 一抓即知：路径里有 `active_storage` 就是新库，同时也能看清 1080 是 `-1080.mp4` 还是 `-video-1080.mp4`。
- **教训**：`AccessDenied` 有**三种**成因 —— Restricted 档、路径格式不对、对象根本不存在。前两种都长得像「素材被下架」，所以下载失败时**先回素材页对一眼直链**，比反复重试或直接换素材都快（十行脚本批量提取 `.mp4`，一次核 8 支）。

---

## 待补充
（开发过程中持续追加）
