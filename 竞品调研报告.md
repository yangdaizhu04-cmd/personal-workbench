# 个人工作台 · 晨雾奶油 Cream Reverie — 竞品调研报告

> 调研日期：2026-09-16 ｜ 调研对象：GitHub 单文件/纯前端个人起始页·仪表盘 + AI 原生本地优先产品
> 对标项目成熟度：本项目已迭代至 v15，23 模块全量完成，本地版零错误回归通过

---

## 一、调研对象总览

本次调研筛选了 **8 个**与"本地优先、个人效率聚合、单页 HTML 工具"形态重叠度高的优秀项目，覆盖自托管仪表盘、浏览器起始页、本地优先知识管理、AI 日记四类：

| # | 项目 | 定位 | GitHub Star | 仓库/官网 |
|---|------|------|------------|-----------|
| 1 | **Glance** (glanceapp/glance) | 自托管信息聚合仪表盘，Go 单二进制 | ~34,800 | [github.com/glanceapp/glance](https://github.com/glanceapp/glance) |
| 2 | **SiYuan 思源笔记** (siyuan-note/siyuan) | 隐私优先本地知识管理，块级引用+AI | ~46,300 | [github.com/siyuan-note/siyuan](https://github.com/siyuan-note/siyuan) |
| 3 | **Homepage** (gethomepage/homepage) | 自托管应用导航仪表盘，YAML 配置 | ~32,400 | [github.com/gethomepage/homepage](https://github.com/gethomepage/homepage) |
| 4 | **Dashy** (Lissy93/dashy) | 高度可定制个人仪表盘，PWA+UI 编辑器 | ~24,500 | [github.com/Lissy93/dashy](https://github.com/Lissy93/dashy) |
| 5 | **Bento** (migueravila/Bento) | 极简优雅可 Hack 的起始页 | ~2,100 | [github.com/migueravila/Bento](https://github.com/migueravila/Bento) |
| 6 | **Bonjourr** (victrme/Bonjourr) | iOS 风极简新标签页，无框架 TypeScript | ~1,700 | [github.com/victrme/Bonjourr](https://github.com/victrme/Bonjourr) |
| 7 | **Howdz/Dashboard** (leon-kfd/Dashboard) | 可拖拽组件化浏览器起始页，Vue3 | ~1,100 | [github.com/leon-kfd/Dashboard](https://github.com/leon-kfd/Dashboard) |
| 8 | **Reflect Diary** (reflectdiary.app) | AI 加密日记，端到端加密+本地优先 | 商业产品 | [reflectdiary.app](https://reflectdiary.app) |

> Star 数为 2026 年 9 月实时搜索核实值（来源：gstars.dev / alternativeto.net / hellogithub.com / mygit.top 等第三方追踪站），随时间浮动。

---

## 二、八维度对比总表

| 维度 | 本项目（晨雾奶油） | Glance | SiYuan | Homepage | Dashy | Bento | Bonjourr | Howdz | Reflect Diary |
|------|-------------------|--------|--------|----------|-------|-------|----------|-------|---------------|
| **① 产品定位** | 个人效率+生活工作台，23模块 | 信息聚合仪表盘（RSS/Reddit/天气） | 块级知识管理+AI | 自托管应用导航仪表盘 | 全能自托管个人仪表盘 | 极简起始页 | iOS风新标签页 | 组件化起始页 | AI加密日记 |
| **② UX/交互仪式感** | 晨间/收工双仪式、GSAP庆祝体系、小花园 | 纯信息展示，无仪式感 | 块级引用图谱、AI对话 | 纯配置驱动，无交互仪式 | UI可视化编辑器、状态检查 | 极简克制 | 极简洁钟+壁纸+Pomodoro | 拖拽编辑模式 | 时间线日记、AI周报 |
| **③ 技术架构** | 纯HTML/CSS/JS零框架，23模块一文件 | Go单二进制<20MB | TS前端+Go后端+Electron | Next.js(React) | Vue.js+PWA | 纯JS | TypeScript无框架 | Vue3+TS+Vite | 原生App+云端 |
| **④ 数据隐私备份** | localStorage+IDB，每日快照×7，JSON导出 | SQLite内嵌，YAML配置 | 本地SQLite+WebDAV/S3同步，E2E | YAML文件，Git版本控制 | localStorage+JSON导入导出 | localStorage | localStorage+GitHub Gist同步 | localStorage+云端同步 | AES-256-GCM端到端加密 |
| **⑤ 扩展生态** | BYOK AI Key，23模块固定 | 无插件，YAML自定义widget | 社区市场bazaar+插件+API | 100+服务widget，YAML配置 | 40+语言、主题包、图标包 | 仅改config.js | 自定义CSS注入 | 组件物料库 | 无 |
| **⑥ 设计视觉** | 晨雾奶油+夜雾双主题，霞鹜文楷，GSAP动效 | 极简卡片，暗色为主 | 块级编辑，图谱视图 | 暗色卡片，Tailwind | 多主题、多色、图标包 | 多套主题(Nord/OneDark) | iOS设计语言，高质量壁纸 | 多套清爽主题 | 极简日记风 |
| **⑦ 性能/体积** | 单文件1.5MB，文件夹版按需加载 | 二进制<20MB，秒开 | 桌面App较重(122MB APK) | Next.js静态站，构建后快 | Vue构建后~MB级 | 极轻，秒开 | 浏览器扩展，秒开 | Vite构建，轻量 | 原生App性能 |
| **⑧ 部署分发** | 文件夹/单文件/CloudBase PWA/Tauri物料 | Docker单容器 | Docker/Electron/Android/iOS | Docker一键部署 | Docker/PWA | GitHub Pages | Chrome/Firefox/Edge/Safari扩展 | 浏览器扩展+网页 | App Store |

---

## 三、分维度深度分析

### 维度①：产品定位与功能

**本项目已做好的：**
- 功能密度远超所有对标项目——23 个模块覆盖待办/日历/习惯/日志/记账/书影剧/健康/技能/番茄钟/统计等，是对标项目中功能最全的
- "效率+生活"双线融合（工作模块 + 生活记录 + 轻娱乐），对标项目大多只做单线（Glance 只做信息聚合，Homepage 只做导航，SiYuan 只做笔记）
- 晨间/收工双仪式是独特差异化卖点，对标项目中无类似设计

**真正有差距可借鉴的：**

1. **SiYuan 的"块级"数据思维**：SiYuan 把每一段话、每一张图、每一个列表项都当作独立 Block（有唯一 ID），可以在任何文档中精确引用某个块，修改源块所有引用处同步更新。本项目的日志/笔记目前是整篇存储，缺乏块级引用能力——如果未来想做"日志中提到的待办自动关联""笔记中的引用跳转"，块级 ID 是基础设施。
   - 借鉴做法：在 `journal` 和 `notes` 的 Markdown 解析中，给每个段落/列表项自动生成唯一 ID，支持 `[[]]` 双向链接语法（成本中等，但长期价值高）。

2. **Reflect Diary 的"AI 主动反思"模式**：Reflect 不是"你问它答"，而是 AI 每周自动生成一份周报反思——识别你写作中的主题变化、焦虑/平静模式、跨月趋势。本项目的 AI 助手目前是被动问答（"我这周怎么样"），可以增加"每周日自动生成 AI 复盘"能力，把现有周报自动总结行升级为 AI 深度版。

3. **Howdz 的组件物料库思路**：Howdz 把所有功能做成独立组件（时钟/天气/搜索/TodoList/热搜），用户像搭积木一样从物料库拖拽添加到自己的起始页。本项目 23 个模块是固定路由，但"今日总览"的 14 类卡片可以借鉴这个思路——把每个卡片做成独立组件，用户可以从物料库自由增删，而不仅仅是拖拽排序和显隐。

---

### 维度②：UX/交互/仪式感

**本项目已做好的：**
- 仪式感设计是所有对标项目中最强的：晨间仪式（昨日回顾→三大件→开工）、收工仪式（逐项顺延→日志→晚安）、打勾星光迸发、每日首次打卡彩带、三大件全完成隆重庆祝、小花园专注生长
- GSAP 入场雾开动效 + 静音动效开关，兼顾美感与无障碍
- ⌘K 万能命令面板（捕捉/跳转/翻译/搜索四合一），对标项目中 Bonjourr 也有命令面板但功能单一

**真正有差距可借鉴的：**

1. **Dashy 的"可视化 UI 编辑器"**：Dashy 提供了一个内置的可视化编辑模式，用户不需要编辑 YAML 文件，直接在页面上点击就能添加/删除/重排应用链接、修改主题。本项目的设置页目前是表单式的，"今日总览卡片拖拽排序"已经做了，但如果能把整个工作台变成"编辑模式"——进入编辑模式后所有模块都可拖拽重排、增删、调大小——体验会有质的提升。
   - 优先级：中。本项目已有拖拽排序基础，扩展到全模块编辑模式是自然演进。

2. **Bonjourr 的"极简洁钟+壁纸"美学**：Bonjourr 的核心设计哲学是"最少的元素，最美的画面"——一个大时钟、一张高质量壁纸、少量链接，没有任何多余信息。本项目今日总览信息密度较高（14 类卡片），可以增加一个"极简模式"开关——一键隐藏所有卡片，只留时钟+天气+今日三大件，给需要专注/冥想的时刻使用。
   - 优先级：中低。作为"防分心设计"的延伸，与项目已有的"专注中内容卡淡出"理念一致。

3. **SiYuan 的"块级缩放"交互**：点击任意块可放大查看、编辑该块，类似"局部放大"的沉浸式编辑体验。本项目的日志/笔记编辑器目前是整块编辑，可以考虑双击某段文字进入"专注编辑模式"——只显示当前段落，背景虚化其他内容。

---

### 维度③：技术架构与工程化

**本项目已做好的：**
- 零框架零构建依赖的纯 HTML/CSS/JS，双击即开——这是对标项目中最独特的优势（Glance 需要 Go 二进制，Homepage 需要 Node.js 构建，Dashy 需要 Vue 构建，SiYuan 需要 Electron）
- 23 个模块一模块一文件 + `WB.registerModule` 注册机制，架构清晰
- `build.js` 构建单文件版（1.5MB），esbuild 压缩 + 字体 base64 内联
- `tools/test-smoke.mjs` 无头浏览器回归冒烟（110 路由零错误），工程化意识远超同类个人项目
- 字体子集化（4500 常用字 → 2.2MB woff2），精细的体积控制

**真正有差距可借鉴的：**

1. **Glance 的"按数据源独立刷新策略"**：Glance 为每个 widget 配置独立的刷新间隔——高频数据（股票）5 分钟刷新一次，静态数据（书签）24 小时缓存，天气按 Open-Meteo 默认频率。这种差异化缓存策略大幅减少无效请求和电量消耗。本项目目前所有联网内容（天气/汇率/热点/每日一句）的缓存策略较统一，可以借鉴 Glance 的做法：
   - 天气/AQI：1 小时缓存（Open-Meteo 支持免费但不宜频繁请求）
   - 汇率：12 小时缓存（已有，保持）
   - 热点：每天缓存（已有，保持）
   - 每日一句：每天缓存
   - 宠物图：按需请求，不缓存
   - 优先级：低。本项目已有缓存层（08-network.js），细化策略成本低收益小。

2. **SiYuan 的"SQL 查询嵌入"**：SiYuan 允许在笔记中直接嵌入 SQL 查询语句，实时查询自己的知识库数据。例如写 `<!-- SQL: SELECT * FROM blocks WHERE content LIKE '%番茄%' -->` 就会在笔记位置渲染查询结果。本项目的统计看板目前是固定 ECharts 图表，可以增加一个"自定义查询"能力——让用户在日志中写一行特殊语法，自动渲染出"本月待办完成率""最近一周专注时长"等动态数据块。
   - 优先级：中高。这是把"数据层"和"写作层"打通的杀手级功能，且本项目已有完整数据架构，实现成本可控。

3. **Dashy 的"状态检查"机制**：Dashy 会定期 ping 你配置的每个自托管服务，在图标上显示绿/黄/红状态灯。本项目的"常用链接"模块目前只是静态链接列表，可以增加一个"链接健康检查"——定期 HEAD 请求所有收藏链接，失效的标灰提示。
   - 优先级：低。与本项目"链接二维码"模块的定位有重叠但场景不同。

---

### 维度④：数据隐私备份安全

**本项目已做好的：**
- localStorage + IndexedDB 分层存储，大文件（封面/音乐/涂鸦）走 IDB
- 每日自动快照存 IndexedDB 保留 7 份，一键恢复
- 回收站 30 天 TTL，可恢复可彻底删除
- JSON 全量导出 / Markdown 日志导出 / CSV 账单导出
- BYOK 密钥只存本地，不上传服务器
- 云端版访问密码服务端校验（HMAC）

**真正有差距可借鉴的：**

1. **Reflect Diary 的 AES-256-GCM 端到端加密**：Reflect 的每个日记条目在发送到服务器之前就已在设备端加密，加密密钥存在设备安全硬件中，服务器甚至开发者都无法读取内容。本项目云端同步（CloudBase）目前是明文 JSON 传输和存储——虽然有访问密码，但如果数据库被攻破，所有数据可读。借鉴做法：
   - 在云端同步前增加一层客户端加密：用户设置一个"同步加密密钥"，所有 push 到云端的数据用 AES-GCM 加密，pull 时本地解密
   - 密钥只存本机 localStorage，不传输到服务器
   - 优先级：高。这是数据安全的根本性提升，且实现成本可控（Web Crypto API 原生支持 AES-GCM）

2. **SiYuan 的 WebDAV/S3 同步选项**：SiYuan 除了官方同步服务，还支持用户自己的 WebDAV 服务器、S3 兼容存储做数据同步。本项目目前只支持腾讯 CloudBase 一种云端方案。借鉴做法：
   - 增加 WebDAV 同步选项（坚果云/Nextcloud/群晖等都支持 WebDAV），用户填自己的 WebDAV URL + 账号密码即可同步
   - 这比 CloudBase 部署简单得多——不需要云函数，直接 REST API 读写
   - 优先级：高。WebDAV 是个人用户最易获取的同步方案，大幅降低多端同步门槛

3. **Bonjourr 的 GitHub Gist 同步**：Bonjourr 提供了一个极巧妙的同步方案——用 GitHub Gist 存储配置数据，用户填自己的 GitHub Token，所有配置自动同步到 Gist，再从 Gist 拉到其他设备。不需要自己搭服务器，利用 GitHub 免费基础设施。本项目可以借鉴：
   - 作为 CloudBase 的轻量替代方案：用户填 GitHub Token + Gist ID，数据自动同步
   - 零服务器成本，部署门槛极低
   - 优先级：中。适合不想折腾 CloudBase 部署的用户

---

### 维度⑤：扩展生态/集成

**本项目已做好的：**
- BYOK 支持（GLM/DeepSeek/OpenAI 兼容），用户自带 API Key
- 内置 8 个联网 API（天气/汇率/热点/翻译/宠物/壁纸/每日一句/B站）
- 23 个模块功能丰富，但模块间联动已有基础（番茄绑待办/技能、日志自动摘要、统计看板聚合全量数据）

**真正有差距可借鉴的：**

1. **Homepage 的 100+ 服务 widget 生态**：Homepage 支持 100+ 自托管服务的实时数据 widget（Plex 播放量、qBittorrent 下载状态、Pi-hole 拦截数等）。本项目目前的联网 API 都是通用公共 API，缺乏对个人自托管服务的集成。不过考虑到本项目定位是"个人效率生活工作台"而非"homelab 仪表盘"，这个方向的优先级不高。
   - 可借鉴的思路：在"常用链接"模块中增加"服务状态"能力——用户填入自己的自托管服务 URL，工作台定期 ping 并显示状态灯。

2. **SiYuan 的社区市场（bazaar）**：SiYuan 有一个官方社区市场，用户可以下载主题、插件、模板。本项目目前是"作者自己做所有模块"的封闭模式。如果未来开源后想吸引社区贡献，可以借鉴：
   - 定义模块开发规范（WB.registerModule 已有），让社区开发者可以贡献第三方模块
   - 做一个模块市场页面，展示社区贡献的模块
   - 优先级：低。当前阶段（v15）作者自己迭代效率更高，开源社区运营是后续话题。

3. **Bonjourr 的自定义 CSS 注入**：Bonjourr 在设置中提供了一个"自定义 CSS"文本框，高级用户可以写 CSS 覆盖任何样式。本项目已有"自定义强调色"覆盖 CSS 变量，但没有提供任意 CSS 注入能力。
   - 优先级：低。小众需求，但实现成本极低（一个 textarea + 一个 `<style>` 标签）。

---

### 维度⑥：设计视觉

**本项目已做好的：**
- "晨雾奶油"设计哲学非常成熟：米白奶油底 + 雾蓝淡紫低饱和点缀 + 大圆角 + 柔和大弥散阴影
- 双主题（晨雾奶油/夜雾深色）+ 三套点缀色 + 自定义强调色
- 内置霞鹜文楷字体子集（4500 常用字），中文排版独特气质
- 手绘风 SVG 线性图标库（~70 枚，描边 1.8 统一）
- ThreeUI 视觉扩展（动态壁纸着色器/番茄钟液态珍珠/窗景页/3D 书架/锦鲤引言卡）——这是所有对标项目中视觉最丰富的
- GSAP 动效体系完整且有静音开关

**真正有差距可借鉴的：**

1. **Bento 的"主题即配置"哲学**：Bento 提供 Nord、One Dark、Dark、White 等多套预设主题，用户只需改 config.js 一行 `theme: 'nord'` 就换整套配色。本项目已有三套点缀色（mist/peach/mint），但"主题切换"目前只切换点缀色，不改整体氛围。可以考虑增加"主题包"概念——不仅改强调色，还改背景材质、卡片圆角、阴影强度、字体风格。
   - 优先级：中。用户已有自定义强调色能力，主题包是锦上添花。

2. **Glance 的"移动端优先"设计**：Glance 在移动端表现极佳——单列布局、卡片紧凑、触摸友好。本项目已有移动端手势（左右滑切模块/下拉刷新/底栏导航），但"今日总览"的 14 类卡片在小屏幕上可能信息过载。可以借鉴 Glance 的移动端单列紧凑布局策略。

3. **Bonjourr 的高质量壁纸系统**：Bonjourr 内置了大量高质量摄影壁纸（来自 Unsplash/Pexels），每天自动切换，壁纸色自动提取作为 UI 主题色。本项目已有 Bing 壁纸（默认关）和 Unsplash BYOK，可以增加"壁纸色自动提取"——从当前壁纸提取主色，自动应用为强调色。
   - 优先级：中。Canvas API 提取主色技术成熟，且与"晨雾奶油"的柔和气质契合。

---

### 维度⑦：性能/体积/加载

**本项目已做好的：**
- 单文件版 1.5MB（含字体/图标/全部代码），在同类工具中已算优秀
- 字体子集化（4500 字 → 2.2MB woff2），精细体积控制
- 窗景页 iframe 懒加载、关闭即清 src 释放 GPU
- ThreeUI islands 按需加载（3D 书架点击才注入）
- DPR≤1.5、IntersectionObserver + document.hidden 暂停渲染
- reduced-motion 回退 + 静音动效开关

**真正有差距可借鉴的：**

1. **Glance 的极致轻量理念**：Glance 整个二进制 <20MB，其中大部分是 Go 运行时。它的前端是纯服务端渲染的 HTML，没有任何 JS 框架，页面加载即渲染。本项目的 1.5MB 单文件版已经不错，但可以进一步优化：
   - ECharts 5.5.1（~1MB minified）是体积大头——统计看板只用了柱状图/折线图/饼图/热力图 4 种，可以按需引入 ECharts 的精简包（echarts/core + 只注册需要的图表类型），预计可减少 60-70% 的 ECharts 体积
   - GSAP 3.12.5（~50KB gzip）相对较小，可保留
   - 优先级：中。对文件夹版无影响，主要影响单文件版和云端版首屏加载

2. **Bonjourr 的"零框架"性能**：Bonjourr 用 TypeScript 写但不使用任何框架，纯 DOM 操作，浏览器扩展启动速度极快。本项目也是零框架，架构方向正确。

3. **Dashy 的 PWA 预缓存策略**：Dashy 用 Service Worker 做了精细的预缓存策略——核心壳预缓存，动态内容按需缓存。本项目的 sw.js 已有 cache-first 静态壳，但可以细化：
   - 把 ECharts/GSAP 等大库放在运行时缓存（首次打开后离线可用）
   - 把窗景页等大资源标记为 lazy cache
   - 优先级：低。当前 PWA 仅云端版生效，且物料待部署。

---

### 维度⑧：部署分发/多端适配

**本项目已做好的：**
- 三种形态：文件夹版（双击即开）、单文件版（build.js 生成 1.5MB HTML）、云端版（CloudBase PWA）
- Tauri 2 桌面打包物料就绪（托盘常驻/全局快捷键/开机自启）
- GitHub Pages 在线版已部署
- 移动端适配：手势切模块/下拉刷新/底栏导航/触屏拖拽
- PWA 可安装（云端版）

**真正有差距可借鉴的：**

1. **Bonjourr 的"浏览器扩展"分发模式**：Bonjourr 作为 Chrome/Firefox/Edge/Safari 扩展发布，用户一键安装，打开新标签页就是工作台。这种分发方式比"双击 HTML 文件"和"部署 PWA"都更顺滑——用户不需要记住文件位置，不需要配置 HTTPS，安装即用。
   - 借鉴做法：把本项目打包成浏览器扩展（Chrome Extension Manifest V3），用户从 Chrome Web Store 安装后，新标签页就是工作台
   - 技术可行性：本项目纯 HTML/CSS/JS，打包成扩展只需要加一个 `manifest.json` + `background.js`，业务代码零改动
   - 优先级：高。这是最低成本、最高收益的分发渠道扩展——尤其适合"每天打开浏览器就看到工作台"的使用场景

2. **SiYuan 的全端覆盖**：SiYuan 同时发布 Windows/Mac/Linux 桌面版（Electron）、Android APK、iOS App、Docker 服务端、Chrome 剪藏扩展。本项目目前主要是桌面浏览器 + 移动端 PWA。可以借鉴：
   - 至少把 Tauri 打包完成（物料已就绪，缺 Rust 环境），产出 .exe 安装包
   - 优先级：高。Tauri 物料已备好，只差打包环境

3. **Howdz 的"扩展+网页双模式"**：Howdz 同时发布浏览器扩展和独立网页版，用户可以选择安装扩展（新标签页替换）或直接访问网页。本项目的 GitHub Pages 在线版已有，但可以把"在线版"作为 PWA 安装到手机主屏，实现"打开手机就看到工作台"。

---

## 四、按优先级排序的优化建议清单

### 🔴 P0 — 高优先级（低成本高收益，建议优先做）

| # | 建议 | 具体做法 | 为什么 | 对标来源 |
|---|------|----------|--------|----------|
| 1 | **打包浏览器扩展（Chrome/Firefox）** | 加 `manifest.json`（Manifest V3）+ `newtab` 覆盖页，把 `index.html` 作为新标签页。业务代码零改动，只需扩展入口配置 | "每天打开浏览器就看到工作台"是最自然的使用场景；比双击 HTML 文件、比 PWA 安装都更顺滑；Bonjourr/Howdz 都走这条路且验证成功 | Bonjourr, Howdz |
| 2 | **云端同步增加客户端加密（AES-GCM）** | 用户设一个"同步加密密钥"，push 前用 Web Crypto API AES-GCM 加密所有数据，pull 后本地解密。密钥只存本机 localStorage | 当前 CloudBase 同步是明文 JSON，数据库被攻破则数据全泄；加密后即使服务器数据泄露也无法读取内容；Reflect Diary 的核心卖点就是 E2E 加密 | Reflect Diary |
| 3 | **增加 WebDAV 同步选项** | 设置页增加"WebDAV 同步"入口，用户填 WebDAV URL + 账号密码，直接 REST API 读写 JSON 文件。不需要云函数、不需要 CloudBase 部署 | WebDAV 是个人用户最易获取的同步方案（坚果云/Nextcloud/群晖都支持），大幅降低多端同步门槛；比 CloudBase 部署简单 10 倍 | SiYuan |
| 4 | **完成 Tauri 打包产出 .exe** | 安装 Rust 工具链 + VS Build Tools，`cd desktop && npx tauri build` | 物料 100% 就绪，只差执行；桌面 App 的体验（托盘常驻/全局快捷键/开机自启）是浏览器版无法替代的 | SiYuan(Electron), Dashy |

### 🟡 P1 — 中优先级（有价值但成本略高或非核心）

| # | 建议 | 具体做法 | 为什么 | 对标来源 |
|---|------|----------|--------|----------|
| 5 | **AI 助手增加"自动周报反思"** | 每周日自动触发 AI，基于本周日志/待办/心情/专注数据生成深度复盘文字（不是现在的统计行，而是 AI 写的一段话） | 把现有"周报自动生成"从"数据汇总"升级为"AI 洞察"；Reflect Diary 的核心体验就是 AI 主动反思而非被动问答 | Reflect Diary |
| 6 | **今日总览卡片升级为"组件物料库"** | 把 14 类卡片做成独立组件，设置页增加"添加卡片"入口，从物料库自由选择添加（如：增加"汇率速览""今日一书""播客推荐"等已有但未在总览显示的卡片） | 目前卡片只能拖拽排序和显隐，不能新增；Howdz 的核心卖点就是"从物料库自由搭积木"；本项目已有大量未充分展示的内容模块 | Howdz |
| 7 | **日志/笔记增加块级引用能力** | Markdown 解析时给每个段落/列表项自动生成 ID，支持 `[[]]` 双向链接语法；日志中引用的待办/笔记可点击跳转 | 打通"写作"和"数据"的壁垒；SiYuan 的核心竞争力就是块级引用；长期看这是笔记系统的基础设施 | SiYuan |
| 8 | **壁纸色自动提取作为强调色** | Canvas API 从当前 Bing/自定义壁纸提取主色调，自动应用为 UI 强调色（用户可关闭） | 增强壁纸与 UI 的融合感；Bonjourr 的经典做法；与"晨雾奶油"柔和气质契合 | Bonjourr |
| 9 | **ECharts 按需引入精简包** | 用 `echarts/core` + 只注册用到的图表类型（BarChart/LineChart/PieChart/HeatMapChart/GridComponent/TooltipComponent），替换全量 echarts.min.js | 单文件版体积可减少 ~600KB；Glance 的极致轻量理念；对首屏加载和统计页渲染速度都有帮助 | Glance |

### 🟢 P2 — 低优先级（锦上添花，有资源再做）

| # | 建议 | 具体做法 | 为什么 | 对标来源 |
|---|------|----------|--------|----------|
| 10 | **增加"极简模式"开关** | 一键隐藏今日总览所有卡片，只留时钟+天气+今日三大件 | 与已有"专注中内容卡淡出"理念一致；Bonjourr 的极简美学借鉴 | Bonjourr |
| 11 | **常用链接增加健康检查** | 定期 HEAD 请求收藏链接，失效标灰提示 | Dashy 的 status-checking 功能；但使用频率不高 | Dashy |
| 12 | **自定义 CSS 注入入口** | 设置页增加一个 textarea，用户可写任意 CSS 覆盖样式 | 实现成本极低；Bonjourr 的经典能力；小众需求但满足高级用户 | Bonjourr |
| 13 | **今日总览卡片支持"任意 CSS 布局网格"** | 允许用户自定义卡片大小（1x1/1x2/2x1），而非统一尺寸 | Howdz 的拖拽改大小能力；当前只有排序和显隐 | Howdz |
| 14 | **GitHub Gist 同步选项** | 用户填 GitHub Token + Gist ID，数据自动同步到 Gist | CloudBase 的轻量替代方案；零服务器成本；Bonjourr 验证过此模式 | Bonjourr |

---

## 五、本项目独特优势（对标项目做不到的）

在分析差距的同时，必须指出本项目在多个维度上已经**超越**了所有对标项目：

1. **功能密度最高**：23 个模块覆盖效率+生活+娱乐，对标项目最多做 5-8 个模块且功能单一
2. **仪式感设计最完整**：晨间/收工双仪式 + GSAP 庆祝体系 + 小花园生长 + 锦鲤翻卡，对标项目无任何类似设计
3. **视觉精致度最高**：晨雾奶油设计哲学 + 霞鹜文楷字体 + ThreeUI 3D 扩展（动态壁纸/窗景页/3D 书架/液态珍珠），对标项目大多是"工具感"而非"美感"
4. **零框架零构建的极简架构**：双击 index.html 即开，不需要 Node.js 构建、不需要 Docker、不需要 Go 二进制——所有对标项目都需要某种构建步骤或运行时
5. **断网优雅降级设计**：8 个联网 API 全部有缓存+内置数据兜底，断网绝不报错——这在对标项目中极为罕见（Glance 断网直接空白，Homepage 依赖 Docker 运行时）
6. **完整数据护城河**：每日快照×7 + 回收站 30 天 + JSON/MD/CSV 三种导出——对标项目大多只有 JSON 导出或没有快照
7. **工程化意识最强**：29 条踩坑记录 + 无头浏览器冒烟回归（110 路由零错误）+ 构建脚本 + 字体子集化脚本——这在个人项目中极为罕见

---

## 六、总结

本项目 v15 的成熟度已经非常高，**核心功能和设计不需要大改**。真正值得投入的优化方向集中在：

1. **分发渠道**（P0）：浏览器扩展 + Tauri 打包 —— 让更多人更容易用上
2. **数据安全**（P0）：客户端加密 + WebDAV 同步 —— 让数据真正属于用户
3. **AI 深度**（P1）：自动周报反思 —— 把 AI 从"被动问答"升级为"主动洞察"
4. **可定制性**（P1）：卡片物料库 + 块级引用 —— 从"固定 23 模块"走向"可生长的工作台"

这些改进不需要推翻现有架构，都是在 v15 基础上的增量演进。
