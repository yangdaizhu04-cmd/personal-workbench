fix(desktop): 便携版提醒改走自绘提醒卡（系统通知静默失效）

用户实测：通知自检按了两次，没看到通知。追下去是三层叠在一起：

1. 注册表 NOC_GLOBAL_SETTING_TOASTS_ENABLED = 0 —— 系统通知总开关本来是关的，
   任何应用都弹不出来，而应用侧完全无法感知
2. 就算打开，便携版也弹不出来：tauri-plugin-notification 的 Windows 分支只在
   「不是从 target\debug|release 跑」时才设置 app_id，便携时 notify-rust 退回
   Toast::POWERSHELL_APP_ID，而本机 Win11(26200) 的开始菜单已经没有
   「Windows PowerShell」快捷方式（只剩 ISE）→ 通知被系统静默丢弃，show() 仍返回 Ok
3. 看到的「两个图标」是幽灵图标：之前测试用 Stop-Process -Force 强杀进程，
   Windows 不回收托盘图标（当时只有一个进程在跑）

做法：
- 便携版不再赌系统通知，弹工作台自己的提醒卡：desktop/toast.html + WebviewWindow
  （360×138 无边框置顶，右下角，9 秒自动收，点一下唤出主窗口，连点两条复用同一窗口）
  它不经过系统通知，不受系统开关与 AUMID 影响，样子也是自家的奶油风
- installed()（开始菜单有 个人工作台.lnk）才走系统通知（能进通知中心、尊重专注助手），
  发不出去再回落提醒卡；只是把 exe 拷到别处也判为「没装」，走提醒卡
- test_notify 会说明用的是哪条通道；fired.log 末列记通道（ok | card / ok | system）
- 顺手补 tauri-plugin-single-instance：开机自启 + 手动双击同时发生会起两个托盘
  两条提醒线程（同一条提醒弹两次）
- build.js --tauri 增加拷贝 desktop/toast.html 到 dist；capabilities 的 windows 加 toast

验证：收进托盘后 fired.log 出现 offwork:2026-09-24 | ok | card；
轮询到提醒卡窗口出现的瞬间只截了右下角一小块，确认卡片真的渲染出来（🌙 该收工了）。
冒烟 双形态 × 三主题 × 28 路由 0 错误 + 沉浸层 155/155。exe 59.59 → 59.72 MB
