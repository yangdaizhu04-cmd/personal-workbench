feat(desktop): 关掉窗口也按点提醒（保留项 C1）

原痛点：桌面版点 × 直接销毁窗口，WebView 一停所有 JS 提醒全静默；
而数据只在本机 localStorage 里，Rust 读不到。

一、数据过河
- 前端把**算好的绝对时间戳清单**推给 Rust（set_reminders），Rust 不做任何日期推算
- 清单四条：待办到点（含提前量）· 倒数日与生日 · 晨间 · 收工
- 每条带 at 与 until（有效期）：待办 at+30min（与网页版窗口一致）、晨间 +4h、
  收工 +5h、倒数日与生日当天 23:59 —— 没有有效期会在半夜弹出早上的晨间提醒，
  而"一小时内"的旧规则会让晚上打开的人永远收不到当天生日

二、谁发（写死，避免一条提醒响两次）
- 窗口可见且未最小化 → 前端（main.js 待办 + WB.desktop.inWindow 倒数日/晨间/收工）
- 隐藏 / 最小化 / 已关窗 → Rust 后台线程（每 20 秒扫一次）
- 番茄结束始终归前端（1 秒心跳在，不需要跨进程接力）
- 顺带补齐：倒数日/生日原来完全没有通知，现在窗口开着也会说

三、关窗语义与托盘
- CloseRequested → prevent_close + hide：点 × 收进托盘（不退出），
  真正的退出移到托盘右键菜单；同时补「显示主窗口」（原来托盘没菜单，窗口一关唤不回来）
  与「切换开机自启」

四、配套
- 设置页新增「桌面版」分区（只在 Tauri 里出现）：后台提醒开关 · 晨间提醒时刻 ·
  开机自启 · 通知自检（让 Rust 直接发一条，确认这台机器真弹得出来）· 关窗说明
- 诊断两件套（关窗后没有界面可看）：reminders.json 最后收到的清单（不存在=前端没推）、
  fired.log 每条通知一行 ok/err（分开"没触发"与"系统拒了"）
- 前端推送连挂 3 次在界面上说一次，把静默失败变成看得见的失败
- 新增 tauri-plugin-notification + capabilities/default.json，开 withGlobalTauri
  （前端不装 npm 包，直接 window.__TAURI__.core.invoke）

验证（真机跑出来的）：
- 清单落盘 15 条、until>at 15/15
- CloseMainWindow 之后进程仍活着（收进托盘）
- **隐藏窗口后 fired.log 出现 offwork:2026-09-24 | ok** —— 后台线程触发且系统收下了
- 网页侧零影响：探针 12 项（available()=false / sync 不发 IPC / 设置页不显示桌面分区 /
  inWindow 跳过 todo:*）；冒烟 双形态 × 三主题 × 28 路由 0 错误；沉浸层 155/155
- exe 58.91 → 59.59 MB

未验的两点已写进文档：通知的视觉呈现（未安装程序有时不弹 toast，用自检按钮确认）、
专注助手等系统设置会静音通知
