feat(snapshots): 恢复前先兜底 + 差异预览 + 选择性恢复（保留项 C2）

起因是翻出一个漏口：WebDAV 拉取 / JSON 导入 / 导入配置 覆盖前都会先拍快照，
唯独「恢复快照」这个最危险的动作不拍 —— 点错了就永远回不去，
而列表里只有日期，几个日期长得一样，等于闭着眼睛覆盖。

① 恢复自己也要兜底
- restore() 在任何写入之前先 take("恢复前")：恢复变成可以反悔的

② 恢复前看得见
- 新增 diff(key)：按模块给出「现在 → 这份快照」（待办 121 → 128、账本 498 → 512…），
  只列会变的模块；条数没变但内容改了标「内容有改动」
- 差异按「组」聚合（groupOf = 冒号前的名字），中文名放 GROUP_CN，没收录的显示原名

③ 从整机还原变成捞东西
- restore(key, picks) 只恢复勾选的模块：快照里有就写回、现在有而快照里没有就删掉、
  没勾的一律不碰；不给 picks 时保持原来的全量行为（这条退路不删）

顺手修掉两个隐患：
- 手动快照不再顶掉当天早上那份自动快照（snapshot:日期-HHMMSS 并存，保留份数 7 → 10）
- 同一分钟内两次「恢复前」不再互相覆盖（精确到秒 + 撞了加序号）
  —— 那张恰恰是用来反悔的，被踩掉等于安全网自己踩自己

界面上把"快照里没有这块"说成「会被清空 / 回到默认」而不是"会少掉 1 条"，
并写明快照不含 IndexedDB 的大文件（涂鸦 / 上传的音乐 / 影视封面）。

验证：临时探针 18 项（自动与手动并存 · 差异数值与中文名 · 没变的模块不进清单 ·
选择性恢复只动勾的块 · 从「恢复前」整份退回去成功 · 老的全量恢复行为不变 ·
同一分钟不互相覆盖 · 渲染出来的时刻是 HH:MM · 零错误）；冒烟 双形态 × 三主题 × 28 路由 0 错误；
沉浸层 155/155；截图复核两张。单文件 1724 → 1732 KB（+8 KB）
