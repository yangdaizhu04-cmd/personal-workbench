{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "个人工作台桌面版：核心窗口能力 + 系统通知 + 开机自启开关（前端只调这三类插件命令）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
