/* 00-env.js —— 云端环境配置（部署脚本会自动改写 cloudBaseUrl；本地双击版留空即可） */
window.WB_ENV = {
  /* 云函数 HTTP 访问根地址，形如：
     https://<envId>.service.tcloudbase.com
     三个云函数路径分别为 /gate /sync /proxy（在云控制台开启"HTTP 访问服务"后生效） */
  cloudBaseUrl: "",
};
