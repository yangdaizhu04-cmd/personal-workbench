# 待优化清单 · 个人工作台

> 撰写日期：2026-09-18
> 范围：**动效 / 视觉 / 交互手感**（不含业务功能与数据层）
> 来源：源码勘察（`css/main.css`、`js/*`、`islands/*`）＋ 对外部动效范式（Lumora Hero 营销页）的对照借鉴
> 说明：文中行号是撰写时的快照；改动后请以「选择器 / 函数名」锚点为准
> 用法：按 P0 → P3 推进，每条含「现状 → 位置 → 改法 → 验收」，完成后勾选复选框

---

## 0. 速览

| 优先级 | 条目 | 改动面 | 收益 | 完成 |
|---|---|---|---|---|
| **P0** | [2.1 冷却闸门](#21-冷却闸门-交互互斥) | `06-ui.js` + 3 处调用 | 修 bug 级（连点打架） | [x] |
| **P0** | [2.2 图标交叉变形](#22-图标交叉变形) | 1 函数 + 1 段 CSS | 立即可见 | [x] |
| **P0** | [2.6 补 `skShine` 定义](#26-补-skshine-定义骨架屏微光) | 1 段 CSS | 修「引用了没定义」 | [x] |
| **P1** | [2.3 交叉淡入换场](#23-交叉淡入换场) | islands + 2 处 CSS | 观感提升最大 | [x] |
| **P1** | [2.4 stagger 错峰入场](#24-stagger-错峰入场) | `06-ui.js` + CSS | 质感提升 | [x] |
| **P1** | [3.1 渐变层交叉过渡](#31-渐变层无法插值) | `main.css` | 主题切换不再硬跳 | [x] |
| **P2** | [1.1–1.4 设计令牌补齐](#1-设计令牌补齐前置改造) | `main.css` :root | 后续改动的地基 | [x] |
| **P2** | [2.5 呼吸微动效](#25-呼吸微动效-breathe) | CSS + 4 处挂载 | 贴合「温柔」气质 | [x] |
| **P2** | [2.7 退场动画](#27-退场动画缺失) | `06-ui.js` + CSS | 关闭不再生硬 | [x] |
| **P2** | [4.1 层级 token + 顶栏越过抽屉](#41-层级规范--z--token) | `main.css` | 可维护性 / 修遮挡 | [x] |
| **P3** | [2.8 拖拽重排 FLIP](#28-拖拽重排无位移动画) | `06-ui.js` | 手感 | [x] |
| **P3** | [2.9 手势跟手](#29-手势不跟手) | `15-gestures.js` | 手感 | [x] |
| **P3** | [4.2 `prefers-reduced-motion` 直连](#42-prefers-reduced-motion-未直连) | 全局 | 无障碍 | [x] |

---

## 1. 设计令牌补齐（前置改造）

现状：`:root` 只有 `--radius/--dur/--sidebar-w` 与颜色 token（`css/main.css:9-15`）。缺口集中在**缓动、时长、模糊、层级**四类，导致同类效果在各处手写近似值。

### 1.1 缓动 `--ease-*`

**现状**：全站没有缓动变量。同一「回弹感」手写了 5 个近似 bezier：

| 位置 | 值 |
|---|---|
| `css/main.css:296` `.modal` popIn | `cubic-bezier(.34,1.4,.5,1)` |
| `css/main.css:326` `.cmdk` | `cubic-bezier(.34,1.35,.5,1)` |
| `css/main.css:313` `.toast` | `cubic-bezier(.34,1.3,.5,1)` |
| `css/main.css:359` `.sheet` | `cubic-bezier(.3,1.1,.4,1)` |
| `css/main.css:265` `.ring-fg` | `cubic-bezier(.22,.9,.34,1)` |

**改法**：在 `css/main.css` 的 `:root` 增补，并把上表逐个替换为变量：

```css
:root{
  --ease-std:cubic-bezier(.4,0,.2,1);      /* 标准档：状态切换 / 错峰入场 */
  --ease-pop:cubic-bezier(.34,1.35,.5,1);  /* 弹层回弹档：取代 5 个近似值 */
  --ease-soft:cubic-bezier(.3,1.1,.4,1);   /* 柔和上浮档：sheet / koi 翻卡 */
  --ease-flow:cubic-bezier(.22,.9,.34,1);  /* 签名档：进度条 / 进度环 */
  --ease-cross:cubic-bezier(.4,0,.2,1);    /* 交叉淡入档：见 2.3 */
}
```

**验收**：`grep -rn "cubic-bezier" css/ js/` 结果只剩 `:root` 中的定义（vendor 除外）。

### 1.2 时长 `--dur-*`

**现状**：只有单一 `--dur:.28s`（`main.css:13`），其余时长散落：`0.15/0.18/0.38/0.5/0.8/1/2.5s`。

**改法**：补成三档，形成「微反馈 < 状态切换 < 场景换场」的梯度：

```css
:root{
  --dur-tap:.18s;    /* hover / 按压 */
  --dur:.28s;        /* 状态切换（保持原值，兼容现有 21 处引用） */
  --dur-in:.38s;     /* 入场 */
  --dur-scene:.7s;   /* 场景级：主题 / 壁纸 / 大层交叉 */
}
```

**注意**：`--dur-scene` 上限 **700ms**。营销页用 1000ms 是氛围，工具页超过 700ms 会显得迟钝。

### 1.3 模糊 `--blur-*`

**现状**：6 处 `backdrop-filter`，模糊值硬编码 6/8/10/14px（`main.css:123/135/289/322/410`、`pomodoro.js:123`），且**没有玻璃基类**，无法复用。

**改法**：

```css
:root{ --blur-s:6px; --blur-m:10px; --blur-l:14px; }
```

顺带抽一个玻璃基类（这次借外部范式的核心之一——**渐变描边**，用 `mask-composite` 挖空实现 1.4px 细边，比 `border` 更通透）：

```css
.glass{
  position:relative; overflow:hidden;
  background:var(--glass-bg);                       /* 主题里定义，如 rgba(255,252,244,.5) */
  backdrop-filter:blur(var(--blur-m)); -webkit-backdrop-filter:blur(var(--blur-m));
  box-shadow:inset 0 1px 1px rgba(255,255,255,.1);
}
.glass::before{
  content:""; position:absolute; inset:0; border-radius:inherit; padding:1.4px;
  background:linear-gradient(180deg,
    rgba(255,255,255,.45) 0%, rgba(255,255,255,.15) 20%,
    rgba(255,255,255,0) 40%, rgba(255,255,255,0) 60%,
    rgba(255,255,255,.15) 80%, rgba(255,255,255,.45) 100%);
  -webkit-mask:linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite:xor;
  mask:linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask-composite:exclude;
  pointer-events:none;
}
```

**适用点**：`#sidebar`、`.modal`、`.cmdk`、`.toast`、`#scene-viewer`。
**不适用**：`.card`（内容卡片需要不透明底保证可读性，保持现状）。
**注意**：暗色主题下描边需另给一套低透明度值，避免发白。

### 1.4 层级 token

见 [4.1](#41-层级规范--z--token)。

---

## 2. 动效缺口

### 2.1 冷却闸门（交互互斥）

**现状**：所有动画都是「点一下就播」，没有互斥。可复现的问题：

- `D` 键 / 月亮按钮（`js/07-theme.js:111-119`）在 `.5s` 过渡中连按 → 渐变层撕裂
- `js/modules/scenes.js:36-39 / 54-57` 开窗 `.55s` 与关窗 `.32s` 重叠打架
- 路由快速切换（`js/09-router.js:65-68`）叠出多层 `.view-inner`

外部范式（Lumora）用 `isTransitioning` + 与 CSS 时长严格对齐的定时器解决，值得照搬。

**改法**：`js/06-ui.js` 新增通用闸门（挂在 `WB.ui` 上导出）：

```js
/* 冷却闸门：ms 必须 ≥ 对应动画时长，否则动画会叠加 */
const locks = new Map();
function lock(key, ms){
  if(locks.get(key)) return false;                 // 冷却中 → 忽略本次操作
  locks.set(key, setTimeout(() => locks.delete(key), ms));
  return true;
}
```

调用点：

```js
/* 07-theme.js toggleTheme */
function toggleTheme(){
  if(!WB.ui.lock("theme", 550)) return;            // 与 body .5s 过渡对齐
  ...
}
/* scenes.js 开窗 */
if(!WB.ui.lock("scene", 600)) return;
/* 09-router.js render */
if(!WB.ui.lock("route", 400)) return;
```

**验收**：狂按 `D` 键 5 次，主题只切换 1 次且过渡完整；窗景开/关不叠帧。

### 2.2 图标交叉变形

**现状**：图标是 `innerHTML` 硬换，无过渡：

- `js/07-theme.js:115-117` 太阳 ↔ 月亮
- `index.html:50` `#btn-menu`（移动端抽屉开合时图标不变）
- `index.html:73` `#scene-close`

外部范式：两个图标绝对叠放，`opacity` + `rotate(±90deg)` + `scale(.75)` 交叉，300ms。

**改法**：`css/main.css` 加：

```css
.icon-swap{position:relative; display:inline-grid; place-items:center;}
.icon-swap>*{grid-area:1/1;
  transition:opacity var(--dur) var(--ease-std), transform var(--dur) var(--ease-std);}
.icon-swap>*:last-child{opacity:0; transform:rotate(-90deg) scale(.75);}
.icon-swap.is-b>*:first-child{opacity:0; transform:rotate(90deg) scale(.75);}
.icon-swap.is-b>*:last-child{opacity:1; transform:none;}
```

`js/06-ui.js` 加工具函数并导出到 `WB.ui`：

```js
/* 交叉变形：切换按钮内的两个图标（A ↔ B） */
function swapIcon(btn, iconA, iconB, showB){
  if(!btn.dataset.swapReady){
    btn.innerHTML = '<span>' + icon(iconA) + '</span><span>' + icon(iconB) + '</span>';
    btn.classList.add("icon-swap");
    btn.dataset.swapReady = "1";
  }
  btn.classList.toggle("is-b", !!showB);
}
```

**验收**：切换深浅色时太阳/月亮旋转交叉，无跳变；移动端抽屉开合时汉堡 ↔ 叉号交叉。

### 2.3 交叉淡入换场

**现状**：涉及「层被整体替换」的三处都是瞬时的：

| 位置 | 现象 |
|---|---|
| `islands/core.jsx:11-46` `bgSync()` | `data-theme` 变化 → `bgRoot.render()` 整体换组件，无过渡（暗色还是 iframe，换源即白屏） |
| `js/modules/scenes.js:50` | 关窗直接 `iframe.src=""`，白闪后消失 |
| `css/main.css:133-137` | `#sidebar` 亮/暗两套渐变靠选择器瞬间换色 |

外部范式：多个全屏层叠放，只切 `opacity`，1000ms 交叉。

**改法**：宿主内做双槽位（A/B），新层先以 `opacity:0` 挂载，下一帧加 `.is-on`，旧层延迟卸载：

```css
#bg-shader .bg-slot{position:absolute; inset:0; opacity:0;
  transition:opacity var(--dur-scene) var(--ease-cross);}
#bg-shader .bg-slot.is-on{opacity:1;}
```

```js
/* islands/core.jsx（改后需 tools/build-islands.mjs 重新构建） */
let switching = false;
function applyBg(key){
  if(switching) return;                       // 复用 2.1 的闸门思路
  switching = true;
  const host = document.getElementById("bg-shader");
  const old = host.querySelector(".bg-slot.is-on");
  const box = document.createElement("div");
  box.className = "bg-slot";
  host.appendChild(box);
  mount(box, key);                                     // 新层 opacity:0
  requestAnimationFrame(() => box.classList.add("is-on"));
  if(old){ old.classList.remove("is-on"); setTimeout(() => unmount(old), 700); }
  setTimeout(() => switching = false, 700);
}
```

`#sidebar` 同理：亮色铺 `::before`、暗色铺 `::after`，切主题只交叉两者的 `opacity`（渐变无法插值，只能靠叠层）。

**验收**：切换深色时壁纸是「淡出淡入」而不是闪断；关窗有 `.32s` 淡出。

### 2.4 stagger 错峰入场

**现状**：全项目**唯一**的 stagger 在首页卡片（`js/main.js:14`，`.055` 步进）。`.modal` / `.cmdk` / `.sheet` 内容都是整块弹出。

外部范式：`100 + index*50 ms`，`--ease-std`，500ms。

**改法**：

```css
.stagger-item{opacity:0; transform:translateY(12px);
  transition:opacity var(--dur-in) var(--ease-std) var(--d,0ms),
             transform var(--dur-in) var(--ease-std) var(--d,0ms);}
.stagger-item.is-in{opacity:1; transform:none;}
```

```js
/* 06-ui.js：modal 打开后（root.appendChild(scrim) 之后）统一触发 */
function staggerIn(scope, step = 50, base = 100){
  [...scope.querySelectorAll(".stagger-item")].forEach((n, i) => {
    n.style.setProperty("--d", (base + i * step) + "ms");
    n.classList.add("is-in");
  });
}
```

挂载顺序：
1. `.modal-body` 内的表单分区（现在一次性出现）
2. `.cmdk-list` 结果项（`css/main.css:331-338`）
3. `.sheet` 分组（`css/main.css:362-363`）
4. 路由切页后的列表项（现在是整块 `.view-inner` 一个动画 + 外层卡片 stagger）

用 CSS 变量 `--d` 传延迟，避免内联 style 绕过 `no-motion` 熔断（见 [4.3](#43-抄外部效果时的约束)）。

**验收**：打开「设置」弹窗，各分区依次浮现；命令面板结果逐条滑入。

### 2.5 呼吸微动效 `.breathe`

**现状**：全站唯一循环动画是 `blobFloat`（`css/main.css:114-118`，46s，几乎察觉不到），宠物也复用同一个（`js/modules/fun.js:206`）。**没有短周期「呼吸/漂浮」档**。

外部范式的关键细节：位移只有 6px、3s、`ease-in-out`，且**配恒定 `scale(1.03)`**——位移时不会露出边缘。

**改法**：

```css
@keyframes breathe{
  0%,100%{transform:translateY(0) scale(1.03);}
  50%{transform:translateY(-6px) scale(1.03);}
}
.breathe{animation:breathe 3s ease-in-out infinite;}
html.no-motion .breathe{animation:none !important;}
```

**挂载点**（都比较自然）：

| 挂载点 | 位置 | 说明 |
|---|---|---|
| 侧栏 logo `.brand-mark` | `css/main.css:139` / `index.html:38` | 最轻的一点「活着」的感觉 |
| 番茄球待机 | `css/main.css:393-397` | 仅 `data-state="idle"` 时；运行中不要加，会和 shader 抢注意力 |
| 空状态插画 | `.empty svg`（`css/main.css:280`） | 比静止插画友好 |
| 下拉提示条 `#pull-tip` | `js/15-gestures.js:46` | 提示「可以松手了」 |
| Bing 壁纸 | `body.wallpaper-on` | 恒定 `scale(1.03)` 正好用来做缓慢 Ken Burns 而不露边 |

**约束**：必须是 CSS class，不要内联 style —— 这样 `html.no-motion` 的全局熔断（`css/main.css:380-382`）自动接管。

### 2.6 补 `skShine` 定义（骨架屏微光）

**现状**：`js/06-ui.js:234` 引用 `animation:"skShine 1.4s infinite"`，但**全项目没有任何地方定义 `@keyframes skShine`** → 骨架屏微光一直没播，只剩静态渐变。

**改法**：它就是一段「渐变 + 横向扫过」，用同一套渐变思路补上：

```css
@keyframes skShine{ from{background-position:200% 0;} to{background-position:-200% 0;} }
html.no-motion .card[style*="skShine"]{animation:none !important;}
```

**验收**：断网触发骨架屏（如天气卡片），能看到微光横向扫过。

### 2.7 退场动画缺失

**现状**：所有浮层只有进场，关闭是瞬时的：

| 浮层 | 进场 | 退场 |
|---|---|---|
| `.modal` | `popIn .3s`（`main.css:296`） | `js/06-ui.js:29-33` `scrim.remove()` 瞬时 |
| `.cmdk` | `popIn .28s`（`main.css:326`） | `js/modules/commands.js:180-185` `hidden=true` 瞬时 |
| `.sheet` | `sheetUp .32s`（`main.css:359`） | `js/09-router.js:139-143` 瞬时 |
| `.toast` | ✅ `toastIn` | ✅ `toastOut`（唯一有退场的） |

**改法**：照搬 `.toast.leaving` 的成熟套路（加 class → 延时移除），时长与进场对齐但缩短约 30%：

```css
.modal.leaving{animation:popOut .18s var(--ease-std) both;}
.cmdk.leaving{animation:popOut .18s var(--ease-std) both;}
.sheet.leaving{animation:sheetDown .2s var(--ease-std) both;}
@keyframes popOut{to{opacity:0; transform:scale(.97) translateY(8px);}}
@keyframes sheetDown{to{opacity:0; transform:translateY(40%);}}
```

```js
function close(ok){
  if(motionOff()){ scrim.remove(); }
  else{
    scrim.classList.add("leaving");
    setTimeout(() => scrim.remove(), 200);
  }
  WB.bus.emit("modal:closed");
  if(onClose) onClose(ok);
}
```

**注意**：`modal:closed` 事件与 `main.js` 的弹窗栈必须在退场**开始时**就发射/出栈，不要等 `setTimeout`，否则 200ms 内快捷键仍被拦（参见踩坑 #028）。

### 2.8 拖拽重排无位移动画

**现状**：`js/06-ui.js:207-218` 在 `dragover` 里直接 `container.insertBefore()`，元素瞬间跳位；CSS 反馈只写在 `.list-row` 上（`css/main.css:273-274`），首页 15 张卡片连 `dragging` 高亮都没有。

**改法**（二选一）：

1. **轻量**：记录所有兄弟节点的 `getBoundingClientRect()`，`insertBefore` 后用一次 `requestAnimationFrame` + `transition:transform .25s var(--ease-std)` 做位移动画（手写 FLIP，约 25 行）
2. **省事**：引入 GSAP `Flip` 插件（`vendor/gsap.min.js` 是 core，需另取插件文件）——注意会略微增大体积

另外给 `.card` 补上和 `.list-row` 一致的拖拽反馈：

```css
.card.dragging{opacity:.4; transform:scale(.98);}
.card.drop-target{box-shadow:inset 0 3px 0 var(--accent);}
```

### 2.9 手势不跟手

**现状**：全部是「阈值判定式」，过程中没有 transform 跟随：

- 左右滑切模块（`js/15-gestures.js:75-79`）：`|dx|>72` 触发，中间无跟随
- 下拉刷新（`js/15-gestures.js:37-55`）：只改提示条 `opacity`，页面本身不位移
- 锦鲤卡翻牌（`js/modules/today.js:542-548`）：松手后靠 CSS 完成，拖动中无反馈

**改法**：给下拉刷新加跟手位移（阻尼 + 松手回弹），这是收益最大的一处：

```js
/* touchmove 中 */
const pull = Math.min(dy * 0.4, 88);                    // 0.4 阻尼 + 上限
view.style.transition = "none";
view.style.transform = "translateY(" + pull + "px)";
/* touchend 中 */
view.style.transition = "transform .3s var(--ease-soft)";
view.style.transform = "";
```

**注意**：跟手期间设 `transition:none`，松手才交还 CSS（与 `main.js:14` 用 `clearProps` 交还 CSS 的思路一致）；`no-motion` 时跳过跟手。

---

## 3. 主题 / 壁纸切换质量

### 3.1 渐变层无法插值

**现状**：`body` 的 `background .5s`（`css/main.css:65`）与 `.card` 的 `background .5s`（`:186`）能平滑过渡，但**所有渐变属性是硬跳**：

| 位置 | 元素 |
|---|---|
| `main.css:101-113` | `.blob-a/b/c` 的 `radial-gradient`（暗色分支） |
| `main.css:133/137` | `#sidebar` 的 `linear-gradient` |
| `main.css:206` | `.btn.primary` |
| `main.css:431-435` | `.koi-card` |

**改法**：渐变不能靠 `transition` 插值，只能叠层 + 交叉 `opacity`（与 2.3 同一手法）。优先级：`#sidebar` > `.blob` > `.koi-card` > `.btn.primary`。

### 3.2 阴影硬跳

**现状**：只有 `.card` 的 `box-shadow` 在过渡列表里；`.modal`（`:295`）、`.toast`（`:312`）、`.cmdk`（`:325`）、`.btn.primary`（`:208`）、`.koi-card`（`:437`）切主题瞬间换阴影。

**改法**：把这些元素的过渡列表补上 `box-shadow .5s ease`（`box-shadow` 是可插值属性，成本低）。

### 3.3 `meta theme-color` 不更新

**现状**：`index.html:9` 固定 `#f6f1e7`，深色模式下浏览器地址栏/状态栏仍是米白。

**改法**：`js/07-theme.js` 的 `apply()` 内同步：

```js
const meta = document.querySelector('meta[name="theme-color"]');
if(meta) meta.content = resolvedTheme() === "dark" ? "#232339" : "#f6f1e7";
```

### 3.4 首帧主题闪变

**现状**：`<html data-theme="light">` 静态写死（`index.html:2`），夜雾用户启动时会看到一次米白 → 深蓝的 `.5s` 变色（`#veil` 只遮住一部分）。

**改法**：在 `index.html` 的 `<head>` 里、样式表之前插一小段同步脚本读取 `localStorage` 并直接写 `data-theme`（避免 FOUC 的标准做法，注意要和处理设置存储的键名保持一致）。

### 3.5 壁纸下的对比度自适应

**现状**：`body.wallpaper-on`（Bing 每日壁纸 / 自定义图）只有一层固定蒙层（`main.css:90-98`），浅色文字遇到浅色壁纸会看不清。

**改法（借鉴外部范式的分区策略）**：

- 按壁纸主色亮度计算 `--veil` 透明度并 `700ms` 过渡
- **关键 UI 保持自身对比度不跟随场景**：`#sidebar`、`#topbar`、`#bottombar`、`.card` 永远走自己的不透明底

---

## 4. 工程 / 性能 / 无障碍

### 4.1 层级规范 + `--z-*` token

**现状**：11 个魔法数，无 token、无层级表：

```
0（背景：#bg-blobs / #bg-shader / #focus-orb）
1（#app 内容层）
90 #bottombar │ 99 #pull-tip（JS 内联，未登记）│ 110 #sheet-root
120 .modal-scrim │ 150 #cmdk-root + #scene-viewer（同级）│ 160 #toast-root
180 #fx-canvas │ 190 番茄全屏提示（JS 内联）│ 200 #veil
```

**已知问题**：`#sheet-root`（z=110）盖住 `#topbar`，移动端抽屉打开时顶栏被遮。
**外部范式**的做法是：内容层 → 遮罩层 → **导航层抬到遮罩之上**，遮罩用 `pointer-events:none` 保证穿透。

**改法**：

```css
:root{
  --z-bg:0; --z-content:1; --z-bar:90; --z-sheet:110; --z-scrim:120;
  --z-panel:150; --z-toast:160; --z-fx:180; --z-alert:190; --z-veil:200;
}
```

并把 `#topbar` 抬到 `--z-sheet` 之上（或在 sheet 内提供明确的关闭按钮）。
另外把 JS 内联的两个魔法数（`15-gestures.js:46`、`pomodoro.js:120`）也改成读变量，避免「CSS 里查不到的层级」。

### 4.2 `prefers-reduced-motion` 未直连

**现状**：项目只认手动开关 `html.no-motion`（`07-theme.js:67` + `main.css:380-382`），自身代码 **0 处**监听 `prefers-reduced-motion`（只有 vendor 场景页有）。

**改法**：`07-theme.js` 初始化时把系统偏好作为默认值来源（用户显式设置后以设置为准）：

```js
if(window.matchMedia("(prefers-reduced-motion: reduce)").matches && !store.hasUserSetMotion()){
  html.classList.add("no-motion");
}
```

### 4.3 抄外部效果时的约束

引入任何新动效时，必须满足：

- [x] 走 CSS class / keyframes，**不要内联 style**（否则绕过 `html.no-motion` 的 `!important` 熔断）
- [x] JS 驱动的部分加 `WB.ui.motionOff()` 守卫（`js/06-ui.js`）
- [x] 冷却时长与 CSS 动画时长**严格对齐**（2.1）
- [x] 循环动画元素加 `will-change:transform`（`.blob` / `.breathe` / `.wb-mini-spin` / `#focus-orb`）
- [x] 新增长时长动画（≥0.5s）不要出现在输入/滚动路径上

### 4.4 `will-change` 与合成层

**现状**：`will-change` 仅 1 处（`.blob`）。
**改法**：给持续动画元素（`.breathe`、`.wb-mini-spin`、`#focus-orb`）加 `will-change:transform`，但**不要滥用**（会吃显存）；动画结束后要能回收。

### 4.5 `backdrop-filter` 叠加成本

**现状**：`#veil`(14px) + `#sidebar`(10px) + `.modal-scrim`(6px) 可能同时存在；移动端是成本点。
**改法**：① 用 1.3 的 `--blur-*` 统一并适度降级窄屏（如 ≤860px 用 `--blur-s`）；② 弹窗打开时给 `#veil` 之外的重叠层加 `will-change:backdrop-filter` 反而更差，**正确做法是减少同时存在的层数**（例如 `.modal-scrim` 打开时不必再叠 `.cmdk-scrim`）。

---

## 5. 明确不做（避免走弯路）

| 项 | 原因 |
|---|---|
| 引入多路全屏视频做背景 | 与「双击即开 / 本地优先 / 离线可用」的定位冲突；氛围职责已由 `#bg-blobs` + 动态壁纸承担 |
| 场景换场用 1000ms 之类的长时长 | 营销页慢是氛围，工具页慢是迟钝；上限 700ms，交互微反馈保持 `--dur:.28s` |
| 用内联 style 驱动动画 | 会绕过 `no-motion` 熔断、破坏「CSS 管状态动效 / GSAP 管入场编排」的分工（`开发交接文档.md:70`） |
| 给 `.card` 加毛玻璃 | 内容卡片依赖不透明底保证可读性，毛玻璃只用于浮层 |
| 引入重型动效库 | 现有 GSAP core + CSS 已够用；仅在 2.8 需要 FLIP 时再单独评估插件体积 |

---

## 6. 自测清单

改动动效后逐项跑：

- [x] `node tools/test-smoke.mjs` 全部路由通过（见 `开发交接文档.md` §9）
- [x] 设置 → 关闭「动效」，全站无任何动画/过渡/微光，`#veil` 不出现
- [x] 深浅色切换：无硬跳（渐变、阴影、壁纸均已交叉过渡）
- [x] 连点 `D` 键 5 次：只切换 1 次（2.1 闸门生效）
- [x] 打开/关闭 modal、cmdk、sheet：进出场都有动画，且关闭后快捷键立即可用（不被弹窗栈拦）
- [x] 移动端 ≤860px：抽屉打开时顶栏仍可点击 / 有明确关闭入口
- [x] `node build.js` 单文件版构建通过，且单文件版（无 islands）不报错、动效正常降级
- [x] 低性能场景（DevTools 4x CPU 降速）：滚动不掉帧，`backdrop-filter` 层数受控

---

## 附：本次对照的「外部范式」提炼

| 手法 | 本质 | 本项目对应落点 |
|---|---|---|
| 多层叠放 + 只切 opacity 做交叉淡入 | 用叠层换「渐变/组件无法插值」的平滑 | 2.3 / 3.1 |
| 恒定 `scale(1.03)` 配位移 | 位移时永不露边 | 2.5（Ken Burns） |
| 与 CSS 时长严格对齐的冷却闸门 | 让交互节奏与动画时长一致，杜绝叠加 | 2.1 |
| `100 + i*50ms` 错峰入场 | 用延迟造层次，不增加任何动画成本 | 2.4 |
| 双图标交叉旋转（±90° + scale .75） | 图标切换也能有方向感 | 2.2 |
| 场景变色 + 关键 UI 保持恒定色 | 「谁跟随场景、谁不变」要显式声明 | 3.5 |
| 分层 + 导航抬到遮罩之上 | 叠层前提是层级规范 | 4.1 |

---

## 完成记录（2026-09-18）

12 项已全部落地，`css/main.css` / `js/06-ui.js` / `js/07-theme.js` / `js/09-router.js` / `js/15-gestures.js` / `js/main.js` / `js/modules/{scenes,commands,pomodoro,todos,mood,fun}.js` / `islands/core.jsx`（已重建 `vendor/threeui/islands/core.js`）/ `index.html`。

执行顺序调整为：**1.1–1.4 令牌前置 → P0 → P1 → P2 → P3**（2.3/3.1 依赖 `--ease-cross`、`--dur-scene`，不能等 P2 才补）。

### 与原方案有出入的地方

| 条目 | 出入 | 原因 |
|---|---|---|
| 2.1 路由闸门 | 只锁**入场动画**，不锁 `render()` 本身 | 锁在 render 入口会丢渲染 / 延迟内容（踩坑 #031） |
| 2.2 `#scene-close` | 换成了图标库的 `close`，没做交叉变形 | 它只有一个语义（关闭），没有 A↔B 可交叉 |
| 2.3 islands | 没用 `switching` 布尔闸门，改成「每槽独立 root + 多槽共存」 | 单闸门会丢掉冷却期内的换场请求，导致壁纸与主题不一致；多槽天然不丢请求 |
| 2.5 番茄球待机 | **未挂载** `.breathe` | `#focus-orb[data-state="idle"]` 的 `opacity` 是 0（只有 running 才显形），挂了也看不见 |
| 2.5 Bing Ken Burns | **未实现** | 壁纸是 `body` 的 `background-image`，没有独立元素可 `scale`；要做必须先加一层 `<div id="wallpaper">`，属结构改动，收益低于风险 |
| 2.5 宠物 | 由 `blobFloat`(46s) 换成 `.breathe`(3s) | 原实现是内联 `animation`，既几乎看不出动、又绕过 `no-motion` 熔断 |
| 2.6 骨架屏 | 内联 `animation` 收进 `.card.skeleton` | 内联会绕过 `html.no-motion` 的 `!important`（踩坑 #037） |
| 2.9 左右滑 | 仍是阈值判定式，没做横向跟手 | 横向跟手会和浏览器自身的横滚 / 边缘返回手势抢手势；下拉刷新那一处收益最大，已做 |
| 4.1 顶栏越过抽屉 | 走备选方案：**抽屉内加显式关闭入口** | `#app` 已是层叠上下文，`#topbar` 抬不过 `--z-sheet` 而不破坏弹窗层级（踩坑 #036） |
| 4.4 `#focus-orb` | `will-change` 常驻 | 该元素只在番茄页存在于 DOM 内，不存在跨页回收问题 |
| 4.5 | 用 `body.scrim-open` 摘掉侧栏 `backdrop-filter`（减层数） | 文档也指出「加 `will-change:backdrop-filter` 反而更差」 |

### 新增的第 5 档缓动

`--ease-spin`（长距离旋转档，用于「吃什么」转盘）。四档里确实没有等价表达，硬塞会走形（踩坑 #038）。

### 顺带修掉的 4 个 bug

1. **`$$` 未解构**（`06-ui.js`）→ 拖拽重排全程抛 `ReferenceError`，`onReorder` 从未生效（踩坑 #030）。
2. **命令面板防抖悬空**（`commands.js`）→ 输入后 120ms 内回车关闭会抛 TypeError（踩坑 #034）。
3. **遮罩类摘不掉**（`06-ui.js`）→ `body.scrim-open` 在退场期间同步判断，导致侧栏永久丢失 `backdrop-filter`（踩坑 #033）。
4. **主题图标不同步**（`07-theme.js`）→ 2.2 的 `swapIcon` 只挂在 `toggleTheme()` 上，设置页改主题 / `auto` 跟随系统时顶栏图标不更新；已收进 `theme.apply()` 这个唯一汇聚点（踩坑 #040）。
5. **`.icon-swap` 的 `!important` 压掉了 `.mobile-only`**（`main.css`）→ 为赢过 `inline-flex` 加的 `display:inline-grid !important` 顺带压掉了 `.mobile-only{display:none !important}`，汉堡按钮在桌面端冒出来；改用「绝对定位子元素叠放」从根上绕开 display 冲突（踩坑 #041）。

### 验收证据

- `node --check` 全量语法门禁通过；`grep -rn "cubic-bezier" css/ js/` 只剩 `:root` 里的 5 档定义。
- `node tools/test-smoke.mjs`：folder/single × light/dark 共 4 轮 × 28 路由，**0 console/page 错误**。
- 无头浏览器逐条行为验收：令牌 21/21 可解析；`D` 键连按 2 次只切 1 次且图标交叉；modal/cmdk/sheet 退场动画在跑、`WB.modalOpen()` 立刻转 false、清理后计数归零；`route` 冷却期内 `#view` 仍只有 1 个 `.view-inner` 且有内容；`prefers-reduced-motion: reduce` 下 `no-motion` 自动生效、显式开关后恢复动画；FLIP 反向位移非空且 120ms 后归零、`onReorder` 顺序正确；`#sidebar::before/::after` 交叉过渡 `transition-duration:0.7s`、`meta[theme-color]` 同步为 `#232339`。

---

# 第二轮 · 人性化与一致性（2026-09-23 起）

> 来源：一轮全项目体验审查（文档 + 代码 + 联网对照 NN/g《确认弹窗》、flomo 回顾体系、Things 3 手感、Bonjourr 克制）
> 范围：**零新增功能**，只修一致性、手感与数据安全兜底。动效/视觉层见上文（2026-09-18 那轮 12 项已全部落地）
> 交付口径：每批改完跑 `node tools/test-smoke.mjs`（双形态 × 双主题 × 28 路由）+ `node build.js` 体积核对 → 本地 commit → 更新本文档与 `开发踩坑.md` → 清理 `.tmp/` 与临时探针

## A. 批次与进度

| 批 | 主题 | 内容 | 状态 |
|---|---|---|---|
| 1 | 数据安全 | 导入「取消=覆盖」修复 / 撤销条（删除可撤销）/ 日志与笔记草稿保护 | [x] |
| 2 | 承诺一致 | ⌘K Shift+回车 / 空状态文案 / 待办提醒开关 / 晨间仪式 / 跨 4 点换日 | [ ] |
| 3 | 手感 | 滚动位置 / 待办拖拽真实排序 / 月份保持 / 总览顿挫 | [ ] |
| 4 | 键盘与可达 | 弹窗 Esc / 空格劫持 / focus trap / aria / 对比度 | [ ] |
| 5 | 舒适设施 A | 骨架屏 / 待办搜索批量 / 60 条提示 / 时刻解析 / 记住上次选择 | [ ] |
| 6 | 舒适设施 B | 移动端热区与软键盘 / ECharts 按需 / 习惯页性能 | [ ] |

## B. 批 1 · 数据安全（2026-09-23，已完成）

| 条目 | 现状 → 改法 | 位置 |
|---|---|---|
| 导入「取消」= 覆盖（丢数据） | 二选一确认框 → 三选一（取消 / 覆盖导入 / 合并导入）；取消与关闭一律不动数据；覆盖前自动拍一张「导入前」快照兜底 | `js/modules/settings.js` |
| 删除没有撤销出口 | 吐司支持操作按钮 → 全站删除后弹「已移入回收站 · 撤销」（6s，悬停暂停计时），点撤销按原 id 还原 | `js/06-ui.js`、`js/02-store.js` |
| 日常删除先弹确认 | 11 处日常删除去掉确认框，改由撤销条兜底；删完不再重复弹「已放入回收站」（统一由 `toTrash` 弹） | `todos` `notes` `ledger` `goals` `countdown` `wishes` `skills` `links` |
| 长表单关掉即丢 | 日志与笔记编辑器：关闭时静默存草稿，重开自动带出并提示「已恢复上次没写完的内容」，保存后自动清除 | `js/modules/journal.js`、`js/modules/notes.js` |

**保留确认框的删除**（有副作用，无法完整撤销）：清单删除（会解除任务关联）、习惯删除（打卡记录一并删）、书影剧（同时删 IDB 封面）、快照删除、清空回收站、彻底删除、放弃番茄计时。

**验收**：`node tools/test-smoke.mjs` folder/single × light/dark × 28 路由 **0 console/page 错误**；临时探针 12 项断言全过（删除→撤销→还原、导入取消不动数据、覆盖确实替换、草稿存取）；`node --check` 全量语法通过；单文件 1621 KB（改前 1623 KB）。
