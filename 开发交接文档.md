# 开发交接文档

> 个人工作台 · 架构说明 / 数据结构 / 设计哲学 / 构建与部署 / 维护手册
> 需求依据：`需求方案.md`（v15，开发的唯一依据）。
> 状态：**本地版全功能完成并经浏览器全量回归（23 模块 0 错误）**；云端物料齐备待部署（见 §6）。

## 0. 快速上手

- **日常启动**：双击 **`启动工作台.bat`** —— 自动用 node（或 python 兜底）在 `http://127.0.0.1:8765` 起本地静态服务（最小化窗口，重复双击会复用已有实例）并打开浏览器；**这样双开两个标签页才能共享数据并自动同步**（`file://` 直开时每个文件 localStorage 隔离，双开不同步；`tools/dev-server.js` 是配套的静态服务）。什么都没装时退回直接双击 `index.html`（单开使用不受影响）。
- **单文件版**：双击 `构建单文件.bat`（即 `node build.js`）生成 `个人工作台.html`。
- **GitHub**：https://github.com/yangdaizhu04-cmd/personal-workbench （main 分支）
- **在线版（GitHub Pages）**：https://yangdaizhu04-cmd.github.io/personal-workbench/ —— 文件夹形态直接上网页，可安装为 PWA（云同步/AI 等 CloudBase 功能仍需 §6 部署后才激活）
- **云端版（多端同步）**：`ENV_ID=你的环境ID bash deploy.sh`（需先 `tcb login`，详见 §6）。
- **桌面 App**：见 §7（✅ 已打包：58.9MB exe，2026-09-23，托盘常驻 + Ctrl+K 全局唤出）。
- **开发环境**：Windows + Git Bash + Node v24 + gh CLI；Python（miniconda）仅字体裁剪用。
- **改代码后发布到 Pages**：`git add -A && git commit -m "..." && git push`（Pages 自动重新部署，约 1 分钟）。

## 1. 目录结构

```
个人工作台/
├── index.html            入口（文件夹形态，双击即开）
├── css/main.css          设计系统 + 组件 + 模块样式（唯一样式文件）
├── js/
│   ├── 00-env.js         云端环境配置（部署脚本自动改写）
│   ├── 01-utils.js       工具：dom(el)/日期(bizDate)/防抖/复制/下载
│   ├── 02-store.js       数据层：localStorage + 事件总线 + 回收站(30天TTL)
│   ├── 03-idb.js         IndexedDB：封面图/音乐/涂鸦（含图片压缩 400px/JPEG）
│   ├── 04-data-builtin.js    内置数据：51语录/792单词/365今日一问
│   ├── 04b-data-content.js   内置内容：64书单/22播客/72家常菜食谱/20小确幸
│   ├── 05-icons.js       手绘风 SVG 线性图标库（~70 枚，描边 1.8 统一）
│   ├── 06-ui.js          通用 UI：弹窗/吐司/空状态/彩带星光/拖拽/进度环/合成铃声
│   ├── 07-theme.js       主题：晨雾奶油/夜雾 + 三套点缀色 + 自定义 + Bing壁纸/Unsplash
│   ├── 08-network.js     网络层：超时/缓存/JSONP，永不抛错（失败返 null）
│   ├── 09-router.js      hash 路由 + 侧栏/底栏/抽屉导航 + WB.registerModule
│   ├── 10-markdown.js    迷你 Markdown（先转义防注入）
│   ├── 11-calendar-util.js   节假日班休(2025-2026)/农历/节气(24+寄语)/月相/倒数日
│   ├── 12-daily.js       联网内容：每日一句/天气+AQI+IP定位/汇率/热点/治愈一刻
│   ├── 13-exports.js     导出：日志笔记 Markdown / 账单 CSV（带 BOM）
│   ├── 14-cloud.js       云端引擎：密码门/自动双向同步/快照/PWA 注册（本地版静默）
│   ├── 15-gestures.js    移动端手势：左右滑切模块/下拉刷新（沉浸专注中自动让位）
│   ├── 16-immersive.js   沉浸专注层：全屏场景+细线环+巨型数字+环境音联动（script 排在 modules 之后）
│   ├── 17-ambience.js    氛围引擎：按时段编排 场景/音景/主题（默认关闭，见 §8h）
│   ├── main.js           启动：入场雾开/快捷键/提醒调度/仪式触发/周报自动生成
│   └── modules/          23 个模块（一模块一文件，见 §4）
├── vendor/               本地开源库：echarts 5.5.1 / gsap 3.12.5 / solarlunar / qrcode-generator / font/霞鹜文楷子集
│   ├── video/            沉浸专注层的循环视频背景（8 支 1080p 无缝循环，共 25MB；来源与授权台账见 vendor/video/README.md）
│   └── threeui/          ThreeUI 移植层（MIT）：islands/{core,shelf}.js 构建产物 / pages/ 自包含场景页×4 / LICENSE
├── islands/              ThreeUI React island 源码（core.jsx/shelf.jsx/condensation/，需素材仓库才能重建）
├── tools/build-islands.mjs   islands 打包脚本（esbuild IIFE，仓库缺失时跳过用已提交产物）
├── tools/test-smoke.mjs      全站回归冒烟（双形态×双主题×28 路由，收 console 错误 + 截图）
├── tools/test-immersive.mjs  沉浸专注层专用回归探针（进出/计时/音景/快捷键/层级/降级）
├── make-font-subset.py   霞鹜文楷常用字子集生成器（4500 字 → 2.2MB woff2）
├── build.js              单文件构建（内联 CSS/JS/字体 base64）
├── deploy.sh             云端一键部署（改 env → 函数 → 构建 → 托管）
├── manifest.webmanifest / sw.js   PWA（仅云端版生效）
├── cloud/
│   ├── cloudbaserc.json  CloudBase Framework 配置
│   ├── functions/        gate(密码校验) / sync(同步+快照) / proxy(热搜/iTunes/AI)
│   └── hosting/          构建产物（build.js --cloud 生成）
├── desktop/              Tauri 2 桌面打包物料（tauri.conf.json + main.rs + README）
├── .tmp/                 字体裁剪原料（TTF 13MB + 字频表，可删，重跑 make-font-subset.py 需重新下载）
├── 个人工作台.html        单文件构建产物
├── 需求方案.md / 开发踩坑.md / 开发交接文档.md
```

## 2. 设计哲学：晨雾奶油 · Cream Reverie

全文见需求方案 §3/§4；开发向摘要：

- **材质**：米白奶油底 `--bg:#f6f1e7`，卡片 `#fdfaf3` 大圆角 20px，柔和大弥散阴影（无硬边）；点缀雾蓝 `#7fa3bd` + 淡紫 `#a294c9` 只出现在关键动作。
- **夜雾深色**：深蓝紫夜雾底 `#232339` + 微亮雾蓝淡紫；`data-theme="dark"`，快捷键 D，持久化。
- **暮霞粉**：低饱和暖粉底 `#f9efe9` + 卡片 `#fdf7f3` + 暖褐文字（`--ink-2 #75655f` / `--ink-3 #8d7c76`），点缀仍是雾蓝淡紫 —— 口径是"把色相往玫瑰偏"，不是粉色少女风；`data-theme="rose"`。三套主题共用同一份令牌骨架，只在 `css/main.css` 里各给一套颜色变量（**新增主题的四处落点见 §9**）。
- **三套点缀色**：`data-accent="mist|peach|mint"` + custom（自定义强调色覆盖 CSS 变量）。
- **字体**：`LXGW WenKai Subset`（霞鹜文楷 Lite v1.522 裁剪 4500 常用字，SIL OFL）→ 系统楷体 → 无衬线兜底。
- **动画**：GSAP 管入场雾开/卡片依次升起/庆祝；CSS 管背景光斑呼吸/hover 上浮/进度环描边；`静音动效` 一键关（html.no-motion）。
- **庆祝体系**：打勾星光迸发 + 每日首次打卡小彩带 + 三大件全完成/拼图完成隆重庆祝。

## 3. 数据架构

### 3.1 分层
- **localStorage**（前缀 `wb:`）：全部文字类数据；`wb:netcache:*` 为联网缓存；`wb:trash` 回收站（30 天 TTL 自动清理）。
- **IndexedDB**（库 `wb-files`）：`cover:*` 书影剧封面（400px/JPEG≈30-60KB）、`music:*` 本地音乐、`doodle:*` 涂鸦。
- 记录通用字段 `id/createdAt/updatedAt`；ID = `Date.now().toString(36)+随机`。

### 3.2 键位清单（与模块一一对应）
`todos, todoLists, habits, habitLogs, journals, moods, notes, ledger, ledgerCats, links, texts, goals, countdowns, wishes, media, health, healthDefs, skills, pomoLog, badges, weekReports, doodles, trash, settings, uiPrefs, byok, soundPrefs, musicList, soundLibs, dishOptions, bigThree:{日期}, morningDone:{日期}, offworkDone:{日期}, firedReminders:{日期}, pomodoro(运行态), wishTarget:{年}, lifeRefAge, sync:since, lastSnapRemindDay`（快照本体存 IndexedDB `wb-files` 库，key 前缀 `snapshot:日期`，保留 7 份）

### 3.3 关键约定
- **业务日**：凌晨 4 点前算昨天（`U.bizDate()`）。所有"相对今天"计算一律用 bizDate，禁止 todayStr（踩坑 #012）。
- **删除**：所有模块删除 → `WB.toTrash(module, record)` → 回收站可恢复/彻底删除/过期自动清理。
- **重复任务**：模板 + 当日实例（`repeatOf` 字段关联）；完成实例即生成下一期。
- **番茄计时**：时间戳制 `{accumMs, resumeTs}`，关页面/切走照走，重开自动恢复；离开期间到点自动结算。
- **云端同步冲突**：记录级 `updatedAt` 新者胜；本地回收站内的 id 视作墓碑，pull 时跳过。
- **模块导出**：`WB.registerModule` 只做路由注册，**不产生全局引用**；模块能力要跨文件调用，必须在文件尾部显式挂 `WB.<name> = {...}`（踩坑 #024）。跨文件跳转一律 `WB.router.go(id)`，禁止裸函数名（踩坑 #025）。
- **全局快捷键**：入口统一在 `main.js initShortcuts()`；弹窗打开时只留 Esc；快捷键触发的 UI 刷新必须容忍目标组件未挂载（踩坑 #026）。

## 4. 模块清单（23 个，路由 ↔ 文件）

| 路由 | 文件 | 要点 |
|---|---|---|
| #/today | today.js | 14 类卡片可拖拽排序/显隐；防分心（专注中/收工后内容卡淡出）；收工按钮；**晨雾絮语 + 宠物「小雾团」**（可命名 / 记得昨天的日子 / 第一人称说话，见 §8i）；**旧忆跨年对比** |
| #/todos | todos.js | 清单/子任务/重复/时刻+提前量提醒/四象限(拖拽改优先级)/过期提醒区/已完成折叠清理 |
| #/calendar | calendar.js | 月历聚合(待办/心情色/打卡环/日志角标/节假日节气) + 日视图时间轴(拖任务上时间块) |
| #/goals | goals.js | 月度目标 +10% 快捷、月初归档 |
| #/countdown | countdown.js | 阳历+农历生日、每年重复、下个假期卡 |
| #/wishes | wishes.js | 目标件数、点亮庆祝、年度报告 |
| #/habits | habits.js | 打勾型/次数型、连续天数(不可补卡)、累计、近两周热区 |
| #/journal | journal.js | 三栏+自由区、MD 预览、今日一问(365)、补写历史、自动摘要行 |
| #/mood | mood.js | 5 档表情 + 月历色块 |
| #/notes | notes.js | 卡片流、MD+标签、搜索、置顶、语音速记(Web Speech) |
| #/ledger | ledger.js | 支出/收入、自定义分类、总+分类预算 80%/100% 温和提醒、月汇总 |
| #/media | media.js | 书影剧课、封面三选一(上传压缩→IDB/URL/内置插画)、进度评分短评、月度看板 |
| #/health | health.js | 体重/睡眠默认 + 自定义指标、各指标独立 ECharts 趋势 |
| #/skills | skills.js | 技能卡、教程链接(B站 BV 自动识别→内嵌官方播放器)、里程碑、番茄联动时长 |
| #/pomodoro | pomodoro.js | 三预设+全自定义、到点需确认下一段、三件套提醒、绑定待办/技能、时间戳恢复、小花园(番茄=苗/4个=花/放弃=蔫)、沉浸入口 |
| (#/pomodoro 内) | 16-immersive.js | 沉浸专注层：全屏**八场景**（经典四景：晨雾/深海/篝火/星野 + 世界风光四景：雪山/湖泊/碧海/草甸，点「◐」弹网格选）+ 细线环 + 巨型数字 + 3-2-1 准备 + 4-7-8 呼吸 + 环境音联动 + 锁屏控制（F/Esc，见 §8d） |
| (#/pomodoro 内) | sound.js | 七源混合：雨声/海浪/篝火/钢琴/白噪/晨间氛围/Lo-Fi 真实录音(vendor/audio 本地文件 URL 加载, 无缝循环, 失败自动回落合成; 前四路 2026-09-22 换入 pixel-town 的 Pixabay 录音, 后三路 CC0, 台账见 vendor/audio/README.md) + 本地播放器(IDB, 顺序/随机/单曲) + 声音库(URL, 失效标灰)；歌单跳网易云/Spotify；空格播放暂停；专注恢复组合/收工静音 |
| #/links | links.js | 网址收藏、本地二维码、常用文本一键复制 |
| #/fun | fun.js | 吃什么转盘(带食谱+记日志)、小确幸、拼图、涂鸦板(IDB)、治愈一刻(宠物图/插画兜底) |
| #/podcasts | podcasts.js | 内置 22 档、每日一档、收藏、跳小宇宙/Apple |
| #/books | books.js | 四主题 64 本、今日一书、跳豆瓣/微信读书、一键收进书影剧馆 |
| #/stats | stats.js | ECharts×7(趋势/专注/心情/时间去向/月支出/技能分布/年热力图)、周报(自动+手写总结)、徽章墙×9、人生刻度 4000 周 |
| #/trash | trash.js | 30 天、恢复、彻底删除、一键清空 |
| #/settings | settings.js | 外观(深色/点缀/动效/壁纸/背景)、番茄与提醒、天气与内容源、数据管理(JSON/MD/CSV)、BYOK、云端备份、通知、快捷键、关于 |
| 全局 ⌘K | commands.js | 捕捉(日期词/笔记)、跳转、翻译(MyMemory)、全局搜索 |
| 全局 | rituals.js | 晨间仪式(昨日回顾→三大件→开工)、收工仪式(逐项顺延→日志→晚安)、三大件管理 |
| #/assistant | assistant.js | 云端 AI：三按钮/对话式数据问答/笔记标签建议；本地版显示引导 |

## 5. 联网能力（全部 API 已实测 200 + CORS ✅）

| 能力 | 端点 | 断网表现 |
|---|---|---|
| 每日一句 | v1.hitokoto.cn / v1.jinrishici.com | 缓存→内置双语语录，永不空窗 |
| 天气+AQI | api.open-meteo.com + air-quality-api.open-meteo.com | 卡片隐藏 |
| IP 定位 | ipwho.is（可手动改城市，geocoding-api.open-meteo.com） | — |
| 汇率 | open.er-api.com/v6/latest/CNY | 12h 缓存→更旧缓存→隐藏 |
| 每日热点 | 60s.viki.moe/v2/60s | 卡片隐藏 |
| 翻译 | api.mymemory.translated.net | toast 提示暂不可用 |
| 治愈一刻 | api.thecatapi.com / dog.ceo | 内置奶油风插画 |
| Bing 壁纸 | bing.biturl.top（CORS ✓）+ bing.com 图片直连 | 回落奶油风 |
| 语音速记 | 浏览器 Web Speech | 按钮置灰/提示 |
| B站内嵌 | player.bilibili.com iframe | 黑屏占位（在线功能） |
| 单词/节假日/书单/播客/食谱/每日一问 | 内置数据 | 永远可用 |

## 6. 云端部署（CloudBase）

**当前状态：物料 100% 就绪，因云开发账号未登录而未执行部署（登录需要用户扫码，AI 无法代办，见踩坑 #016）。**

### 部署步骤
```bash
npm i -g @cloudbase/cli
tcb login                      # 扫码登录
ENV_ID=你的环境ID bash deploy.sh
```
脚本自动完成：注入 envId 到 js/00-env.js → 部署三个云函数（开 HTTP 访问）→ `node build.js --cloud` → 静态托管 cloud/hosting/。

### 架构
- **gate**：访问密码首次自设，服务端存 HMAC 哈希（`wb_auth` 集合），校验通过发 token。
- **sync**：`wb_data`（按 uid 的全量 KV，记录级 updatedAt 合并）、`wb_snapshots`（快照，留 30 份）。
  - push/pull/snapshot/snapshots/restore 五个 action。
  - 自动双向同步：本地变更 3s 防抖 push；启动 + 每 5 分钟 + 联网恢复时 pull。
  - **每日自动快照**：可在云控制台为 sync 函数加定时触发器（建议每日 03:00，event `{action:"snapshot-auto"}`——或在客户端部署定时任务调 snapshot；当前版本以手动备份+pull 保底）。
- **proxy**：微博热搜 / iTunes 播客搜索 / AI 代理（BYOK：GLM、DeepSeek、OpenAI 兼容；无 Key 时走云开发大模型 hunyuan-lite）。
- **PWA**：manifest + sw.js（cache-first 静态壳），手机/桌面可安装全屏。
- **前端**：`14-cloud.js` 仅在 https 环境且配置了 cloudBaseUrl 时激活；`file://` 本地版整体静默，数据仍只在本机。

### 数据库集合（自动创建）
`wb_auth`、`wb_data`、`wb_snapshots`——建议在控制台把它们设为"仅创建者可读写"加一道保险（函数走管理员 SDK 不受限）。

## 7. 桌面 App（Tauri 2）

- ✅ **已打通（2026-09-23），exe 与文件夹版功能 100% 一致**：本机装齐 rustup + Rust 1.98（MSVC）+ VS Build Tools 2022；`main.rs` 适配 autostart 插件 2.5（`LaunchAgent`，旧 `Launchd` API 已移除）与 global-shortcut handler 返回 `()`；`tauri.conf.json` 删掉新版插件不认的 `plugins.autostart` map；新增奶油风「晨」窗图标。
- 打包链（日常打包照此）：`node build.js --tauri` → `desktop/dist/`（index.html + css/js/vendor **全量拷贝**，含 12 个场景视频 38.7MB / 窗景页 / 3D 书架 / 7 个音源，无 `WB_SINGLE_FILE` 标记）→ `cd desktop && npx -y @tauri-apps/cli@2 build`（v2 CLI 不带 "tauri" 子命令）。`tauri.conf.json` 的 `frontendDist: "../dist"`。
- 产物：**58.9MB `desktop/src-tauri/target/release/personal-workbench.exe`**（托盘常驻 + 全局 Ctrl+K 唤出 + 开机自启可选，冒烟启动通过）。`desktop/dist/` 与 `src-tauri/target/` 已 .gitignore。云端 PWA 仍走 `node build.js --cloud` 单文件（音源已补拷，场景视频按设计禁用）。
- 备选 Electron：不再需要（Rust 链路已通）。

## 8. 构建脚本 build.js ✅

- `node build.js` → 根目录 `个人工作台.html`（~1.5MB：esbuild 压缩全部 CSS/JS 后内联，字体 base64，自动剥离 manifest 引用与 ThreeUI islands，注入 `window.WB_SINGLE_FILE=1` 标记）。
- `node build.js --cloud` → 另写 `cloud/hosting/{index.html, manifest.webmanifest, sw.js}`；sw 预缓存清单由构建自动生成（shell + islands core；场景页/书架 shelf.js 走运行时缓存，首次打开后离线可用）。
- 构建前会自动执行 `tools/build-islands.mjs`（ThreeUI 仓库在时重建 islands；否则用 vendor/threeui/islands 内已提交产物）。
- 内置校验：HTML 部分（剔除 script）不得残留本地相对引用。
- 注意：脚本里的字体路径经 `safePath()` 根目录边界校验（安全扫描要求）。

## 8b. ThreeUI 视觉扩展 ✅（2026-09-13）

来源：[ThreeUI Community](https://github.com/MengTo/threeui)（MengTo/Design+Code，MIT）本地仓库 `C:\Users\echo\Desktop\diy\灵感开发项目一\threeui`；许可副本在 `vendor/threeui/LICENSE`。React 组件经 esbuild 打成 IIFE「island」普通脚本（`tools/build-islands.mjs`，React 从素材仓库 node_modules 借用打包，工作区不装 React）。

**五大件：**
1. **动态壁纸**（设置→外观→背景，四选一：光斑/动态/Bing/自定义；旧键 wallpaper/themeCustomBg 自动迁移）：亮色=Condensation 晨露（Canvas2D，色板已改晨露蓝）；夜雾主题自动切 Constellation Field 星座（`vendor/threeui/pages/constellation-night.html`，自包含 iframe，色板已对齐夜雾）。挂载层 `#bg-shader`（islands/core.js 监听 data-theme 与 body.bg-dynamic-on）。
2. **番茄钟专注球体**：专注=液态珍珠（LiquidFormBackground，浅色主题 CSS invert 成奶油银珠，夜雾原生暗底）；休息/完成=暖金能量星球（EnergyOrb，hue 145/glow 1.5）。相位事件 `WB.bus "pomo:phase"`（idle/focus-running/focus-paused/rest-running/rest-paused/celebrate），宿主 `#focus-orb` 由 pomodoro.js 每次渲染搬进计时环后并发 `pomo:orb-attach` 强制重挂。
3. **窗景页三扇窗**（`js/modules/scenes.js` + `#scene-viewer` 浮层）：山雾四季 landscape.html / 和塔暮色 japanese-tower.html / 书房灯下 complete-shelf.html——全是自包含 HTML（2.4M/2.4M/0.9M），iframe 懒加载、关闭即清 src 释放 GPU；gsap 开窗动效；入口=今日页「窗外」行 + ⌘K 命令（"窗景"分组）。
4. **books 3D 书架**：BookshelfScene（three r165，封面纯程序生成无贴图文件），island `vendor/threeui/islands/shelf.js`（1.7MB）按需加载（点击「📚 3D 书架」才注入 script），容器随视图重建自动重挂（MutationObserver）。
5. **锦鲤引言卡**（今日页卡片，可拖拽翻卡）：致敬 koi-studies 的自制轻量版（纯 DOM/CSS，和纸+墨字+朱印），无 16.8MB 原素材负担。

**工程约定**：file:// 双击版全程兼容（islands 为普通脚本）；`WB_SINGLE_FILE=1` 单文件版自动隐藏窗景入口/书架按钮并剥离 islands（动态壁纸也随 islands 剥离，回退光斑）；性能内建保障——各组件自带 IntersectionObserver+document.hidden 暂停、DPR≤1.5、reduced-motion 回退（背景层由 html.no-motion 直接关闭）；新增设置键 `bgMode`。**不加新功能时的重构建**：`node tools/build-islands.mjs && node build.js`。

**踩坑**：见 `开发踩坑.md` #019–#023（npm 中文目录/esbuild define 引号/nodePaths 借依赖/组件布局 CSS 与双 canvas 裁剪/display 覆盖 hidden）。

## 8c. 动效与设计令牌规范 ✅（2026-09-18）

来源：`待优化.md`（动效/视觉/交互手感清单，12 项 P0–P3，全部完成）。**动效改动前先读它的 §4.3 约束与 §5「明确不做」。**

### 令牌（`css/main.css` 的 `:root`，全站唯一取值来源）

| 族 | 值 | 用途 |
|---|---|---|
| `--ease-std` | `cubic-bezier(.4,0,.2,1)` | 状态切换 / 错峰入场 |
| `--ease-pop` | `cubic-bezier(.34,1.35,.5,1)` | 弹层回弹：modal / cmdk / toast |
| `--ease-soft` | `cubic-bezier(.3,1.1,.4,1)` | 柔和上浮：sheet / 锦鲤翻卡 / 下拉回弹 |
| `--ease-flow` | `cubic-bezier(.22,.9,.34,1)` | 进度条 / 进度环 |
| `--ease-cross` | `cubic-bezier(.4,0,.2,1)` | 叠层交叉淡入 |
| `--ease-spin` | `cubic-bezier(.15,.9,.25,1)` | 长距离旋转（吃什么转盘） |
| `--dur-tap` / `--dur` / `--dur-in` / `--dur-scene` | `.18` / `.28` / `.38` / `.7s` | 微反馈 < 状态切换 < 入场 < 场景换场（**上限 700ms**） |
| `--blur-s/m/l` | `6` / `10` / `14px` | 唯一模糊来源；≤860px 自动降到 `4/6/8px` |
| `--z-*` | `0/1/90/110/120/150/160/180/190/200` | bg → content → bar → sheet → scrim → panel → toast → fx → alert → veil |

硬验收：`grep -rn "cubic-bezier" css/ js/` 只剩 `:root` 里的 6 档定义；`grep -n "z-index\|backdrop-filter" js/` 只剩受控的少数几处（动态元素一律读 CSS 变量）。

### 动效原子（`main.css` 的「动效原子」段）

| 类 / 函数 | 说明 |
|---|---|
| `.glass` | 玻璃浮层基类，`mask-composite` 挖 1.4px 渐变细边。`.modal/.cmdk/.toast/#scene-viewer .scene-frame` 复用同一套 mask 描边但**保留不透明底**（内容可读性优先，`.card` 明确不加毛玻璃） |
| `.icon-swap` + `WB.ui.swapIcon(btn, a, b, showB)` | 双图标交叉变形（±90° + `scale(.75)`）；用到 `.icon-btn` 上会自动覆盖 `display:inline-grid` |
| `.stagger-item` + `WB.ui.staggerIn(scope, step, base)` | 错峰入场。延迟用 CSS 变量 `--d` 传；内部只在目标 ≥2 个时才动手，单项弹窗自动跳过 |
| `.breathe` | 呼吸（3s / 6px / 恒定 `scale(1.03)`）；小元素用 `--breathe-y` 收幅度 |
| `.card.skeleton` | 骨架屏微光（`@keyframes skShine`，此前只有引用没有定义） |
| `body .flip-anim` | 拖拽 FLIP 位移。刻意用 `body` 后代选择器压过 `.list-row`/`.card` 自带的 transform 过渡 |
| `.pomo-flash` / `#pull-tip` / `.pet-art` | 动态创建元素的样式归属地（不要再写内联 style） |

### 三个关键机制

1. **冷却闸门 `WB.ui.lock(key, ms)`** —— `ms` 必须 ≥ 对应动画时长。已接 3 处：`theme`(700，对齐 `--dur-scene`)、`scene`(600)、`route`(400)。
   ⚠️ **路由的闸门只锁入场动画，不锁 `render()` 本身**：`go()` 先改 hash 再由 `hashchange` 触发 render，冷却期被丢掉的 render 不会补发，会出现「hash 变了视图没变」、「点了没反应」。冷却期内直接以终态呈现（踩坑 #031）。
2. **看相位，不看 DOM** —— `WB.modalOpen()` / `commands.isOpen()` / `router.sheetOpen()` 全部读各自的 phase 变量。退场动画期间 DOM 还在，但逻辑上必须已经算「关掉了」，否则那 200ms 内快捷键仍被弹窗栈拦（踩坑 #028、#033）。
3. **代次令牌** —— `scenes.js` 的 `gen`、`commands.js`/`router.js` 的 `token`，防止退场兜底定时器误伤「期间重新打开」的面板（踩坑 #035）。与闸门的分工：**闸门防重叠，令牌防误伤**。

### 主题 / 壁纸切换

- 所有**渐变**（`#sidebar`、`.blob`、`.koi-card`、`.btn.primary`）都走「叠层 + 交叉 `opacity`」——渐变不可插值。`#sidebar` 是 `::before`(亮)/`::after`(暗)；`.blob` 同上；`.koi-card` 是 `::after` 暗色和纸（顺带修了暗色下墨字不可读）；`.btn.primary` 用可插值的 `inset box-shadow` 做压暗蒙层（`--pri-mask`）。
- `meta[theme-color]` 在 `theme.apply()` 里同步（`#232339` / `#f6f1e7`）。
- **首帧主题**：`index.html` 的 `<head>` 里、样式表**之前**有一段同步脚本读 `localStorage["wb:settings"]` 写 `data-theme`/`data-accent`/`no-motion`。**键名（`wb:` 前缀）改动时要同步改它**。
- **壁纸对比度自适应**：`theme.adaptVeil(url)` 把图缩到 24×24 算平均亮度，写 `--wp-veil`（.55–.86）；远程图跨域会 taint canvas → 抛错即静默回落 CSS 默认 `.72`。
- `body.scrim-open`：有全屏遮罩时摘掉侧栏 `backdrop-filter`（减少**同时存在**的模糊层数，比加 `will-change` 有效）。摘除动作必须挂在遮罩**真正 remove 之后**（踩坑 #033）。
- 动态壁纸双槽位交叉淡入：`islands/core.jsx` 的 `bg-slot`，新层 `opacity:0` 挂载 → 下一帧 `.is-on` → 旧层 700ms 后卸载。**每个槽一个独立 React root**，用 `slotRoots` Map 成对管理（改了要 `node tools/build-islands.mjs`）。

### 无障碍

`prefers-reduced-motion: reduce` 作为**默认值来源**（用 `theme.motionUserSet()` 判断用户是否显式设置过），用户开关优先。

### no-motion 熔断的真实边界

`html.no-motion *{animation:none !important; transition:none !important}` 是唯一熔断点。它**对内联的 animation/transition 同样生效**（层叠里 `!important` 声明优先于 inline 声明），真正绕过它的是：

- **GSAP 补间**（不读 CSS）→ 必须自己加 `WB.ui.motionOff()` 守卫（`veilOut` / `animateCards` / `scenes.js` / 路由转场 / confetti 都加了）；
- **手写逐帧动画**（rAF / 定时器改属性）→ 同上（FLIP、下拉跟手都加了守卫）；
- **只靠 `transitionend` 收尾的逻辑** → no-motion 下 transition 被熔断、`transitionend` 永不触发，必须配 `setTimeout` 兜底。已有兜底：`scenes.js` 的 500ms 守卫（同时解决后台标签页 rAF 停摆，踩坑 #014）、`pullRelease()` 只清内联样式无副作用。

**重构建**：改了 `islands/*.jsx` → `node tools/build-islands.mjs && node build.js`；只改 css/js → `node build.js`。

## 8d. 沉浸专注层 ✅（2026-09-21）

来源：用户提供的另一个项目（Lumora，`C:\Users\echo\Desktop\diy\vibecoding\111`）的沉浸式番茄钟。可搬的四条：**绝对时间戳计时**（本项目早已有）、**SVG `stroke-dasharray` 细线环**、**逐字符定宽的巨型数字**、**F 键隐藏全部 UI + 全屏静默降级**。它没有、本项目补上的两条：**鼠标静止自动隐藏控件**、**结束用钟声 + 环境音淡出 + 一句提示语**（不用彩带粒子，贴合克制的气质）。

### 形态与层级

- 新增 `#focus-stage` 全屏层（`js/16-immersive.js` 动态建，挂 `body`），**不动原来的卡片式番茄页**；两者共享同一份 `WB.store["pomodoro"]` 状态，来回切不丢进度。
- 层级刻意选 **`--z-sheet`(110)**（不是 `--z-alert`）：压住 `#app`(1) 与 `#bottombar`(90)，又低于 `.modal-scrim`(120)、cmdk(150)、toast(160)、fx-canvas(180)、pomo-flash(190) —— 于是沉浸中弹确认框、吐司、彩带**都不需要额外抬层**。
- 应用外壳用 `body.immersive-on #app{opacity:0; pointer-events:none}` 隐藏（不卸载 DOM），`body.immersive-fade` 在进/出全程提供 `transition`，退场 760ms 后才摘掉。

### 场景与壁纸

| 场景 | 亮色主题 | 暗色主题 | 说明 |
|---|---|---|---|
| `mist` 晨雾（默认） | 米白→雾蓝淡紫 | 深蓝紫 | 最贴近工作台 |
| `deep` 深海 | 始终深蓝压暗 | 同左 | 长专注 |
| `ember` 篝火 | 暖橘奶油 | 深褐暖光 | 夜里不刺眼 |
| `star` 星野 | 始终墨蓝 | 同左 | 安静收尾 |
| `snow` 雪山 / `lake` 湖泊 / `sea` 碧海 / `meadow` 草甸 | 浅底深字（**湖泊在视频态翻转成浅字**） | 各自夜色调 | v6 世界风光组 |
| `dune` 沙丘 / `glade` 林间 / `fall` 飞瀑 / `cloud` 云海 | 浅底深字（**林间在视频态翻转成浅字**） | 各自夜色调 | v7 风光 B 组（2026-09-23） |

**共 12 套**：场景数由 `js/16-immersive.js` 的 `SCENES`/`SCENE_ORDER` 决定，同时要在 `css/main.css`（底/动态层/tint/ink/pick-dot）与 `js/modules/settings.js`（设置页 seg）各加一份 —— **设置页是第二处硬编码，加场景最容易漏**。`tools/test-immersive.mjs` 里有第三处（面板项数断言 + 场景遍历截图 + `FX` 关键帧映射）。

- 配色全是 `--fs-*` 变量（文字/描边/玻璃/雾面/按钮），**换场景靠可插值属性过渡**（color/stroke/background-color），而**背景渐变不可插值 → JS 叠一个旧场景槽位 `.fs-scene-out` 交叉淡出**（沿用 §8c 的老手法）。
- **场景动态层**（`.fs-fx` 的 `::before/::after`，每个场景两片，全部只动 `opacity`/`transform`）：

| 场景 | 动态 | 周期 |
|---|---|---|
| 晨雾 | 两道柔雾带横移 | 72s / 96s |
| 深海 | 四团不规则大柔光斑反向漂移（**不要用 repeating 条纹**，见踩坑 #046） | 44s / 67s |
| 篝火 | 火光不规则跳动 + 火星缓慢上浮 | 5.4s / 17s |
| 星野 | 5 层互质平铺的星点闪烁 + 银河斜带 | 6.5s / 9.5s |
| 雪山 / 湖泊 / 碧海 / 草甸（v6） | 雪粒下落 / 暖金波光呼吸 / 水面微光呼吸 / 两块云影掠过草坡 | 26 / 37 / 33 / 61s |
| 沙丘 / 林间 / 飞瀑 / 云海（v7） | 暖光带横扫沙脊 / 树冠天光呼吸 / 水雾浮沉 + 复用雪粒做溅水 / 两层云带横移 | 84 / 41 / 26 / 96s |

- **特征动画一律在 `::before`**（`glow`/云影等在 `::after`）——`tools/test-immersive.mjs` 约定"特征动画取 `::before` 的 animationName"来断言每个场景的动态层在跑，加场景时必须遵守这条分层。

- **进度色温**：`.fs-tint` 单色层，`opacity` 由 JS 按番茄进度写 `--fs-warmth`（0 → 0.28），过渡 1.4s linear；专注越往后场景"特征光"越浓，休息与待确认时归零。用**单色**而不是渐变，是为了换场景时能插值（渐变会硬跳）。
- 光斑漂移比全站 `.blob` 快一档：沉浸层用 `fsDrift`（22s/31s，幅度 ±5vmax），全站仍是 `blobFloat`（46/58s）。

### 循环视频背景（C 轮）

- **素材**：12 支 **1920×1080** / 24fps / 无音轨 / 正向无缝循环（结尾与开头 2~3.5s 交叉溶解），合计 **38.68 MB**，放 `vendor/video/{mist,deep,ember,star,snow,lake,sea,meadow,dune,glade,fall,cloud}.mp4`；来源、授权、转码命令全部记在 `vendor/video/README.md`。**2026-09-22（v4）**：用户评价篝火/星野「效果很不好」后——篝火由「黑底满屏火焰」换成室内壁炉 2707（3.41MB）、星野由「黄绿极光」换成银河+山脊 4148（5.71MB）。**2026-09-22（v5）**：场景由 4 套扩到 8 套，新增**室内组**（咖啡馆 808 热饮静物 / 书房 21612 烛光古地图 / 雨窗 99839 焦外灯斑 / 暖灯 3111 床头灯卧室），按用户口径「安静温馨、能平静专注、有电影感」重选——无人物、无强动态、浅景深或单点光源；四支合计仅 5.47MB（静物/暗调画面压缩率极高）。选素材**必须先核实授权档位**（踩坑 #051：Mixkit 优质夜景多为 Restricted 档，1080 直链返回 AccessDenied），下载失败时**先回素材页对直链**（踩坑 #052：Mixkit 有老库 `videos/{id}/` 与新库 `active_storage/video_items/{id}/{ts}/` 两套 CDN 格式，用错格式的报错与 Restricted 一模一样）。**2026-09-22（v6）**：室内组被评价「效果非常差」后整体换成**世界自然风光**（雪山 4283 / 湖泊 4998 / 碧海 5008 / 草甸 21577），口径定为"世界美景 + 电影感"。**2026-09-23（v7）**：8 套扩到 12 套（**风光 B 组**：沙丘 4149 / 林间 529 / 飞瀑 50566 / 云海 4695，新增 13.66MB）；原定的「秋色枫林」因 `autumn` 分类**整批 Restricted**（踩坑 #054）改由「云海之上」承接；**循环点的连续性 = 原片 `[start, start+F]` 的变化量**（不是"结尾像不像开头"），用 `PSNR(帧(start+F), 帧(start))` 量化，十二支实测 13.9~29.8 dB（踩坑 #055）。
- **选材标准**（v3 定稿）：实拍自然素材、构图上没有会与中央数字打架的小主体、暗部占比高（浅色文字可读）、动作慢但形态有张力。v1 的"深海"是游泳池底灯光、"篝火"是壁炉木柴特写、"晨雾"是 CGI 感白雾——都换了；v2 的篝火（野火小景）与星野（暗极光）因**冲击力不足**在 v3 又换成黑底满屏火焰与黄绿极光。
- **授权**：Mixkit **Stock Video Free License**（`copyrightNotice: "Free"`、可商用、**无需署名**）—— 注意它**不是 CC0**，不得把素材本身转售或打包成素材库再分发。授权正文由前端渲染、静态抓不到，台账里留了官方链接供核对。
- **加载策略**：`<video class="fs-video" muted loop playsinline preload="none">`，**只有沉浸层打开时才挂 src**，退出即 `pause()` + 清 `src` + `load()` 释放解码器与缓冲；换场景同步换片（先淡出露出 CSS 场景，新的 canplay 后淡入）。
- **不加载的三种情况**：单文件版（`WB_SINGLE_FILE`，保持自包含）、`html.no-motion`（尊重熔断）、设置里关掉「视频背景」。前两种会自动回落纯 CSS 场景。
- **可读性**：实拍素材明暗不可控，所以有视频时叠一层 `.fs-halo`（中心给数字、上下给控件），颜色取各场景的 `--fs-halo`（与文字色反极性）；**篝火那支还额外在视频态翻转整套配色极性**（实拍是暗木+亮火，与它 CSS 场景的奶油底深字相反）。
- **省电**：视频真播起来时 `#focus-stage.has-video` 会把场景自带的动态层 `opacity:0` + `animation-play-state:paused`（是真的停，不只是看不见）。
- **不写进 SW 预缓存**：首次进入沉浸时由运行时缓存，之后离线可用（安装包不因此变重）。
- 设置键：`pomoImmersiveVideo`（默认开）。
- **动态壁纸开着时**（`body.bg-dynamic-on.wb-islands`）场景层压到 20%，透出壁纸，只铺一层与主题同色的雾面（亮=奶油雾+深字，暗=夜雾+浅字）。**注意别在浅色壁纸上盖深色**（试过，画面发灰发泥）。单文件版没有 islands → 自动回落纯 CSS 场景。

### 交互与联动

| 能力 | 实现 |
|---|---|
| 进入 | 计时卡「沉浸」按钮 / 快捷键 `F` / ⌘K「进入沉浸专注」/ 设置项「开始专注时自动进入」 |
| 退出 | `Esc` / `F` / 右上角 ✕；`fullscreenchange` 反向同步（退了全屏就退沉浸） |
| 全屏 | `requestFullscreen().catch(() => {})` 静默降级；条件 `active && fsAsked && !fullscreenElement` 才算用户退出 |
| 自动隐藏 | 鼠标静止 3.2s / 触摸 4.6s → 控件 `opacity:0 + pointer-events:none`（500ms 过渡）；一动即回；点背景手动切换 |
| 空格 | 运行中=暂停 / 暂停=继续 / **待确认=推进到下一段**（绝不能走 `resume()`，会重复计时+重复记分） |
| 3-2-1 准备 | 3×1100ms 数字倒数 + 幕布模糊（`.fs-prep`），420ms 缩放入场，**不占用计时**（只是入场仪式） |
| 呼吸引导 | 休息运行中：环 19s `scale(1→1.055)` 与 4-7-8 文案（吸 4s/屏 7s/呼 8s）严格同步 |
| 环境音 | 进入时若没在播就 `restoreCombo()`；`pomo:start` 从 0 渐入 1.2s；专注中压低到 **55%**（`pomoSoundDuck`）；结束/放弃渐出 900ms 后静音 |
| 场景推荐音 | 在场景面板里换场景时，若当前**没有任何环境音在播**，提示一句"XX 配「YY」更好听 ♪（声音面板里选）"——**只提示、绝不自动改播放状态**；同一场景每次沉浸只说一次（`SOUND_HINT` 映射 + `hintShown`，进入沉浸时重置） |
| 键盘（沉浸中） | `←`/`→` 换场景（按 `SCENE_ORDER` 首尾循环，场景名浮 1.4s）、`M` 环境音开关（与 ♪ 同路径，按钮状态同步）、`P` 与空格同效。`M`/`P` **防连发**（按住不放会来回抖动），`←`/`→` 不防（快速翻场景是想要的） |
| 键盘（全站） | `M` = 环境音开关（与空格同义）、`P` = 番茄钟暂停/继续（任何页面都管用；待确认时推进到下一段；无会话时提示「现在没有进行中的番茄」）。沉浸里空格管计时、沉浸外空格管声音的既有语义**一律没动** |
| 场景入口（三处） | ① 沉浸层右上角「◐」面板；② **番茄钟计时卡底部的场景色点条**（准备态与进行态都有：12 个色点 + 当前场景名，当前项带描边）；③ **⌘K「沉浸场景」组**（搜中文名直接切；只在有查询词时出现，否则空面板会被 12 条刷屏）。三处共用 `WB.immersive.pick(id)`：记住偏好 + 在沉浸中立即换景 |
| 锁屏 | Media Session：metadata（阶段 + 时长 + 绑定对象）、play/pause/stop/nexttrack、`setPositionState` 进度 |
| 标题栏 | 沉浸中显示 `mm:ss · 专注/休息/已暂停/专注完成 · 个人工作台`；**结束闪烁优先**，退出即还原 |
| 移动端 | 沉浸中短路横滑切页与下拉刷新（`15-gestures.js`）；`env(safe-area-inset)` 内边距；≤600px 单独调字号与控件尺寸 |

### 新增设置键（`07-theme.js` 的 `DEFAULTS`）

`pomoImmersive`(开) / `pomoImmersiveAuto`(关) / `pomoImmersiveScene`("mist") / `pomoImmersiveFull`(开) / `pomoImmersiveGuide`(开，管准备+呼吸+自动隐藏) / `pomoSoundDuck`(开)。**不用写迁移**：`07-theme.js` 的 `all()` 是浅合并，新增键对老数据自动兜底。

### 声音层新增 API（`sound.js`）

`setDuck(to, ms)` / `duckReset(v)` / `duckLevel()`。要点：**两条链路必须一起管** —— 合成音源在 WebAudio 的 gain 节点上，六个真实录音 / 本地音乐 / 音频库是 `HTMLAudioElement.volume`；`duckFactor` 是独立乘数层，**不写进 `soundPrefs`**，所以和用户的音量滑块互不覆盖（`setVol` 末尾会重新落一遍乘数）。没有音源在播时只记目标值、不建 `AudioContext`、不排定时器（待机零成本）。

### 顺手修掉的两处旧账

1. **`pomo:finish` 是死订阅**：`sound.js` 一直在监听它，但全项目没人 emit → 番茄结束/放弃后声音面板不会静音。已在 `finish()/giveUp()/stopAndClear()` 里补上，并改成**先 900ms 淡出再静音**。
2. **`confirmBox` 点完按钮不关弹窗**（#042，HEAD 里就有）：全屏遮罩永久留在 DOM 挡住整页、Esc 也关不掉。只修了 `confirmBox` 这一条被本次功能踩到的路径（改成入 `WB.ui.modal` 栈 + 自己 `m.close()`），**其余 100+ 调用点保持原样**，是否统一改成「返回 falsy 即自动关」待决定。

### 回归

`node tools/test-immersive.mjs`（**155 项**：文件版 90 + 单文件版 65）覆盖：进出沉浸、全屏降级、环与数字逐秒推进、标题倒计时、静止自动隐藏、十二套场景的动态层关键帧、光斑提速、进度色温、场景交叉淡入、**← / → 换景与首尾回绕 + 偏好落盘、M 静音与 ♪ 按钮同步、番茄卡场景条、⌘K 场景命令、P 暂停/继续与轻提示**、**视频背景（src/静音循环/在播/盖上 CSS 动态层/换场景换片/退出即释放/单文件与 no-motion 不加载）**、空格三态、完成与放弃结算、`pomo:finish`、确认框层级与关闭、`no-motion` 熔断、动态壁纸透出、单文件降级；截图落 `.tmp/immersive/`。另有全站冒烟 `node tools/test-smoke.mjs` 兜底。

**两个 headless 坑**（详见踩坑 #043）：读过渡终值前要强制出帧；全新 profile 会自动弹晨间仪式，探针开头必须先关掉并写 `lastOpenDay`。

## 8e. 播客精选「收听」跳转修复 ✅（2026-09-23）

用户报告：播客精选里随便点开一档、点「收听」，小宇宙开出来的一律是「找不到了」空页面。

- **根因**：小宇宙**网页版没有搜索路由**，`www.xiaoyuzhoufm.com/search?q=xxx` 落到兜底空状态页（**HTTP 200**、标题「找不到了」，任何关键词都一样）。原来是「今日推荐卡」与列表「收听」两处都拼这个搜索链接（踩坑 #057）。
- **数据层**：`js/04b-data-content.js` 的 `PODCAST_CLEAN` 每行新增**第 4 列 = 小宇宙节目主页 ID**（即 `https://www.xiaoyuzhoufm.com/podcast/<id>`），22 档里 **18 档**已核实（逐个用无头浏览器真开页面、比对标题）；`自由客 / 日谈公园 / 不好笑俱乐部 / 山海四合` 4 档检索不到官方主页，第 4 列留空。
- **交互层**：`js/modules/podcasts.js` 新增 `listenXY(p)`（唯一汇聚点，两条路径共用）——有 ID 就 `window.open` 主页；没 ID 则**复制播客名 + 吐司**「已复制「X」，去小宇宙 App 里搜索收听」，按钮 `title` 同步说明。**不再打开任何链接**（此前 4 档会开空页）。Apple Podcasts 按钮不动（苹果搜索页是有效的）。
- **验证**（临时探针 15 项全过，用完即删）：数据层 6 项（22 档 / 18 档为 24 位十六进制 ID / 无重复 / 其余为空串）；交互层 6 项（18 档点击后的 `window.open` 目标逐个比对、**零次跳向 `/search?q=`**、4 档无 ID 的未开页面且都出了复制吐司、今日推荐卡同逻辑、控制台零错误）；网络层 2 项（**假 ID 反例标题为「找不到了」**；18 个 ID 真开页面标题都含节目名）。`node tools/test-smoke.mjs` 双形态 × 双主题 × 28 路由 0 错误；亮/暗两主题截图人工复核。
- **产物**：`node build.js --cloud` 同步重建单文件版与云端产物，并逐个核对 18 个 ID 都进了两份产物、`xiaoyuzhoufm.com/search` 字面量归零；`个人工作台.html` 1657.9 KB → **1658.6 KB（+0.7 KB / +0.04%）**。

## 8f. 读书推荐跳转体检 ✅（2026-09-23）

顺着播客那条线的同类排查：读书推荐有 3 个外部跳转，实测 **1 个不可用**。

- **豆瓣搜索（已修）**：原用新版 `search.douban.com/book/subject_search?search_text=`，是**纯前端 SPA** —— 实测两种失败形态：① 资源被拦（jQuery/main.js 全 `ERR_BLOCKED_BY_ORB` → 「正在搜索…」永远转圈）；② 换真实 UA 后页面直接说**「搜索访问太频繁。」**。改为**老版服务端渲染**入口 `https://www.douban.com/search?cat=1001&q=<书名>`（`DOUBAN_SEARCH` 常量，模块顶部注明原因）：5 本书实测都直接在 HTML 里给出「相关书籍 + 评分 + 评价人数」，脚本挂掉也能看（踩坑 #058）。
- **微信读书（未动，可用）**：`weread.qq.com/web/search/books?keyword=<书名>` 实测「出现在书名中的结果 共 N 条」。今日一书按钮与「点击书名」两处都用它。
- **同批体检的另外三个外链（未动）**：Apple Podcasts `podcasts.apple.com/search?term=…&entity=podcast`（Top Results 正常，播客模块那个按钮没问题）；Spotify `open.spotify.com/search/<q>`（未登录先给登录墙，平台设计）；网易云 `music.163.com/#/search/m/?keyword=…`（web 端搜索已强制登录，`keyword=` 与 `s=` 两种参数都只弹登录框，无登录态下无法判定，故保留原样）。
- **验证**：临时探针 9 项全过 —— 今日一书的「豆瓣搜索 / 微信读书」与「点书名」三次点击的目标 URL 逐个比对、无 `search.douban.com` 残留、卡片文案与行为一致、真开两个页面都出现结果且无风控提示、控制台零错误；`node tools/test-smoke.mjs` 双形态 × 双主题 × 28 路由 0 错误；亮/暗截图复核。双产物重建（`个人工作台.html` −208 B，注释被压缩器剥掉、URL 更短）。

## 8g. 界面一致性：状态刷新改成「数据推给界面」✅（2026-09-23）

用户报告：沉浸里暂停番茄钟、按 Esc 退出后，卡片仍写「🍅 专注中」。顺着这条线全项目排查，共 **14 处**同类缺陷（状态变了、已经渲染出去的界面留着旧值），一次性做结构性重做（踩坑 #066）。

### 三条刷新约定（新增功能请照此实现）

1. **低频状态变更 → 广播 `view:dirty`**：`09-router.js` 订阅后统一重绘当前视图。`pomodoro.js` 的 `begin/pause/resume/finish/giveUp/confirmNext/stopAndClear` 与沉浸层 `pick()` 都走它 —— 模块里**不要**再各自调 `WB.router.render()`（漏一处就会留下过期 DOM）。适用：暂停、完成、换场景、改偏好。
2. **高频更新 → 局部改文本**：秒级数据走 `pomo:tick` 订阅（今日页番茄卡只改两个文本节点）。**绝不**把整页重建挂到秒级事件上。
3. **弹窗关闭 → `modal:closed`**：router 已订阅，弹窗里改完数据关窗即自动刷新背后视图。注意 `06-ui.js` 的动作语义是 **「有 `onClick` 的按钮必须自己 `m.close()`；没有 `onClick` 的按钮才会自动关」** —— 本轮顺手把 10 处「按钮写着关/完成却不关窗」改成无 `onClick` 形式。

### 修复清单

| # | 场景 | 修法 |
|---|---|---|
| 1 | 沉浸里暂停/继续 → Esc 退出，卡片显示旧状态 | pause/resume 广播 `view:dirty` |
| 2 | 沉浸里 ←/→ 换景 → 退出，场景条高亮不跟随 | `pick()` 广播 `view:dirty` |
| 3 | 番茄倒数走完，卡片停在「专注中」、待确认按钮不出现 | `finish()` 补广播（原来漏了这句） |
| 4 | 离开期间走完，回来显示「00:00 · 专注中」 | 同 3 |
| 5 | 系统媒体面板（锁屏/耳机键）播放态与标题不跟随 | 抽 `mediaSync()` 挂到 `pomo:phase` |
| 6 | 结束的标题闪烁过后丢掉页名 | `stopFlash()` 改用 `WB.router.docTitle()` |
| 7 | 今日页番茄卡数字冻结、暂停后仍写「专注中」 | 订阅 `pomo:tick` 局部改文本 + 新增「已暂停 / 待确认」形态 |
| 8 | 跨天落在今日页，重复任务的当天实例不生成 | `materializeRepeats()` 提到路由层 render |
| 9 | 日历勾重复模板 ≠ 待办页语义 | 日历改走 `WB.todos.toggle` |
| 10 | ⌘K 存笔记后笔记列表不更新 | exec 补 render |
| 11 | 弹窗里改完用 ×/Esc 关，背后卡片留旧值 | router 订阅 `modal:closed` |
| 12 | 习惯/待办/记账/三大件之后徽章不结算 | 4 处补 `badgeCheck()` |
| 13 | 徽章墙开着时新解锁不点亮 | `liveBadgeGrid` + `paintBadgeGrid()` |
| 14 | 设置页上传/恢复背景后「背景」控件高亮不跟随 | 各处补 render（颜色选择器只在 `change` 时重绘，避免拖动卡顿） |
| 附 | `10-markdown.js` 用了 `esc` 却从未解构 → 笔记页一有笔记就整页崩 | 补 `const { esc } = WB` |
| 附 | 勾重复模板会新建模板链 → 今天凭空多出一条重复待办 | 改成推进模板自身日期 + 标记完成当天实例 |

### 验证

临时探针 8 项全过（① 用户报的那条 ② 换景跟随 ③ 倒数走完待确认 ④ 重复任务物化 ⑤ 模板语义 ⑥ ⌘K 存笔记 ⑦ 弹窗关闭重绘 ⑧ 标题含页名；零控制台错误）；`test-immersive.mjs` 155/155；`test-smoke.mjs` 双形态 × 双主题 × 28 路由 0 错误。

### 跨标签页同步（同日补充）

双开工作台时，另一个窗口改了数据，本页自动跟上：

- **发现层**（`02-store.js`）：监听 `storage` 事件 —— 它只在**其它**标签页触发（本页自己写自己收不到），只认 `wb:` 前缀、忽略 `netcache:`，命中就广播 `store:external`。
- **决策层**（`09-router.js`）：**能刷就刷，不能刷先记着** —— 弹窗开着、或焦点在输入框/文本域里时刷新，等于把用户正在填的东西连根拔掉，此时只标 `stalePending`；在「弹窗关闭」或「页面重新可见」（`visibilitychange`）时补刷。补刷时顺带调 `WB.pomodoro.syncTicker()`，让本页的番茄心跳与 store 对齐（另一页开始/暂停/清空都能跟上，状态是时间戳制不会跑偏）。
- **注意**：`file://` 打开时每个文件的 localStorage 是隔离的，双开同步只在 http(s)（含部署版）下生效 —— 验证要用本地静态服务起两个标签页（探针里内嵌了一个 node http server）。
- **验证**：双标签页探针 8 项全过（另一页新增待办自动出现 / 弹窗开着挂起、关闭补刷 / 输入框聚焦挂起 / 另一页开始番茄本页数字在跳 / 另一页暂停本页变已暂停 / 另一页清空本页回未开始形态），零页面错误；沉浸层 155 项、冒烟 28 路由无回归。

## 8h. 氛围引擎 ✅（2026-09-23，第五轮批 1）

来源：第五轮方案主线 🅐。要解决的是**资产闲置** —— 工作台已经有 12 套沉浸场景、12 支 1080p 循环视频、7 路音源、双主题、3 套点缀色，但它们**彼此不知道对方存在**，全靠手动开关。本层不造新素材、不加依赖，只做「编排」。

### 形态与边界

- 新增 `js/17-ambience.js`（script 排在 `16-immersive.js` 之后、`main.js` 之前 —— 它要调 immersive/sound 的接口）。
- **四段固定分界**：晨 06–11 / 昼 11–17 / 暮 17–21 / 夜 21–06（跨零点）。分界刻意不做成可配置：那是"作息表"不是"氛围"，用户要改的是**搭配**。
- **默认关闭**（`ambienceOn:false`）：编排在位但一次都不主动改用户的界面。设置页首次进入有一行引导（不是弹窗，不进 modal 栈），点「启用跟随节律」或「先不用」各记一次 `ambienceAsked`。
- **只在时段切换时应用一次**：`lastSeg` 门闩保证同一段内绝不重复动手 —— 用户在段内手动改的主题/场景/声音不会被引擎抢回去。
- **改当前时段的下拉会立刻应用并回一句轻提示**（`setSeg` → `apply(true)`）；改**别的**时段则什么都不做。门闩只管"系统自己触发"的路径，用户主动改的那一下必须同步可见（踩坑 #070）。
- **应用提示**：时段切换 / 启用开关 / 改搭配 / 点「立即应用」都会 toast 一句「✦ 跟着节律切到「夜」：星野 · Lo-Fi 心流 · 夜雾」；静默只留给启动那一秒（`apply(true, true)`）。
- **五格分两处落地**：沉浸专注 = **场景 / 音景**；首页 = **主题 / 点缀色 / 窗外**（外加一层随时段走的氛围层色温）。壁纸模式（光斑/动态/Bing/自定义）不在编排里。
- 场景走 `WB.immersive.pick(id)`，与用户在沉浸里点场景面板是**同一条路**；沉浸层的既有语义一个字没动，它仍然自己读 `pomoImmersiveScene`、自己管切换与提示。
- 设置页新分区「氛围编排」：**每个时段两行** —— `晨 06:00–11:00 · 当前`（场景含「不干预」/ 音景含「无」）+ `↳ 首页`（主题 / 点缀色含「不干预」/ 窗外含「不干预」），另有「立即应用当前时段」与「恢复默认编排」；当前那行标「· 当前」并在说明位显示各格现值。九个控件挤一行读不出哪格管哪，所以拆两行。

### 首页氛围层（`#amb-veil`）

- `index.html` 里紧跟 `#bg-shader` 的整屏柔光层，`z-index:var(--z-bg)`；引擎关着时 `body` 上没有 `.amb-on`，整层不可见（老用户零影响）。
- 色温按段走：晨 `h34`（暖橙）/ 昼 `h200`（雾蓝）/ 暮 `h24`（暖橘）/ 夜 `h250`（蓝紫），端点第二色相 `h2` 各段不同（雾蓝/薄荷/淡紫/蓝），强度 `.18–.34`。**刻意压到只影响"光感"，不碰可读性**。
- **渐变怎么"呼吸"**：渐变不可插值（§8c 的老账），改用 `@property --amb-h/--amb-h2/--amb-o{syntax:"<number>"}` 注册成可补间的数字属性，`transition:--amb-h 1.6s linear` —— 段切换是呼吸不是硬跳；不支持 `@property` 的浏览器退化成硬切，功能不受影响。
- 主题相关浓淡全在 CSS：亮色 `1` / 暗色 `.62`；**壁纸开着收 `.34`、动态壁纸 `.5`**（暗色再各降一档）—— 浅底上叠重色会发灰发泥（同 §8d 的教训）。
- 调色口径：**低饱和目标下先降饱和度再降透明度**（初版 `s58%/α.36` 在浅色主题下是粉的，降到 `s40%/α.28` 才回到"暖奶油 + 淡紫"）。氛围类改动必须在**两套主题下各出一张截图**（踩坑 #071）。

### 窗外：只点亮入口，不自动开窗

`WB.ambience.nowWindow()` 返回当前时段那一扇（引擎没开或选「不干预」时为 `null`）；今日页的「窗外」行据此把标签换成「现在的窗外：」并把那一扇**排到行首 + 高亮**。**绝不自动开窗** —— 那是 2.4MB 的 iframe 弹层，得由用户点。单文件版不含窗景页，设置页那一格自动不出现。

### 音景：两个必须守住的点

1. **幂等** —— 新增 `WB.sound.setCombo(key)`：把七路自动音源精确切到指定一路，目标与现状一致时**零动作**，所以重复调用绝不打断正在播的音频。只动这七路，**本地音乐与我的音频库一律不碰**。
2. **自动播放策略** —— 新增 `WB.sound.unlocked()`（`ctx.state === "running"`）。没解锁时**不起播也不假装起播**：只切场景与主题，并在返回值里回 `soundBlocked:true`，设置页据此换成"音景要等你手动播放过一次声音之后才会自动切"。静默失败最贵的地方是它把"没生效"伪装成"生效了"（踩坑 #067）。

**「无」是清除不是省略**：某段音景选「无」时要**显式停掉引擎自己放起来的那一路**（`setCombo(null)`），而不是"这段不进入逻辑"——否则上一路继续响，开关形同虚设（踩坑 #068）。

### 新增设置键（`07-theme.js` 的 `DEFAULTS`）

`ambienceOn`(false) / `ambiencePlan`({}) / `ambienceAsked`(false)。三个键都落在 `settings` 里 → 云端与 WebDAV 的同步白名单天然覆盖，**不需要改同步层**；`all()` 是浅合并，老档案无需迁移。

### 唤醒时机

启动延后 1.4s（避开雾开与首屏卡片动画，且 immersive/sound 都已挂好）+ 每 60s + `visibilitychange` + `focus`（休眠/切走回来最容易跨过时段分界）。开关被打开时订阅 `settings:changed` 立即对齐一次 —— 派生状态不挂在具体入口上。

### 回归

临时探针 **37 项**（时段分界八处 + 跨零点、默认关闭时不动界面、启用后场景/主题落库、未解锁不起播且如实标记、同时段门闩、`setCombo` 幂等与换路、自定义只覆盖该段、改别的段不打断当前、恢复默认、设置页 4 行 × 3 下拉与引导行）；`test-smoke` 双形态 × 双主题 × 28 路由 0 错误；`test-immersive` **155/155**（动过 `sound.js` 导出，故跑专项）；人工复核三张截图（桌面亮 / 桌面暗 + 开着引擎 / 移动 420px）。单文件 1677 → **1682 KB（+5 KB）**。踩坑 #067–#069。

## 8i. 小雾团与晨雾絮语：关系深化 ✅（2026-09-23，第五轮批 2）

来源：第五轮方案主线 🅒。依据是 Finch **40,294 条评论**的情感依附分析（Appbot 2026-08）：忠诚来自**互惠关系**（我照顾它 = 我照顾自己），不是"可爱" —— 用户会给小鸟起名字、会说"因为它在等我"所以回来。反面教训同样重要：**数据丢失被体验为情感伤害**（丢了两年 streak 的用户写的不是"同步 bug"，而是"失去了我的小鸟"）。

### 三件套

| 能力 | 实现 | 数据 |
|---|---|---|
| 名字 | 絮语行渲染成 `☁️ 团团：<它在说>`（说话人 `.murmur-name` 点缀色）；四句状态话都插名字；弹窗标题即名字，「给它起个名字 / 改个名字」留空即回默认 | KV `companionName` |
| 第一人称絮语 | 9 条规则文案全部改成它对你说（"我替你高兴""我就守在旁边"），不再出现第三人称的宠物名 | `murmur:{date}`（当日基调机制未动） |
| 它记得的日子 | 每天冻结一句关于**昨天**的记忆，弹窗里倒序回放最近 5 条 | KV `companion:{date}` = `{date, line, done, pomo, sig, ts}` |

### 两个容易写错的地方

1. **记忆写在"第二天"而不是"当天"**：今天还没过完，能盖棺的只有昨天。入口是今日页渲染时调用的 `rememberYesterday()`（同日重复调用无副作用）。
2. **记忆必须带数据签名**：`sig = 完成数|番茄数|三大件|心情档`。签名没变就一个字都不重写（"记忆稳定"是产品语义），签名变了才重写 —— 因为本项目**支持补写历史**，"同一天只写一次"会把今天补写的昨天永久漏在记忆之外（踩坑 #073）。

### 旧忆卡：从"随机 3 条"改成"跨年对比"

同月同日（`MM-DD`）的日志与笔记**按年份倒序全列**（最多 5 条，一年一行），标签「去年的今天 / N 年前的今天」，右侧给原日期与「看全文 ↗」，摘录 60 → 120 字，≥2 个年份时补一句「同一天，你在 N 个年份里留下过字迹」。随机抽样等于把"跨年"这件事打散了 —— 要的是一眼看到同一个日子在几年里长成了什么样。

### 回归

临时探针 **20 项**；`test-smoke` 双形态 × 三主题 × 28 路由 0 错误；`test-immersive` 155/155；人工复核两张截图（旧忆跨年卡、宠物弹窗 —— 后者顺带查出**状态话里写死了"小雾团"**、改名后不跟随，已改成插名字）。单文件 1687 → **1691 KB（+4 KB）**。踩坑 #073。

## 9. 维护手册

- **改氛围编排**：时段定义与默认搭配在 `js/17-ambience.js` 的 `SEGS` / `DEFAULT_PLAN`；用户改动落在 `settings.ambiencePlan`（设置页「氛围编排」或「恢复默认编排」）。**加时段要看三处**：`SEGS`、`DEFAULT_PLAN`、设置页渲染 —— 设置页是第二处硬编码，最容易漏（同 §8d 加场景的教训）。
- **加一套主题**（皮肤类"成套资源"的通例：先把"它被记录在哪几处"列全再动手，踩坑 #072）：
  ① `css/main.css` 里新增 `html[data-theme="xxx"]{}`，**整套颜色变量都要给**（`:root` 只有令牌没有颜色，不存在"继承 light"）；
  ② `index.html` **首帧内联脚本**的白名单加 `"xxx"`（跑在样式表之前，漏了就是每次启动闪一下）；
  ③ `07-theme.js`：`DEFAULTS.theme` 注释、`apply()` 里的 `meta[theme-color]`、`WB.theme.THEME_CN` 各补一项；
  ④ 设置页「主题」的分段控件加一项；
  ⑤ 判断一律用**排除式**（`html:not([data-theme="dark"])`、`cur !== "dark"`），不要写 `=== "light"`；
  ⑥ `tools/test-smoke.mjs` 的 `THEMES` 加进去（新主题不进冒烟 = 没人守着）；
  ⑦ PWA 改动要 bump `sw.js` 的 `CACHE`。
- **加一个模块**：新建 `js/modules/xxx.js`（`WB.registerModule({id,title,icon,render(view,param)})`）→ `index.html` 加一行 script → `09-router.js` 的 groups 里加入 id → 重跑 `make-font-subset.py`（若新文案含生僻字）+ `node build.js`。
- **改节假日**：`js/11-calendar-util.js` 顶部 `HOLIDAY_RANGES`（2025 为官方确定值；**2026 为公布口径整理值，个别连休边界若与国务院最终通知有出入请直接改表**；2027 未公布留空）。
- **改内置内容**：`04-data-builtin.js`（语录/词库/今日一问）、`04b-data-content.js`(书单/播客/食谱/小确幸)。**播客每行第 4 列是小宇宙节目主页 ID**（留空则「收听」回退为复制节目名，见 §8e）。
- **字体再生成**：miniconda Python 装 fonttools+brotli 后跑 `python make-font-subset.py`（需 `.tmp/LXGWWenKaiLite-Regular.ttf`，下载地址见脚本内注释与踩坑 #003）。
- **常见问题**：见 `开发踩坑.md`（59 条，含原因与修法）。
- **改数据后界面不刷新**：先看 §8g 的三条刷新约定 —— 低频状态变更广播 `view:dirty`、秒级数据只改文本节点、弹窗关闭由 `modal:closed` 兜底。新写模块时别指望"调用方会记得 render"。
- **回归冒烟**：`node tools/test-smoke.mjs`（无头 Chrome 遍历全部路由×**三主题（light/dark/rose）**×双形态，收 console/page 错误，截图到 `.tmp/smoke/`，有错退出码 1）；`--fast` 只测文件夹版亮色。断网导致的资源加载失败**与第三方接口的 CORS 拒绝**（如 60s.viki.moe 限流 429 时不回 CORS 头）自动豁免——联网层设计为优雅降级。需要 `npm i`（devDependencies：esbuild、puppeteer-core）。
- **沉浸专注层专项**：`node tools/test-immersive.mjs`（119 项断言，双形态；截图到 `.tmp/immersive/`）。改了 `16-immersive.js` / `pomodoro.js` / `sound.js` 的 duck 相关逻辑 / `vendor/video/` 后必跑。**它只在无头环境跑，全屏、真机手感与视频的实际观感仍需人工过一遍。**

## 10. 与需求的偏差记录

| # | 需求原文 | 实际实现 | 原因 |
|---|---|---|---|
| 1 | 白噪音"真实录制音频 base64 内嵌" | 雨声/篝火已换 CC0 真实录音（freesound，来源与处理见 vendor/audio/README.md）：本地文件 URL 加载 + 2.5s 交叉淡化无缝循环，加载失败自动回落合成；2026-09-13 六源已全部替换为 CC0 真实录音（新增四路来自 OpenGameArt，台账见 vendor/audio/README.md）；合成器保留作缺文件回退 | base64 内嵌 4MB+ 会撑爆单文件且当时无授权素材；改为 CC0 素材随包分发 + 运行时 URL 加载，离线仍可用 |
| 2 | 语音速记"免费离线" | 保留功能，按联网能力降级处理 | Chrome/Edge 的 Web Speech 实际走云端识别 |
| 3 | 云端每日自动快照（服务端定时） | 手动"备份到云"已完成 + 服务端快照接口已就绪；定时触发器需登录控制台配置 | 定时器属账号侧配置，无法在未登录状态下代办（补配步骤见 §6） |
| 4 | 桌面 .exe 交付 | ✅ 已交付（2026-09-23）：Tauri 2 产出 58.9MB exe，与文件夹版功能 100% 一致（§7） | 构建链由单文件改为全量目录拷贝——起因是云端单文件版缺音源且按设计禁用场景视频，用户要求 exe 与文件夹版完全一致 |
| 5 | 追剧日历 | 已在需求 v14 评估阶段放弃（无需实现） | — |

## 11. 验收记录（2026-09-13）

- 23 个模块逐个渲染：全部 OK，控制台 0 报错。
- 交互冒烟：新增待办（弹窗→保存→列表显示）✓；⌘K 捕捉（"明天 交房租"→明天实例）✓；日期词解析（周五/3月5日）✓。
- 联网实测（浏览器内）：天气+AQI（IP 定位成功）✓、汇率 ✓、每日一句 ✓、热点 15 条 ✓、翻译/治愈一刻/壁纸端点 CORS 均已验证 ✓。
- 双形态：文件夹版双击即开 ✓；单文件版 `个人工作台.html` 双击即开、零控制台错误 ✓。
- 声音面板真实录音（2026-09-13 追加验收）：rain/fire 文件加载 ✓、播放与音量联动（单路×主音量）✓、缺文件回落合成并提示 ✓、文件恢复后关/开自动回到真实录音 ✓、控制台 0 报错 ✓。
- 双主题：晨雾奶油/夜雾 + 三套点缀色切换 ✓。
- 数据持久化：localStorage 重载不丢 ✓；IndexedDB 封面/音乐/涂鸦通道 ✓。
- ThreeUI 视觉扩展（2026-09-13 追加验收，详见 §8b）：动态壁纸亮色晨露/夜雾星座自动切换 ✓；番茄钟专注珍珠/休息暖金星球双相位 ✓（浅色下珍珠 invert 适配、时间标签奶油药丸底）；窗景三扇窗开/关（gsap 动效 + iframe 释放）✓；books 3D 书架按需加载与重挂 ✓；锦鲤引言卡拖拽翻卡 ✓；单文件版窗景/书架自动隐藏、islands 剥离、WB_SINGLE_FILE 生效 ✓；全流程控制台 0 报错 ✓。
- 数据护城河（2026-09-13 追加验收，M10）：每日快照自动拍/列表/删除 ✓；端到端恢复（拍快照→清空数据→恢复→数据回归）✓；7 天未导出提醒逻辑 ✓；`node tools/test-smoke.mjs` 双形态×双主题 110 路由截图零错误 ✓。
- 动效与设计令牌（2026-09-18 追加验收，M11，详见 §8c）：
  - 语法门禁：`js/**/*.js` 全量 `node --check` 通过；`grep -rn "cubic-bezier" css/ js/` 只剩 `:root` 的 6 档定义。
  - `node tools/test-smoke.mjs`：folder/single × light/dark **4 轮 × 28 路由，0 console/page 错误**。
  - 行为验收（无头浏览器逐条）：21 个新令牌全部可解析 ✓；`D` 键连按 2 次只切 1 次且太阳/月亮交叉变形 ✓；modal 的 3 个子节点错峰入场 ✓、`.leaving` 在跑而 `WB.modalOpen()` 立刻转 false ✓、退场清理后 `.modal-scrim` 计数归零 ✓；cmdk 9 条结果全部错峰 ✓、`isOpen()` 在退场开始即转 false ✓；路由冷却期内 `#view` 仍只有 1 个 `.view-inner` 且**有内容** ✓；`#sidebar::before/::after` 交叉过渡 `transition-duration:0.7s` ✓、`meta[theme-color]` 同步为 `#232339` ✓；`.card.skeleton` 命中的 `animationName` 为 `skShine` ✓；`#pull-tip` z-index=90（`--z-bar`）、内层呼吸动画生效 ✓；层级 token 实测 `sidebar 110 / app 1 / fx 180 / toast 160 / cmdk 150 / sheet 110 / bottombar 90` ✓；`prefers-reduced-motion:reduce` 下 `no-motion` 自动生效、`userSet=false`，显式打开动效后 `breathe` 恢复 ✓；移动端 420px 抽屉有显式关闭入口 + 24 项错峰 + 汉堡变叉号 + 退场后清理 ✓；FLIP 反向位移非空、120ms 后归零、`onReorder` 顺序正确 ✓。
  - 顺带修掉 5 个 bug：`$$` 未解构导致拖拽重排全程抛错（#030）、命令面板防抖悬空 TypeError（#034）、`body.scrim-open` 摘不掉（#033）、设置页改主题时顶栏图标不同步（#040）、`.icon-swap` 的 `!important` 压掉 `.mobile-only` 导致汉堡按钮在桌面端露出（#041）。后两条都是靠**截图/计算样式人工复核**发现的——冒烟测试与行为断言都没覆盖到。
  - 视觉人工复核：桌面 1440×900 的亮/暗今日页 + 设置页 + 弹窗退场中途、移动端 420px 抽屉打开态，均正常（侧栏渐变叠层、暗色锦鲤和纸可读、抽屉把手+显式关闭入口、汉堡已变叉号）。

- 沉浸专注层（2026-09-21，M12，详见 §8d）：
  - 语法门禁：`node --check` 对全部改动/新增 JS 通过（含 `js/16-immersive.js`）。
  - `node tools/test-smoke.mjs`：folder/single × light/dark **4 轮 × 28 路由，0 console/page 错误**。
  - `node tools/test-immersive.mjs`：**105 项断言全过**（folder 57 / single 48），覆盖进出沉浸、全屏被拒的降级、环与数字逐秒推进、标题倒计时、静止 3.2s 自动隐藏与一动即回、四套场景的动态层关键帧在跑、光斑提速到 22s、进度色温在长且由 CSS 承担过渡、场景交叉淡入（旧槽位移除）、**视频背景的加载/换片/释放与三种不加载分支**、空格三态（暂停/继续/待确认推进）、完成与放弃结算、`pomo:finish` 事件、确认框层级(120)与关闭清理、`no-motion` 熔断下功能不残、动态壁纸透出（亮=奶油雾+深字 / 暗=夜雾+浅字）、单文件版无 islands 且**不加载视频**回落纯 CSS 场景。
  - 视觉人工复核（`.tmp/immersive/`，桌面 1440×900 × 四场景 + 移动 400×800 + 亮/暗 × 纯 CSS/动态壁纸 / 控件隐藏态）：环 1.6px 细线起点在 12 点、数字居中不跳动、亮色下壁纸透出不再发灰（据此把雾面改成跟随主题）、移动端控件不溢出。
  - **动态观感那轮返工两次**（踩坑 #046）：深海先做成"方格纸"、再做成"竖条纹帘子"，最后改成 4 团不规则大柔光斑才对；星点半径 1.6px 在 1440 宽屏上看不见，提到 2.2–3px 才立起来 —— 装饰性元素必须在目标屏幕尺寸上人工看截图。
  - 体积：`个人工作台.html` 1563.8 KB → **1598.2 KB（+34.4 KB / +2.20%）** —— 视频**不进**单文件版（自包含），所以产物只多了代码与样式；仓库端新增 `vendor/video/` 14.12 MB（4 支 1080p）、`js/16-immersive.js` 26.5 KB、`tools/test-immersive.mjs` 26.6 KB（后者不进产物）。
  - **C 轮（视频背景）**：素材来自 Mixkit 的 4 支 Free-License 实拍短片，**1080p + 正向交叉溶解循环**共 14.12MB（台账 `vendor/video/README.md`）。视觉返工三次：① 篝火 v1 实拍素材把数字吞进木柴里 → 加 `.fs-halo` 三层可读性雾并翻转篝火视频态配色极性（踩坑 #048）；② 整体"不真实且不高清" → 720p+倒放波折循环换成 1080p+正向溶解循环并重选素材（踩坑 #050）；③ 篝火/星野"冲击力不足" → 换黑底满屏火焰与黄绿极光，篝火保持极性翻转、星野维持原极性。
  - **两次"看起来是 bug 其实不是"**：① 单文件版不加载视频是设计（断言当时没区分形态，已修）；② headless 里视频能正常解码与绘制，截图可直接用于视觉复核。
  - 顺带修掉 2 处旧账：`pomo:finish` 死订阅（结束/放弃现在会淡出后静音）、`confirmBox` 点按钮不关弹窗导致全屏遮罩残留（踩坑 #042）。
  - **待人工确认**：真实浏览器里的 `requestFullscreen()`（无头环境会被拒，按设计静默降级）、真机耳机/锁屏键控制、移动端安全区与手势让位。

## 12. 开发进度

- [x] 环境与第三方库（ECharts/GSAP/solarlunar/qrcode/字体子集 2.2MB）
- [x] M1 骨架与设计系统
- [x] M2 记录核心九模块
- [x] M3 仪式与工具（晨间/收工/番茄钟/声音面板/小花园/⌘K/愿望）
- [x] M4 高级视图（月历+时间轴/统计/周报/徽章/人生刻度/回收站）
- [x] M5 生活记录（书影剧/健康/技能）
- [x] M5.5 内容精选（热点/播客/读书推荐）
- [x] M6 联网与云端（API 全实测；云函数+同步客户端+PWA+AI+BYOK 物料齐备；部署待用户登录）
- [x] M7 桌面物料 + 移动适配（手势/底栏/触屏排序）；.exe 已于 2026-09-23 打包（§7）
- [x] M8 动画打磨、空状态、单文件构建、全量回归
- [x] M9 ThreeUI 视觉扩展（动态壁纸/番茄钟球体/窗景页×3/books 3D 书架/锦鲤引言卡 + 构建管线 esbuild 化，2026-09-13）
- [x] M10 数据护城河（每日自动快照 IndexedDB 保留 7 份 + 设置页恢复/删除 + 7 天未导出提醒；`tools/test-smoke.mjs` 一键回归冒烟，2026-09-13）
- [x] 声音面板全真实录音（2026-09-13）：六路 CC0 录音无缝循环（新增白噪/钢琴/氛围/Lo-Fi 四支，OGA+freesound），SW 预缓存 12 项，无头探针实测六路加载播放推进、零合成标注
- [x] Bugfix 轮（2026-09-13）：番茄钟剩余时间不走（闭包旧状态）、晨间仪式弹窗无按钮、今天页一书/播客推荐缺失（books/podcasts 漏导出）、快捷键 N/T/空格/D 失效（main.js 裸调 go + notes 漏导出）、空格键非番茄页报错、× 关闭弹窗后快捷键失灵（modal 栈漏弹，#028）；无头逐键+关闭路径实测全过，踩坑 #024–#028
- [x] M11 动效与设计令牌（2026-09-18）：缓动/时长/模糊/层级四族 token 补齐 + `.glass`/`.icon-swap`/`.stagger-item`/`.breathe`/`.card.skeleton`/FLIP 六组动效原子；冷却闸门 3 处接入；islands 双槽位交叉淡入；主题渐变叠层交叉；modal/cmdk/sheet 退场动画；下拉跟手；`prefers-reduced-motion` 直连。规范见 §8c，清单与偏差见 `待优化.md`，踩坑 #030–#039
- [x] M12 沉浸专注层（2026-09-21）：全屏四场景（晨雾/深海/篝火/星野）+ 1.6px 细线环 + 逐字符定宽巨型数字 + 3-2-1 准备 + 4-7-8 呼吸引导 + 静止自动隐藏控件 + **四套场景各自的动态层与进度色温** + **循环视频背景（4 支 720p，仅沉浸时加载）** + 环境音渐入/专注压低 55%/结束渐出静音 + Media Session 锁屏控制 + 标题倒计时 + 动态壁纸透出（跟随主题的雾面）；新增 7 个设置键与 `tools/test-immersive.mjs`、素材台账 `vendor/video/README.md`。规范与取舍见 §8d，踩坑 #042–#049
- [x] 沉浸视频素材 v4（2026-09-22）：篝火/星野「效果很不好」→ 篝火换成室内壁炉（Mixkit 2707，Free 档）、星野换成银河+锯齿山脊（Mixkit 4148，Free 档），8s 正向无缝循环，四支合计 14.12→12.70MB；排查中撞上 Mixkit Restricted 档陷阱（优质夜景素材免费版仅个人使用、1080 直链返回 AccessDenied ≠ 限流），授权核实清单与识别方法进 `vendor/video/README.md`（踩坑 #051）；CSS 合并 ember 视频态两组重复极性规则；冒烟 28 路由 + 沉浸层 105 项断言全过，视频态截图复核可读性
- [x] 沉浸场景 8 套（2026-09-22，v5）：新增室内组（咖啡馆/书房/雨窗/暖灯）四套 CSS 配色 + 动态层（热气呼吸/光柱浮尘/雨痕下滑/灯光呼吸）+ 四支 1080p 循环视频（Mixkit Free 档，合计 5.47MB）；「◐」由循环切换改为**场景选择面板**（4×2 网格 + 每个场景一个代表色点 + Esc 优先关面板不退沉浸），设置页场景选择器同步改两行网格；按用户「安静温馨 + 电影感」口径重选素材（无人物、无强动态、浅景深/单点光源）；**极性规则**：study/rain 视频态翻浅字、cafe 两种主题都保持深字（数字压在亮杯面上，暗色主题翻浅字会糊）；测试 119 项断言（新增场景面板 4 项 + 新场景截图/动态层/换片各 4 项）；踩坑 #052（Mixkit 两套 CDN 直链格式）
- [x] 沉浸场景 v6：室内组 → 世界风光（2026-09-22 当晚）：用户评价室内组四景「效果非常差」，整体换成**山湖冰川组**——雪山（Mixkit 4283 瑞士阿尔卑斯延时）/ 湖泊（4998 日落湖湾航拍）/ 碧海（5008 绿松石海湾俯瞰）/ 草甸（21577 草原远山），四支 1080p 合计 11.5MB（八支总计 25.02MB）；素材库两个硬约束：Mixkit 无「fjord」版权素材、**冰川/冰岛系列整批 Restricted**（12 支已列入不可用清单）；`meadow` 因近景树叶运动出现溶解重影，溶解加长到 3.5s 化解（踩坑 #053）；暗色主题下雪山/碧海单独放宽视频压暗（全局 brightness .86 会把明亮风景压灰）；全量 119 项断言 + 亮暗双主题截图复核通过
- [x] 沉浸场景 12 套：风光 B 组（2026-09-23，v7）：由 8 套扩到 12 套——沙丘（Mixkit 4149 撒哈拉沙丘延时）/ 林间（529 林间溪流）/ 飞瀑（50566 绿崖白瀑）/ 云海（4695 云海黄昏霞光），四支 1080p 合计 13.66MB（十二支总计 38.68MB）；原定「秋色枫林」因 `autumn` 分类**整批 Restricted**（14911/17746/25127/9940 全中）改由「云海之上」承接（踩坑 #054），并固化**缩略图先行筛查**流程（分类页 → 缩略图拼图目视 → 只对入围核授权）；极性：`glade` 视频态翻浅字（幽绿森林暗调，同 lake 手法），`dune`/`fall`/`cloud` 深字不翻转；pick 面板窄屏改三列（12 项四行）；新增**场景推荐环境音提示**（只提示、不动播放状态，`SOUND_HINT` 映射）；测试 127 项断言通过（文件版 76 + 单文件版 51）+ 全站冒烟 28 路由无错误 + 十二景亮暗双主题截图复核；踩坑 #055（循环点连续性 = 原片 `[start, start+F]` 的变化量，PSNR 量化，十二支实测 13.9~29.8 dB）
- [x] 沉浸层键盘与场景入口（2026-09-23，v7.1）：`←`/`→` 换场景（`SCENE_ORDER` 首尾循环 + 场景名浮 1.4s + 偏好落盘）、`M` 环境音开关（沉浸内外通用，与 ♪ 同路径并同步按钮态）、`P` 番茄钟暂停/继续（任何页面通用，带「已暂停 · 剩 mm:ss」轻提示；无会话时提示没有进行中的番茄）；番茄钟计时卡底部加**场景色点条**（12 点 + 当前名，准备态与进行态都有）、⌘K 加**「沉浸场景」组**（搜中文名直接切，只对有查询词时出现）；三处入口统一走 `WB.immersive.pick(id)`；沉浸层底部提示补键位；设置页快捷键清单补 M/P/←→；**既有空格语义一律未动**（沉浸管计时、沉浸外管声音）；测试 127→155 项（新增键盘换景回绕、M 同步、场景条、⌘K、P 提示），踩坑 #056（测试段落间的状态泄漏 + 只取最后一条 toast 的脆弱断言）
- [x] 界面一致性修复（2026-09-23）：用户报「沉浸里暂停后 Esc 退出，卡片仍写专注中」→ 全项目排查出 **14 处同类缺陷**（状态变了界面留旧值），做结构性重做：**低频状态变更广播 `view:dirty` 由路由层统一重绘**、**秒级数据只改文本节点**、**`modal:closed` 兜底弹窗关闭后的刷新**；连带修掉 Media Session 播放态不跟随、标题闪烁丢页名、今日页番茄卡不跳秒、重复任务跨天不物化、日历勾选语义与待办页不一致、⌘K 存笔记不刷新、徽章结算漏 4 个入口、徽章墙不即时点亮、设置页背景控件不跟随，外加 **10 处「按钮写着关/完成却不关窗」**；顺带挖出两个老 bug：`10-markdown.js` 的 `esc` 从未解构（笔记页一有笔记就整页崩）、勾选重复模板会新建模板链（今天凭空多出重复待办）。规范见 §8g，踩坑 #066；探针 8 项 + 沉浸层 155 项 + 冒烟 28 路由 ×4 组全过
- [x] 跨标签页同步（2026-09-23）：双开工作台时另一窗口的改动本页自动跟上 —— `02-store.js` 监听 `storage`（只认 `wb:` 前缀、忽略 netcache，广播 `store:external`），`09-router.js` **能刷就刷、不能刷先记着**（弹窗开着或焦点在输入框时挂起，弹窗关闭/页面重新可见时补刷，顺带 `WB.pomodoro.syncTicker()` 让番茄心跳对齐，另一页开始/暂停/清空本页都能跟上）；`file://` 下 localStorage 按文件隔离，双开同步仅 http(s) 生效；双标签页探针 8 项全过，沉浸层 155 项 + 冒烟 28 路由无回归
- [x] 播客精选「收听」跳转修复（2026-09-23）：小宇宙网页版没有搜索路由（`/search?q=` 是 200 的兜底「找不到了」页），改为**直达节目主页** —— `PODCAST_CLEAN` 补第 4 列节目 ID（22 档中 18 档已核实、逐个真开页面比对标题），4 档检索不到官方主页的走「复制节目名 + 吐司」回退、不再开空页；今日推荐卡与列表共用 `listenXY()`；探针 15 项（含假 ID 反例与 18 个主页标题核对）+ 冒烟 28 路由全过，双产物同步重建（+0.7 KB）；规范见 §8e，踩坑 #057
- [x] 读书推荐跳转体检（2026-09-23）：「豆瓣搜索」原用新版 SPA 入口（资源被拦就永远「正在搜索…」，换真实 UA 又被「搜索访问太频繁」拒），改用**老版服务端渲染**入口 `www.douban.com/search?cat=1001&q=`（5 本书实测 HTML 里直接有「相关书籍」）；微信读书 `weread.qq.com/web/search/books?keyword=` 实测可用（未动）；同批体检 Apple Podcasts 搜索（可用）、Spotify（登录墙，平台设计）、网易云（web 搜索已强制登录，无登录态无法判定，保留）；探针 9 项 + 冒烟 28 路由全过，双产物重建（−208 B）；见 §8f、踩坑 #058
- [x] M13 人性化与一致性（2026-09-23）：一轮全项目体验审查后的**零新增功能**优化，六批交付 —— ①**数据安全**：导入备份的「取消」不再清空本地数据（原实现里点「再想想」/Esc/点遮罩都走覆盖分支）、撤销条替代日常删除的确认弹窗、日志与笔记草稿保护；②**承诺一致**：⌘K「Shift+回车」真正生效、⌘K 空面板说明行不再像命令、空状态文案指向真实入口、待办提醒脱离番茄动画开关、晨间仪式「跳过」不再白做且补手动入口、跨凌晨 4 点自动换日；③**手感**：同页数据更新不再跳顶也不重放整页动画、待办拖拽写回的 `order` 真正生效、账本与心情翻月后不再被重渲染吃掉；④**键盘与可达**：Esc 提到 `isTyping` 之前（浮层里终于关得掉）、空格让位给聚焦按钮、弹窗焦点陷阱与初始焦点、图标按钮补 `aria-label`、次要文字对比度提到 AA（`--ink-2` 4.7:1 / `--ink-3` 3.4:1，暗色 4.4:1）；⑤**舒适 A**：行内骨架条、待办搜索与多选批量、已完成 60 条提示与展开、⌘K 支持「明天 15:00」、「记住上次清单与提前量」；⑥**舒适 B**：触屏热区 44px、软键盘 `--kb` 让位、图表库移出首屏（约 1MB 不再阻塞启动）、习惯页打卡记录快照（去掉 O(n²) 重解析）。顺带修 6 个真 bug：导入丢数据、事件总线丢第二参、彩带粒子负半径致特效永久静默停摆、待办拖拽假生效、变更后跳顶、浮层里 Esc 失效；新增踩坑 #059–#065；清单/验收/偏差记录见 `待优化.md`「第二轮 · 人性化与一致性」。轮末按用户确认追加**首启卡片折叠条**（常驻三大件/待办/日志，其余 12 张收在折叠条里一键展开；全新档案默认折叠、已布局过的老档案默认展开；实测首屏从 2.57 屏降到 1 屏），并修掉「已经 20719 天没有导出备份」的荒谬天数（从未导出时以 `firstSeenTs` 为提醒起点）
- [x] Tauri 桌面打包（2026-09-23）：58.9MB exe 已产出并冒烟通过（§7）
- [x] M14 新优化批 1 · 小件四件（2026-09-23）：总览一键动作行（开始番茄/写日志/记一笔/备份数据）、数据健康小卡（`datacare`，快照行异步回填带 isConnected 守卫）、自定义 CSS 注入（`customCss` 落 DEFAULTS，唯一挂载点 `theme.apply()` → `#wb-custom-css`，设置页 textarea 防抖自动保存）、今日聚焦习惯（`focusHabit` KV，聚焦行就地打卡复用 `quickCheck`——顺带把它从 `WB.habits` 字面量抽成模块级函数修掉一处 #030 同款静默失效）。清单见 `待优化.md`「第四轮」；后续批 2 晨雾絮语 / 批 3 宠物生命体 / 批 4 晨雾年报
- [x] M14 批 2 · 晨雾絮语（2026-09-23）：总览日期卡下一行规则式洞察（9 条规则按优先级、全本地数据、`murmur:{date}` 当日基调缓存 + 前提实时校验失效重选、措辞温柔不催促）；`WB.habits` 补导出 `invalidateLogs` 供总览页失效习惯快照。零依赖替代旧清单的「AI 周报反思」
- [x] M14 批 3 · 宠物生命体「小雾团」（2026-09-23）：絮语行首的四态宠物（🥚 蛋 / ☁️ 飘 / ☁️✨ 发光 / 🌙 静静陪——心情低落日优先级最高），只陪伴不惩罚；可点 / Enter 戳开弹窗看实时详情。Finch 式情感绑定的零负担实现（不做独立卡片）
- [x] M14 批 4 · 晨雾年报（2026-09-23）：`js/modules/yearReview.js`（无路由模块）Wrapped 式年度回顾——分节叙事大弹窗（完成待办/专注/最坚持习惯/心情气候/日志字数/账本/关键词/陪伴天数，空节隐藏）+ canvas 年度分享卡；入口 = ⌘K「年报」+ 12 月总览浮现按钮。待办年度口径按 `t.date`（无 doneAt 时间戳，与统计页一致）。四批全部完成，第四轮总结见 `待优化.md`
- [x] M15 批 1 · 氛围引擎（2026-09-23，第五轮）：按时段编排「场景 / 音景 / 主题」——四段固定分界（晨/昼/暮/夜）、内置默认搭配、每格可自定义（场景含「不干预」、音景含「无」）、**默认关闭**且只在时段切换时应用一次（段内手动改动不被抢回）；音景走新的幂等 `WB.sound.setCombo`，**未解锁音频时只切场景与主题并如实标记**（自动播放策略，踩坑 #067）；「无」= 显式停播而非省略（#068）；窄屏三下拉换行（#069）。探针 37 项 + 冒烟 28 路由 ×4 组 + 沉浸层 155/155，单文件 1682 KB（+5 KB）。规范见 §8h，清单见 `待优化.md`「第五轮」
- [x] M15 批 1 补 · 首页也跟着变（2026-09-23，用户追加）：编排从"只管沉浸层"扩成两处落地 —— 新增首页氛围层 `#amb-veil`（色温随时段：晨暖橙/昼雾蓝/暮暖橘/夜蓝紫，强度 .18–.34），**渐变靠 `@property` 注册色相实现 1.6s 呼吸**（不给 `@property` 的浏览器退化为硬切）；点缀色与窗外纳入编排（窗外只点亮今日页入口并排到行首，**绝不自动开窗**）；壁纸/动态壁纸开着时整层收淡到 .34/.5（浅底叠重色会发灰）；设置页每时段拆两行（沉浸 / 首页）。探针 22 项 + 冒烟 28 路由 + 沉浸层 155/155，1683 → 1686 KB
- [x] M15 批 1 补 2 · 第三套主题「暮霞粉」（2026-09-23，用户点名）：`html[data-theme="rose"]` 自成一套颜色变量（低饱和暖粉底 + 暖褐文字，`--ink-2` 4.93:1 / `--ink-3` 3.52:1），点缀仍是雾蓝淡紫；写死 `light` 的组件覆盖统一改成 `:not([data-theme="dark"])`；首帧脚本白名单、`meta[theme-color]`、`THEME_CN`、设置页四选一、**D 键记住上次用的亮色主题**、冒烟主题表加 `rose`、`sw.js` CACHE 升 v4。探针 12 项 + 冒烟 双形态 × **三主题** × 28 路由 0 错误 + 沉浸层 155/155，1686 → 1687 KB。规范见 §2 与 §9「加一套主题」，踩坑 #072
- [x] M16 批 2 · 关系深化（2026-09-23）：小雾团从"状态图标"变成"一段关系" —— **名字**（`companionName`，絮语行变 `☁️ 团团：…`、四句状态话带上名字、弹窗可起名/改名）、**它记得的日子**（每天冻结一句关于昨天的记忆存 `companion:日期`，弹窗倒序回放最近 5 条）、**第一人称絮语**（9 条规则全部改成它对你说）；旧忆卡改**跨年对比**（同月同日按年份倒序全列最多 5 条、标签「去年的今天」、摘录 60→120 字、多年补一句总结）。依据是 Finch 40,294 条评论的依附机制分析（互惠关系 > 可爱；数据丢失 = 情感伤害）。探针 20 项 + 冒烟 三主题 × 双形态 × 28 路由 + 沉浸层 155/155，1687 → 1691 KB。踩坑 #073（"只写一次"的幂等遇到"补写历史"就是永久漏记）
- [ ] 第五轮后续批次（🅑 时间的节拍 / 保留项 A4·C1·C2）
- [ ] （用户侧）CloudBase 登录后执行 deploy.sh
